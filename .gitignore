/build/
/build_linux/
/out/
/source/out/
/Output/
/windows/
/linux/
/mac/
/dist/
/installer/
/vcpkg/
/client_distribution/
/server_distribution/
/source/vcpkg_installed/
enc_temp_folder/
.cache/
/attic/user/
/attic/chucklefish/
/attic/debug/
/tiled/
/assets/user/
/assets/devel/
/assets/packed.pak
/mods/*.pak
/mods/*
*/.vs/
*.vs/
*.sln
*.suo
*.vcxproj*
CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Testing
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps


.vscode/

# Local History for Visual Studio Code
.history/

# Built Visual Studio Code Extensions
*.vsix
# Headless render harness (P-0): a LOCAL install holding a COPY of the save so the live world can
# never be touched. 545MB of world data -- regenerate it, never version it.
harness/

# Byproduct of boundary-inventory.py importing layering-lint.py via importlib (#199).
scripts/__pycache__/

# The board HTML is a PUBLISHING INTERMEDIATE, regenerated by `board-export.py --html` on the way
# to the artifact. docs/board.md is the tracked authority; this is 900KB of derived markup that
# would churn on every task update. Never tracked -- see board-export.py BOARD_ARTEFACTS.
docs/board.html

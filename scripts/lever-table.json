{
  "_comment": [
    "THE LEVER TABLE. The matrix is GENERATED from this file, so the set of levers under test is a",
    "reviewable artefact rather than a number somebody remembers. Add a row and the matrix grows by",
    "construction; there is no second place to edit.",
    "",
    "FIELDS, and why each one is mandatory:",
    "  key       the runtime config key, exactly as read by the engine.",
    "  baseline  the value the perf harness runs by default. ASSERTED against",
    "            harness/storage-perf/starbound.config before every leg -- a lever whose baseline has",
    "            drifted from the harness is measuring something nobody declared.",
    "  off       the value that REVERTS THE OPTIMISATION, i.e. restores the behaviour that existed",
    "            before the lever was written. null is not a placeholder: the runner REFUSES a table",
    "            with a null off-value, because guessing it is how an A/B measures nothing.",
    "  witness   the metric key the lever EXISTS TO MOVE. This is the load-bearing field. On",
    "            2026-08-04 an adaptive-border A/B ran with the lever off on BOTH legs and produced a",
    "            full table of plausible deltas; the tell was that lighting.calc.cells was identical",
    "            across the legs, and nothing in the tooling objected. The runner now objects: if the",
    "            witness does not move between baseline and off, the leg is VOID, not a result.",
    "  changesOutput  true when the two legs do not render the same image. Such a lever's cost delta",
    "            is not a free win and may not be quoted as one without saying what was traded.",
    "  why       what the lever does, in one line. Not narration -- it is what a reader needs to judge",
    "            whether the witness is the right witness.",
    "",
    "EVERY off-VALUE BELOW WAS READ OUT OF THE CODE, NOT REMEMBERED. Two would have been wrong from",
    "memory, and both would have silently produced a full matrix of artefacts:",
    "  * parallaxRefreshInterval=0 IS NOT 'off'. 0 is the ADAPTIVE sentinel, resolved to a per-scene N",
    "    before any clamp (StarBackdropPass.cpp: '0 = ADAPTIVE (default), 1 = off/direct, >1 = manual').",
    "    The harness baseline therefore runs the parallax cache ON. Setting it to 0 as the 'off' leg",
    "    would have compared the cache against itself.",
    "  * envRefreshInterval clamps anything <1 up to 1, and the cache only engages at >1. So 0 and 1",
    "    are the same behaviour, and 1 -- not 0 -- is the off-value.",
    "The two keys look like the same quantity, share a type, and are read three lines apart. They are",
    "not on a shared scale."
  ],
  "levers": [
    {
      "key": "envRefreshInterval",
      "baseline": 4,
      "off": 1,
      "witness": "render.cache.env.skipped",
      "changesOutput": true,
      "why": "Env cache: reuse the composed environment for N frames. At N=1 the cache does not engage at all (envCacheActive is interval>1), so the skip counter must fall to zero on the off leg. Output differs by BOUNDED STALENESS, not quality: a reused frame shows content up to N-1 frames old."
    },
    {
      "key": "parallaxRefreshInterval",
      "baseline": 0,
      "off": 1,
      "witness": "render.cache.parallax.skipped",
      "changesOutput": true,
      "why": "Parallax cache. Baseline 0 is ADAPTIVE -- the engine derives N per scene from layer drift rate, snapped to {1,2,4,8,16}. 1 is off/direct. The skip counter must fall to zero on the off leg. Bounded staleness as above, plus a documented sub-ULP quantisation from premultiplied blending."
    },
    {
      "key": "lightingTemporalDecouple",
      "baseline": true,
      "off": false,
      "witness": "lighting.temporal.skipped",
      "changesOutput": true,
      "why": "Reuse the previous lightmap on calm frames instead of recomputing every frame. Off is a byte-identical every-frame recompute, so the skip counter must fall to zero. On, a calm frame shows a lightmap up to lightingTemporalFloorMs old."
    },
    {
      "key": "renderDrawableCache",
      "baseline": true,
      "off": false,
      "witness": "render.drawable.parts.cached",
      "changesOutput": false,
      "why": "Cache built drawables across frames instead of rebuilding per call. Off takes the full-rebuild path, so the cached-parts counter must fall to zero. Byte-identity is verified by a dedicated shadowCompare diagnostic, modulo a documented sub-ULP float-ordering tolerance on position."
    },
    {
      "key": "renderVboOrphan",
      "baseline": true,
      "off": false,
      "witness": "render.vbo.orphaned",
      "changesOutput": false,
      "why": "Orphan the immediate VBO before re-writing it, so glBufferSubData does not implicitly synchronise against an in-flight draw. Byte-identical by construction -- same API calls, same data. The witness counts the orphaning call itself, which is the only quantity that can distinguish the arms: the lever's whole effect is stall cost and it moves no other counter."
    },
    {
      "key": "lightingGatherCache",
      "baseline": true,
      "off": false,
      "witness": "lighting.gather.hit",
      "changesOutput": false,
      "why": "Reuse the per-frame-invariant stable tile grid across frames; off is the original direct gather (B1+B2), documented as the clean A/B baseline. The hit counter is registered INSIDE the cache branch, so it is absent-then-zero on the off leg by construction."
    },
    {
      "key": "scriptProtoCacheEnabled",
      "baseline": true,
      "off": false,
      "witness": "script.proto.cache.loaded",
      "changesOutput": false,
      "why": "Load Lua chunks from the shared proto cache instead of recompiling per context. Off is documented as the original code path byte for byte. Both arms are counted, so the off leg moves script.proto.cache.bypassed while the witness falls to zero."
    },
    {
      "key": "backdropComposeMerge",
      "baseline": true,
      "off": false,
      "witness": "render.cache.env.compose_standalone",
      "changesOutput": false,
      "why": "Defer the env compose into the parallax pass so env+parallax are one full-screen pass instead of two. Proven algebraically byte-identical. The witness counts the STANDALONE arm -- the one the lever turns off -- because render.pass.parallax.compose.gpu_us fires on both arms and its count cannot say which ran. Not exactly zero on the baseline leg: a clause-2 recovery frame suppresses the merge and is separately counted by render.backdrop.compose_recovered."
    }
  ],
  "_gapsComment": [
    "WITNESS COVERAGE IS NOW COMPLETE FOR EVERY LEVER IN THE TABLE. It was not: four levers had no way",
    "to say whether they engaged, including the two with the largest measured wins in this project's",
    "history. The instrument was weakest exactly where the stakes were highest. Three counters closed",
    "three of them; the fourth was never a gap.",
    "",
    "  render.vbo.orphaned                   NEW -- counts the orphaning call itself. Nothing else moves",
    "                                        when this lever flips: it is byte-identical by construction",
    "                                        and its whole effect is stall cost.",
    "  script.proto.cache.loaded/.bypassed   NEW -- both arms, so the OFF leg moves a counter too. A",
    "                                        consumer differencing two snapshots cannot tell ABSENT from",
    "                                        ZERO, so a one-sided counter is only half a witness.",
    "  render.cache.env.compose_standalone   NEW -- counts the arm the lever turns OFF, because the",
    "                                        existing parallax-compose timer fires on both arms.",
    "  lighting.gather.hit                   ALREADY EXISTED. Recording this because the first pass filed",
    "                                        it as a gap and it was not: the counter registers through an",
    "                                        outcome() lambda, so a grep for Telemetry::counter near",
    "                                        'gather' does not find it. That is the same blind spot the",
    "                                        descriptor audit found in the clock ratchet -- an indirect",
    "                                        registration is invisible to a literal grep, and a survey",
    "                                        built on one under-reports the population it claims to cover.",
    "",
    "A TIMER WAS NOT SUBSTITUTED FOR ANY OF THESE. A timer delta asserts the RESULT and calls it the",
    "cause; the witness must assert that the code path CHANGED, which is a different question and the",
    "only one that can void a leg before its cost is read."
  ],
  "gaps": [],
  "_excludedComment": [
    "NOT LEVERS, and excluded for reasons that are not gaps:",
    "  lightingGpu, lightingWorldUpscale, lightingWorldSampleBilinear, lightingTonemap, newLighting,",
    "  monochromeLighting -- these CHANGE RENDERED OUTPUT. Their cost delta is a trade, not a win, and",
    "  quoting one as a free saving would be false. lightingGpu additionally has NO bit-identity oracle",
    "  against the CPU path (lightingGpuShadowCompare is documented as permanently red for an unrelated",
    "  reason: it can detect a change but cannot certify identity), and toggling it while",
    "  lightingWorldUpscale stays pinned at 2 gives the world shader a UV scale that does not match the",
    "  CPU lightmap it is sampling -- a coordinate mismatch, not a quality difference.",
    "  lightingGpuSpreadIterations -- hybrid. Below the converged value it changes pixels (8.8% differ",
    "  at 32 vs 48); at or above it is cost-only. The harness pins exactly 48, the boundary, so there is",
    "  no headroom to lower it without corrupting the comparison.",
    "  envMaxDriftStepPx, parallaxMaxDriftStepPx, lightingTemporalFloorMs, renderDrawableCachePerPart --",
    "  tuning parameters of a lever already in the table, not independent on/off switches.",
    "  vaoBake -- DEAD. Zero readers in source/; #129 reverted the lever it named and the key survived in",
    "  the pinned harness config controlling nothing. config-lint cannot see it: that gate polices",
    "  undeclared getOrDefault reads, and a key declared nowhere and read nowhere is invisible to it."
  ],
  "_pinnedComment": [
    "NOT LEVERS -- values that must hold on EVERY leg and are never flipped. The runner asserts these",
    "alongside the lever baselines before each leg, so a drift is caught before the measurement rather",
    "than inferred from it afterwards.",
    "",
    "serverFidelity: the adaptive-fidelity governor is NEGATIVE FEEDBACK on the very quantity owner",
    "`sim` measures -- StarWorldServerThread.cpp says so in its own comment: a slower tick lowers",
    "fidelity, which makes the next tick cheaper. Left on 'automatic' (the shipped default) an A/B on",
    "the sim budget measures the governor's response to the lever, not the lever. Pinned to 'high': the",
    "governor is disabled and the workload is constant AND maximal, so a lever's effect is visible",
    "rather than absorbed. 'high' rather than a lower rung because the harness already reports 72% idle",
    "-- automatic would have climbed to high anyway, so this pins the status quo instead of changing it."
  ],
  "pinned": {
    "serverFidelity": "high"
  }
}

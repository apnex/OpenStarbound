# Board audit — 2026-08-08

**Generated record of a one-off audit. Not regenerated; superseded by the next one.**

Every open and active board task checked against the tree at `849f97d2` on `integration`, by 15
read-only agents across 7 clusters, with every proposed change put through an adversarial refutation
pass before it was allowed to reach the board.

## Why this exists

`docs/board.md` carries this warning in its own header:

> Status is what the store says, which is not always what the tree says. A content audit on
> 2026-07-25 found three statuses wrong in BOTH directions. Verify against tree content before
> trusting a status to mean work did or did not ship.

That warning had never been acted on at scale. This is the first sweep that tested **correspondence**
rather than internal consistency — the board agreeing with itself proves nothing.

## Shape of the run

| | |
|---|---:|
| tasks audited | 37 (34 open + 3 active) |
| clusters | 7 |
| agents | 40 |
| changes proposed | 32 |
| **survived the skeptics** | **20** |
| refuted or corrected | 12 |
| found by the completeness critic | 10 |

**Twelve of thirty-two proposed changes were refuted**, and most refutations carried a *sharper*
correction rather than a flat rejection. That ratio is the argument for the pass: a board edit that
overstates what shipped removes a task from the queue permanently.

---

## Findings that survived adversarial verification

### `#250` — STILL_OPEN_TEXT_STALE

*cluster: obs-head · confidence: high*

**Claim.** The task is still genuinely open (no reader interface, no library-side sample vocabulary, no socket/OpenMetrics transport exists at HEAD), but BOTH its blockers are now closed (#235, #253) and three load-bearing figures in its body have rotted: the snapshot meta DOES now carry both clocks, the Python consumer inventory is 2,260 lines (not 1,932) for the same five files plus 831 more that #253 added, and the snapshot holds 150 keys, not 143. One claim is not rot but was never true: metrics_main has never called EngineBusyReader.

**Evidence.** OPEN: samplesFor() still in the executable at source/metrics/metrics_main.cpp:74 (its own comment at :80 says "When this moves into the library (task #250)"); no `enumerate(pid, window)` / reader base class anywhere (grep over source/metrics: only free functions ClientBusyReader::read, ThreadBusyReader::read, EngineBusyReader::measure); no socket/OpenMetrics code. BLOCKERS: #235 completed (source/metrics/StarThreadBusyReader.{hpp,cpp}, 74+147 lines, shipped 0eef793b) and #253 completed at HEAD 849f97d2. META CLAIM FALSE: source/core/StarTelemetryReporter.cpp:82-83 writes merged["tMonotonicNs"] and merged["tEpochNs"]; live artefact harness/profiles/obs-join-verify.snapshots/telemetry-11640884.json meta = {schema:4, tEpochNs, tMonotonicNs, vsync}. Shipped by 3a8c530e [#235] 2026-08-08 09:00, i.e. after the task was filed. PYTHON COUNT: at the filing commit 19fd9370 (2026-08-07 18:34) the five files were exactly 661/401/372/313/185 = 1,932; at HEAD `wc -l` gives 813/401/372/489/185 = 2,260, plus scripts/metrics-sample.py 317 + scripts/obs-join.py 514 = 3,091. KEYS: python3 -c 'len(json["metrics"])' on the same snapshot = 150 (file is 72KB, so "~70KB" holds). READERS: `git log -S"EngineBusyReader" -- source/metrics/metrics_main.cpp` returns NOTHING — established never present, not renamed or moved; EngineBusyReader/EngineBusyWindow are referenced only by source/test/metrics_test.cpp:180-233 and source/metrics/CMakeLists.txt. STILL TRUE: no retention anywhere (grep 'retention|prune|unlink|delete' over StarTelemetryReporter.cpp and StarClientApplication.cpp = 0 hits); /home/apnex/OpenStarbound/dev/storage/telemetry is still exactly 54,364 files / 250M.

**Board edit.** Clear `blocked by: #235, #253` — both are completed; #250 is runnable now. In scope item 5, replace "the snapshot meta carries no timestamp at all, only {schema, vsync}" with: the snapshot meta already carries BOTH clocks (schema 4: tEpochNs + tMonotonicNs, StarTelemetryReporter.cpp:82-83, shipped by #235) — so the two-clock rule is half-built and this item shrinks to carrying it on the SAMPLE. Update "143 keys" to 150. Update the unowned-half paragraph: 2,260 lines across the five original scripts (telemetry-window 813, metric-desc-lint 489), and 3,091 counting the two consumers #253 added (obs-join 514, metrics-sample 317) — the gap grew 60% while the row sat pending. Correct "main calls two readers by hand and they return DIFFERENT types (BusyReading vs EngineBusyWindow)": main calls ClientBusyReader and ThreadBusyReader, both returning BusyReading; EngineBusyReader has never been called from main and is exercised only by metrics_test. Add the fact that the predicted cost has now been PAID: scripts/metrics-sample.py shells out to dist/metrics and parses its TSV, which is the "reimplement or shell out" this task was filed to prevent.

### `#255` — STILL_OPEN_TEXT_STALE

*cluster: obs-head · confidence: high*

**Claim.** No matrix run with the joined artefacts has happened (the newest run predates the join), so the task is open — but one of its five 'what it produces' bullets names a metric that no longer exists and asserts a premise #253 already refuted, and one of its two blockers is closed.

**Evidence.** NOT RUN: newest matrix is harness/matrix/matrix-20260807-160513/ (mtime 2026-08-07 16:52) — `find harness/matrix -name '*.joined.json'` = 0 and `-name '*.sovereign.tsv'` = 0, so every banked leg is still three cells of four. STALE METRIC: the bullet "the first real test of cpu.unattributed.busy_ns, which has only ever read 0.0" — `grep -rn 'cpu.unattributed.busy_ns' source/ scripts/` returns exactly ONE hit, source/metrics/metrics_main.cpp:426, and it is the comment recording the RENAME; the live key is cpu.attribution.residual_ns (metrics_main.cpp:455, scripts/obs-join.py:190). Established as a rename, not an absence. Its premise is also dead: #253's be2ceeee unclamped it and 11 live intervals read -2,+5,0,-2,+6,-1,0,+2,-5,0,+2 ticks, both signs. BLOCKER: #253 completed; #254 still pending. HARNESS IS READY: render-profile.sh:184 runs metrics-sample.py --every 0.5 per leg and :422 joins before the leg ends; lever-matrix.sh:592-593 copies <label>.joined.json into the run dir. The 28-leg shape checks out: scripts/lever-table.json has 8 levers -> 3 repeats x (baseline + 8 off) + warmup = 28, and the 47-minute cost matches matrix-20260807-160513 (run id 16:05, dir mtime 16:52).

**Board edit.** Rewrite the fifth bullet: cpu.unattributed.busy_ns no longer exists — it is cpu.attribution.residual_ns, unclamped and signed since be2ceeee [#253], and it has already read non-zero on both signs across 11 live intervals. The matrix is no longer its FIRST test; it is the first test AT SCALE, i.e. whether the tick-quantisation reading survives 28 legs and load phases where threads actually exit. Drop #253 from `blocked by` (completed); #254 remains. Add that the per-leg wiring exists and is gate-checked (render-profile.sh:184/:422, lever-matrix.sh:592).

### `#135` — STILL_OPEN_TEXT_STALE

*cluster: render · confidence: high*

**Claim.** The work is genuinely still not started and the premise (P-3 has not landed) still holds, but every line reference in the block is wrong: the parallax refresh-decision state moved from StarBackdropPass.hpp:99-119 to :191-221, and the N-derivation gate body moved from StarBackdropPass.cpp:266-320 to :483-534 (cpp:266-320 is now the ENV cache's drift/motion term added by #177, a different gate entirely).

**Evidence.** grep -n on source/rendering/StarBackdropPass.hpp puts the six named members at 201 (RetainedSurface m_parallaxCache), 202 (m_parallaxCachePosition), 208 (m_parallaxCacheContentKey), 214 (m_parallaxStillFrames), 219 (m_parallaxRefreshDeferred), 221 (m_lastLoggedParallaxN); the block comment 'Parallax retained-cache (SP-2)' is at :191. source/rendering/StarBackdropPass.cpp:483 is '// CONTENT-ADAPTIVE refresh interval'; the derivation runs to :534 and uses only layer.speed (:505-506), dayLength (:499) and pixelRatio (:508), clamps n to 1..16 (:513-515) and to 4 when parallaxAnimated (:526-527). `git grep -niE 'occlu|visibility term' -- source/rendering/` returns only TilePainter's per-tile occludesBehind logic -- nothing in BackdropPass. So no visibility/occlusion term: established as never-existed, not renamed.

**Board edit.** Replace 'source/rendering/StarBackdropPass.hpp:99-119' with 'StarBackdropPass.hpp:191-221' and 'StarBackdropPass.cpp:266-320' with 'StarBackdropPass.cpp:483-534'. Everything else in the block stands.

### `#136` — STILL_OPEN_TEXT_STALE

*cluster: render · confidence: high*

**Claim.** The four Director-perceptual items are untouched (unverifiable by me), but two of the four 'VERIFIED PRESENT IN integration' file attributions are now wrong, the deploy claim is stale, and item (f)'s conversion target has actually shipped.

**Evidence.** (1) frameBufferGeneration is NOT in source/application/StarGlRenderSurface.hpp -- `git grep -n frameBufferGeneration -- source/` gives StarGlRenderSurface.hpp:240 as a COMMENT ('//     ++m_frameBufferGeneration;') only; the real declaration is source/application/StarRenderer.hpp:194, the impl source/application/StarRenderer_opengl.cpp:883, the consumer source/rendering/StarBackdropPass.cpp:173. Moved, not deleted. (2) render.cache.parallax.* is NOT in source/frontend/StarClientCommandProcessor.cpp -- `git grep -n '"render\.cache\.parallax' -- source/` gives StarBackdropPass.cpp:64-66 (registration) and StarClientApplication.cpp:1811-1813 (read). (3) STILL EXACT: source/test/retained_surface_test.cpp:22-28 is TEST(RetainedSurfaceTest, RecordFilledClearsInvalidation) with 'EXPECT_TRUE(s.invalidated({640,360}, 2.0f)); // zoom (pixelRatio) change'; source/test/CMakeLists.txt:50 puts it in core_tests. (4) The 'animated layers always draw frame 0' cite StarEnvironmentPainter.cpp:262-263 is now :275-276. (5) /home/apnex/OpenStarbound/dev/starbound is mtime 2026-08-05 11:21; HEAD 849f97d2 is 2026-08-08 12:11 -- the deploy is not 'the current integration build'. (6) Item (f)'s successor exists: scripts/render-motion.sh:81-93 asserts refreshed+skipped+bypassed_moving partition the frames via the [walkoracle] PASS/FAIL verdict. Note it is NOT registered in .github/workflows/gates.yml (only render-gate.sh --selftest is).

**Board edit.** In the VERIFIED PRESENT table: frameBufferGeneration -> source/application/StarRenderer.hpp (impl StarRenderer_opengl.cpp:883); render.cache.parallax.* -> source/rendering/StarBackdropPass.cpp (read at source/client/StarClientApplication.cpp). Change 'FREE PERF NOT TAKEN ... StarEnvironmentPainter.cpp:262-263' to :275-276. Drop or date-stamp 'the current integration build is DEPLOYED' (the dev binary is 3 days behind HEAD). Mark (f) DISCHARGED, not merely converted: the oracle is live in scripts/render-motion.sh:81-93 -- and note it is not in the CI gate set.

### `#138` — BLOCKER_RESOLVED

*cluster: render · confidence: high*

**Claim.** P-3 is still genuinely not started, but BOTH named sequencing prerequisites have shipped and are verifiable in the tree, so the only remaining gate is the two feasibility spikes -- and four of the block's line references have rotted.

**Evidence.** BLOCKERS DISCHARGED: #137's Air-Gap seam for BackdropPass is closed (BackdropPass::Input is a reference struct at StarBackdropPass.hpp:93-96; render_layering + render_docs_fresh gates green); #139's Phase 1(b) GL-state assertion pass exists and is armed -- GlPass::auditGlState at source/application/StarGlRenderSurface.cpp:624-676 (framebuffer/viewport/program), OpenGlRenderer::auditGlState at source/application/StarRenderer_opengl.cpp:1342-1380 (scissor/blend), counter render.glstate.mismatches registered at StarRenderer_opengl.cpp:100, fault injection via STAR_RENDERTEST_GLSTATE_DESYNC at :1387-1400. `bash scripts/ci/run-gates.sh` => 55 green, 1 SKIP (metrics_mutual, no GPU on this box), 0 red. NOT-STARTED CONFIRMED: renderParallaxLayers is still an N-layer immediate-mode loop -- StarEnvironmentPainter.cpp:250 (signature), :257 (for (auto& layer : layers)), :260-261 (R-D transparent skip); `grep -l 'sampler2D\[|sampler2DArray|layers\[' assets/opensb/rendering/effects/*.frag` returns nothing; `git grep -nE 'TEXTURE_2D_ARRAY|compiled parallax|single-pass compiled' -- source/ docs/` returns only docs/board.md:1424, the task's own text. The 7-file effect set named in the block is exactly what `git ls-files assets/opensb/rendering/effects/` shows. ROT: :238-249/:248 -> :250/:257/:260-261; StarRootLoader.cpp:113 -> source/game/StarRootLoader.cpp:142; StarBackdropPass.cpp:279-320 -> :483-534; StarBackdropPass.hpp:99-119 -> :191-221.

**Board edit.** Rewrite the SEQUENCING paragraph: #137 and #139-Phase-1(b) are both DONE and verified in-tree; the only remaining gate is the two feasibility spikes. Fix the four line refs: StarEnvironmentPainter.cpp:250/:257 with the R-D skip at :260-261; StarRootLoader.cpp:142; StarBackdropPass.cpp:483-534; StarBackdropPass.hpp:191-221.

### `#161` — STILL_OPEN_TEXT_STALE

*cluster: render · confidence: high*

**Claim.** None of the four Jacobi candidates is in the tree and the shipped-Jacobi description is accurate, but both counter line references -- the ones the block calls 'cheap because both counters already exist' and tells the reader to sweep -- point at the wrong lines.

**Evidence.** ACCURATE: GpuLightmapPass::spreadIterationsFor is at source/rendering/StarGpuLightmapPass.cpp:50, and :62 is `std::max(8u, (unsigned)std::ceil(maxEmission * lp.point.spreadMaxAir))` then capped -- exactly the min(cap, max(8, ...)) the block states. Iteration 0 is still seeded from raw emission: StarGpuLightmapPass.cpp:251 `m_renderer->setEffectTextureAlias("lightState", "emission")` under the comment 'iteration-0 light state == emission'. No warm-start, no multigrid, no residual early-exit: `git grep -niE 'warm.?start|red.?black|gauss.?seidel|multigrid|residual' -- source/` returns zero hits in the GPU spread path; the single Gauss-Seidel hit is source/base/StarCellularLightingOracle.hpp:28, a comment describing the CPU reference solver, not the GPU pass. Established as never-existed. STALE: lighting.gpu.spread.passes is registered at StarGpuLightmapPass.cpp:121, not :43 (:45 is only a comment mentioning it); lighting.gpu.spread.gpu_us is registered at :14 and bracketed at :210 (begin) / :297 (end), not :96/:183.

**Board edit.** In THE MEASUREMENT TO DO FIRST: change 'StarGpuLightmapPass.cpp:43' to ':121' and 'StarGpuLightmapPass.cpp:96/:183' to ':210/:297' (registration at :14).

### `#169` — STILL_OPEN_TEXT_STALE

*cluster: render · confidence: high*

**Claim.** The substance holds -- no F16C, no SIMD, no -march anywhere -- but both source citations are wrong: floatToHalf is at StarWorldClient.cpp:65 not :32, and there is no repo-root CMakeLists.txt at all, so 'CMakeLists.txt:295-296' names a file that does not exist.

**Evidence.** ACCURATE: `git grep -lnE '#include <immintrin.h>|_mm_[a-z]|__m128|__m256' -- source/ ':!source/extern/'` returns only source/game/StarWorldClient.cpp, and its single hit is line 58, a COMMENT ('Deliberately NOT F16C (_mm_cvtps_ph): it rounds half-to-EVEN and emits proper subnormals, while this ...') -- so zero SIMD is established as never-existed, and the comment independently corroborates the rounding-divergence argument. `grep -rn 'march=' --include=CMakeLists.txt --include='*.cmake' .` returns nothing. The evidence doc is real: docs/superpowers/specs/2026-07-25-lighting-cpu-budget-closure-design.md has '## 8. The levers' at :214 and the L3b paragraph at :277 -- the '_section 8 L3b_' citation is correct. STALE: `grep -n floatToHalf source/game/StarWorldClient.cpp` => 65 (definition), 2657 (call site); `ls CMakeLists.txt` => No such file; the -O3 -ffast-math flags are source/CMakeLists.txt:300-307 (GCC arm) and :331-335 (Clang arm).

**Board edit.** Change 'floatToHalf (StarWorldClient.cpp:32)' to 'StarWorldClient.cpp:65'. Change 'CMakeLists.txt:295-296 is -O3 -ffast-math only' to 'source/CMakeLists.txt:300-307 (GCC) and :331-335 (Clang) are -O3 -ffast-math only; there is no root CMakeLists.txt'. Optionally cite StarWorldClient.cpp:58, the in-tree comment that already records the deliberate non-use of _mm_cvtps_ph.

### `#191` — STILL_OPEN_TEXT_STALE

*cluster: render · confidence: high*

**Claim.** Blocker 1 is fully intact and every structural claim about it verifies exactly -- but the load-bearing denominator is wrong: WorldRenderData has 23 data members, not 35, so the slice is '5 of 23' and the 'game-layer struct with 35 members' framing overstates the target by 52%.

**Evidence.** ROT: `awk '/^struct WorldRenderData/,/^};/' source/game/StarWorldRenderData.hpp | grep -E '^\s+[A-Za-z_].*;' | nl` lists 24 entries of which one is the method `void clear();` => 23 data members (StarWorldRenderData.hpp:21-72). The same command against `git show b2ac6c27:source/game/StarWorldRenderData.hpp` also gives 24 -- so 35 was wrong when the block was written, not rotted since. VERIFIED INTACT: blocker 2 is done (struct EntityDrawables now at source/game/StarEntityRenderingTypes.hpp:95, with the migration note at StarWorldRenderData.hpp:17-19). TileDrawer still owns the value: StarTileDrawer.hpp:58 `WorldRenderData m_tempRenderData;` :59 `Mutex m_tempRenderDataMutex;` :35-36 renderData()/lockRenderData(). TilePainter : public TileDrawer at StarTilePainter.hpp:16. The scratch use is StarMaterialItem.cpp:349-370 (locker :350, WorldGeometry(3,3) :352, produceTerrainDrawables :367) -- the block says :349-366, a one-line offset, not worth editing. The read counts are EXACT: grep -oE 'renderData\.(geometry|tileMinPosition|tiles|lightMap|lightMinPosition)' gives TileDrawer geometry x4, tileMinPosition x2, tiles x4 and TilePainter geometry x3, lightMap x3, lightMinPosition x2. Both positions are still Vec2I (:26, :28), and strong_typedef exists at source/core/StarStrongTypedef.hpp:9. BLOCKER: still real -- #199 records the Director's game-layer deferral; the boundary ratchet exists and is green (`python3 scripts/boundary-inventory.py` => 213 push-sink terms across 51 files, exactly the ceiling 213 read from source/test/CMakeLists.txt), so it shows no degradation and no headroom.

**Board edit.** Change 'union = geometry, tileMinPosition, tiles, lightMap, lightMinPosition (5 of 35)' to '(5 of 23)' and 'a type-level change to a game-layer struct with 35 members' to '23 members'. Optionally add: the boundary ratchet #191 waits on currently sits AT its ceiling (213/213), i.e. not degrading.

### `#197` — STILL_OPEN_TEXT_STALE

*cluster: render · confidence: high*

**Claim.** The central claim -- that nothing asserts effect-parameter state -- is CONFIRMED at HEAD against the one instrument that could plausibly have covered it, but the bind-site inventory is wrong in both count and location: there are EIGHT switchEffectConfig("world") sites, not ten, none of the eight line numbers is right, and WorldPainter is not one of them.

**Evidence.** CONFIRMED: EffectParameter::parameterValue is still source/application/StarGlRenderSurface.hpp:30 (exact). The #139 GL-state audit -- the only in-tree assertion pass -- checks draw framebuffer, viewport and program (GlPass::auditGlState, StarGlRenderSurface.cpp:624-676) plus scissor and blend (OpenGlRenderer::auditGlState, StarRenderer_opengl.cpp:1342-1380) and touches no effect parameter. `git grep -rniE 'effectParameter|parameterValue|declared parameter' -- source/test/ scripts/` returns ZERO -- no unit test and no gate asserts parameter state; established as never-existed, and `bash scripts/ci/run-gates.sh` (55 green / 1 skip) covers the whole declared gate set with nothing in it for this. world.config still declares exactly the seven named parameters. The fullbright branch still sets only lightMapEnabled and lightMapMultiplier and leaves the other four stale (StarWorldPainter.cpp:300-305, versus the else-arm's :334-342). backdropCompose.config declares zero effectParameters and environmentCompose.config is gone (deleted in a4ead286). STALE: `git grep -n 'switchEffectConfig("world")' -- source/` returns 8 code sites -- StarClientApplication.cpp:574; StarBackdropPass.cpp:110,136,163,692,715; StarGpuLightmapPass.cpp:409,413 -- so :61/:87/:114/:630/:653, :280/:284 and :533 are all wrong, the count is 8 not 10, and StarWorldPainter.cpp contains no switchEffectConfig at all (it only setEffectParameter's). 'StarRenderer_opengl.cpp:844, "no cross-consumer param bleed"' is now :891. 'StarWorldPainter.cpp:256-303' is now :296-345.

**Board edit.** Replace the exposure sentence with: 'bound at EIGHT sites (StarBackdropPass.cpp:110,136,163,692,715; StarGpuLightmapPass.cpp:409,413; StarClientApplication.cpp:574 -- WorldPainter sets its parameters but never binds it)'. Fix StarRenderer_opengl.cpp:844 -> :891 and StarWorldPainter.cpp:256-303 -> :296-345. Add that #139(b) has SHIPPED, so option (b) is now an extension of the existing auditGlState rather than a new instrument.

### `#198` — STILL_OPEN_TEXT_STALE

*cluster: render · confidence: high*

**Claim.** ZERO TRACE verifies literally -- the grep still returns exactly one line and every other absence claim holds -- but that one line is at StarRenderer_opengl.cpp:106, not :80.

**Evidence.** `git grep -nE 'GL_DEPTH|DEPTH_TEST|depthBuffer|prepass|depthAttachment' -- source/` returns exactly one line: source/application/StarRenderer_opengl.cpp:106 `glDisable(GL_DEPTH_TEST);`. GlSurface's format fields are hdr/alpha/clear/multisample/sizeDiv at source/application/StarGlRenderSurface.hpp:115-119 with no depth face and no depth flag. `git grep -niE "painter's algorithm|z-prepass|opaque pass" -- '*.md'` outside docs/board.md returns nothing, so the 'no survey or design record anywhere' claim is established as never-existed rather than moved. #114 is still closed DEFERRED (docs/board.md:892). Both sequencing prerequisites are satisfied at HEAD: #174's motion harness exists (scripts/render-motion.sh, walk oracle at :81-93) and #139's GL-state audit exists and is armed (GlStateAudit at StarGlRenderSurface.cpp:624 / StarRenderer_opengl.cpp:1342, fault injection STAR_RENDERTEST_GLSTATE_DESYNC). `bash scripts/ci/run-gates.sh` => 55 green, 1 skip, 0 red: nothing new blocks the survey.

**Board edit.** Change 'StarRenderer_opengl.cpp:80 glDisable(GL_DEPTH_TEST)' to ':106'. Optionally note that both sequencing prerequisites (#174, #139) are satisfied in-tree, so the survey is unblocked and only unstarted.

### `#201` — ALREADY_DONE

*cluster: arch-ci · confidence: high*

**Claim.** The native-separator fix landed and is in the tree; boundary_fresh passes here, though I can only verify the code change, not the Windows job's colour.

**Evidence.** scripts/boundary-inventory.py:116 is `out.append(p.relative_to(REPO).as_posix())`, with the defect documented in-place at :109-115 ("on Windows every key here became `source\\game\\Star*.cpp`... boundary_fresh was RED on windows-latest from the day it was registered"). scripts/config-lint.py:127 normalises with `os.path.relpath(...).replace(os.sep, "/")` and :124 labels it cosmetic, exactly as the block describes. Commit e7368000 touched only those two files (9 + 6 lines). `bash scripts/ci/run-gates.sh` -> `boundary_fresh OK`. NOT ESTABLISHED: I could not query GitHub Actions from this environment, so the claim that runs 30194960202/30195729180/30198588776 are now superseded by a green Windows job is unchecked.

**Board edit.** Set status to done. Append: "VERIFIED AT HEAD 2026-08-08: boundary-inventory.py:116 carries as_posix() and boundary_fresh is green under scripts/ci/run-gates.sh. The Windows job's colour was not re-checked — no network from the audit environment."

### `#202` — ALREADY_DONE

*cluster: arch-ci · confidence: high*

**Claim.** Both L1 files are claimed, the application partition exists, the fence holds six files, and the gated residual is still 17 of 17 — all three changes plus the G10 correction are in the tree.

**Evidence.** scripts/render-inventory.py:94 lists "source/application/StarTextureAtlas.hpp", "source/application/StarRenderDiagnostics.hpp" under LAYERS L1; APPLICATION_NON_RENDER declared at :402-411 (15 names) and the unassigned scan extended to source/application at :414-416. `python3 scripts/render-inventory.py --json` returns `unassigned: []` and L1 with 10 files. `python3 scripts/layering-lint.py` -> "OpenGlRenderer appears 0 time(s) in code across 6 file(s), ceiling 0", with both new paths in the fence at scripts/layering-lint.py:59-60. docs/render/architecture-3-target-state.md:153-155 still totals 17 reads / 17 metered. docs/render/layer1-architecture.md:97 states the fence "holds six files rather than four". arch-graph.py namespace labels carry the library (system-boundaries.md:908 `L1_substrate_in_star_application`) and §7's treemap prose says the boxes are directories, not layers (:635-647). MINOR DRIFT: block says L1 goes to "10/4291 lines"; the instrument now measures 4536 lines for the same 10 files — tree growth, and the number is gate-generated so it is current.

**Board edit.** Set status to done. Change "L1 goes 8 files/3796 lines -> 10/4291" to "L1 goes 8 files/3796 lines -> 10 files; the line total is generated and reads 4536 at HEAD".

### `#208` — PARTIALLY_DONE

*cluster: arch-ci · confidence: high*

**Claim.** Four of the five register-field bullets and both other work items are genuinely open, the blocker #207 is still real — but the fifth bullet (rows in section 15's instrument table) already landed at c359fc30, and #256's register gate does not overlap the field additions the way the cluster brief assumes.

**Evidence.** NOT DONE — `consequence`: the component register header is `| name | kind | zone | duty | warrant | contents |` (TSSA:3131); no consequence column, and `grep -n consequence` finds only prose uses, none in a register row. NOT DONE — three-part UNWARRANTED: scripts/spec-consistency.py:478-523 still checks only (a) a warrant exists and (b) it matches CITES; there is no completeness clause. NOT DONE — gate re-homing: §15's instrument table (TSSA:5947-5952) still anchors grant_sweep, link_sweep and dedup_measure; the current-state spec-measures blocks are generated inside §15 (TSSA:5771-5780 "38 of the 53 components have no files yet", :5895-5904); host_api_neutral sits in §15's limits (:5995); boundary_ratchet appears at :6046 and :6172. NOT DONE — the clause itself: `grep -n 'handoff|overflow|timeout|validator' scripts/spec-consistency.py` returns nothing. ALREADY DONE — the §15 rows: TSSA:5961-5963 carry exactly the three rows the bullet asks for ("every handoff states a bound, a policy and an observer", "...states a timeout", "...names a validator, or names its absence"), each marked **designed, not built**; added by c359fc30 (2026-08-02, "section 14's rules reach the instrument table"). Their §14 backing tables exist: back-pressure with bound/on-overflow/observed-by at TSSA:5616-5623, the four-row timeout table at :5595-5600, the validator table at :5714-5718. OVERLAP WITH #256 IS NARROWER THAN ASSUMED: 94f44d59 added scripts/register-artefacts.py, which gates the register's EXISTING `contents` column against the tree (register_artefacts + register_artefacts_fires, both OK in run-gates.sh) and fixed grant-sweep's PASSTHROUGH omission of `metrics`. It adds no register FIELD, so none of #208's five bullets were delivered by it. BLOCKER STILL REAL: board table row for #207 reads **active**.

**Board edit.** Keep status pending and keep "blocked by: #207". Strike the fifth bullet "- the corresponding rows in section 15's instrument table" and replace with: "DONE at c359fc30 — §15 carries all three rows (bound/policy/observer, timeout, validator), each reading 'designed, not built'; §14's tables already hold the data an instrument would read. What is missing is the spec_consistency clause, not the rows." Add: "NOT overlapped by #256/94f44d59 — register-artefacts.py gates the existing `contents` column; it adds no new register field."

### `#66` — STILL_OPEN_TEXT_STALE

*cluster: sim-levers · confidence: high*

**Claim.** LEVER HUNT — nothing shipped and all three named symbols are still exactly where the task says, but two of its pointers have rotted: the cited integration SHA 373f9b3 is on NO branch (local or remote) at HEAD, and the "superseded in priority by #70" redirect now points at a completed task.

**Evidence.** Code present at HEAD, no early-out added: source/game/StarMovementController.cpp:718 `forEachMovingCollision` with `world()->query<PhysicsEntity>(region)` at :720, and `updateForceRegions` at :741 with `world()->query<PhysicsEntity>(boundBox)` at :798 — neither guarded by any contributor count. SHA: `git merge-base --is-ancestor 373f9b3b HEAD` => false, `git branch -a --contains 373f9b3b` => empty (the object still resolves in this clone: 373f9b3b "merge: forEach entity-query optimization — per-query levers (#1/#5/#4/#3)"), but the CONTENT is at HEAD — Lever #4 stamp dedup source/core/StarSpatialHash2D.hpp:207-226 and Lever #3 interior-cell skip :233-276. #70 is status completed on the board (line 110 index row). Provenance for the numbers survives: /root/kubebound/specs/2026-06-20-foreach-optimization.md and its input /root/frackin/perf-explore/explore.data (497MB, dated Jun 20) both still exist, so the 22.5% and the ~0.2% re-attribution are re-derivable, though against the 2026-06-20 binary, not this one.

**Board edit.** In #66's block: replace "already integrated at 373f9b3" with a content pointer ("integrated; verified at HEAD as StarSpatialHash2D.hpp:207-226 (#4) and :233-276 (#3) — the SHA 373f9b3 is unreachable from HEAD, pre-integration history"). Replace "Superseded in priority by task #70" with "#70 is DONE; #66 remains a deprioritised collision-workload lever with no successor claim on it". Add: "the ~0.2% is a share of the 2026-06-20 WST; six sim levers have shipped since (L-WIND-A, collision arena, entityCast, freshenCollision pass-1, L-LIQ-A, SKIP-0), so treat it as an ordering hint and re-measure before building. Source profile still on disk at /root/frackin/perf-explore/explore.data."

### `#90` — STILL_OPEN_TEXT_STALE

*cluster: sim-levers · confidence: high*

**Claim.** All six sub-levers are genuinely outstanding and 11 of the block's 13 line references are still exact — but the SKIP-0 flag site has drifted 122 lines, and the block's closing sentence ("#84 records that no server-thread profile has been taken") is the exact claim #84 itself retracted.

**Evidence.** Verified exact at HEAD: StarEntity.hpp:190 `virtual bool hasDamageSources() const;`, StarEntity.cpp:92 base returns false, the braceless gate StarDamageManager.cpp:84 (flag read) + :90 (`if (!skipEmptyDamageSources || causingEntity->hasDamageSources())`), the flag block at StarDamageManager.cpp:11-12, worldserver.config.patch:48, and all six overriders return true (StarNpc.hpp:179, StarVehicle.hpp:33, StarMonster.hpp:88, StarObject.hpp:125, StarProjectile.hpp:60, StarPlayer.hpp:141). Six sub-levers all still outstanding: StarObject.hpp:125 is still a bare `return true;` and Object::damageSources() StarObject.cpp:1229-1242 still jsonMerges touchDamageConfig with orientation->touchDamageConfig per call (SKIP-1 + L-OBJ-3, no memo anywhere in the file); StarMonster.hpp:88 still carries the verbatim deferred comment (SKIP-2); the selfDamageNotifications loop is still outside the gate at StarDamageManager.cpp:136 with the explaining comment at :88 (SKIP-3); the liquid timer still ticks unconditionally at StarObject.cpp:427-429 with the config test inside checkLiquidBroken at :1524-1531 (L-OBJ-4); StarObject.cpp:458-462 still ticks m_lightFlickering and m_emissionTimers on both sides with no skip (L-OBJ-5), against the PlantWind pattern at StarPlant.cpp:15-16 and :735-740. DRIFT 1: the block says the flag is stored at StarWorldServer.cpp:1625; at HEAD `DamageSourceSkip::enabled.store(...)` is at StarWorldServer.cpp:1747. DRIFT 2: the closing note is false — #175 closed the server-tick budget (tick.server.compute.damage.us at StarWorldServer.cpp:656, .entities.us at :654, both present in every matrix leg), and #84's own 2026-07-25 revision says "RESOLVED: 'NO server-thread profile exists at all' — false as of #175". Minor: SKIP-2's cited range StarMonster.cpp:287-298 covers only two of the three terms; the third (animationDamageParts -> partSources) runs at :302-355, so the function is 287-358.

**Board edit.** In #90: change "StarWorldServer.cpp:1625" to "StarWorldServer.cpp:1747"; change SKIP-2's "(StarMonster.cpp:287-298)" to "(StarMonster.cpp:287-358; the third term, animationDamageParts -> partSources, is :302-355)"; and DELETE the closing sentence "note #84 records that no server-thread profile has been taken since the render/lighting work began", replacing it with "the server budget now exists (#175): size these against tick.server.compute.damage.us / .entities.us — but see #84, an arm-vs-arm A/B at a vegetated scene is refused by the scene fingerprint, so these need the in-process A/B too."

### `#124` — PARTIALLY_DONE

*cluster: sim-levers · confidence: high*

**Claim.** LEVER HUNT — no code has shipped, but the task's own first two phases have: an approved design spec and an implementation plan for this exact lever exist on disk and the block cites neither, so it reads as not-yet-brainstormed when only the build remains.

**Evidence.** Artefacts the block does not cite: /root/kubebound/2026-07-05-lua-marshalling-findings.md (4-agent file:line-verified investigation), /root/kubebound/2026-07-05-lua-marshalling-t1-DESIGN.md whose header reads "Approved design (user, 2026-07-05)" and specifies five byte-identical levers (L-BT/L-VAR/L-ARG/L-PATH/L-RET) plus a two-counter split instrument, and /root/kubebound/2026-07-05-lua-marshalling-t1-plan.md (per-task implementation plan, branch feat/lua-marshalling-t1). The block's own cited file, /root/kubebound/2026-07-05-combat-callgraph-findings.md, exists. NOTHING BUILT, verified lever by lever at HEAD: no branch feat/lua-marshalling-t1 (`git branch -a`); no lua.wrap.callbacks / lua.wrap.jsonmeta counters (grep "lua.wrap" over source/ is empty); StarVariant.hpp has no firstNonTrivialIndex or trivially_destructible fold; contextGetPath still takes the moved buffer (source/core/StarLua.cpp:1103-1108 `std::string utf8Path = path.takeUtf8();`); StarBehaviorState.cpp:182 still does `m_functions.get(funcName)`. The quoted cost figures are still the findings doc's (createWrappedFunction 0.42% at StarLua.cpp:134 and :1456; the symbol is at StarLua.cpp:1202 / StarLua.hpp:594 at HEAD, unchanged).

**Board edit.** Retitle/rewrite #124 from "Brainstorm ... Brainstorm->design->build when user wants" to "BUILD-READY: brainstorm and design are DONE", and add the three uncited artefacts: findings /root/kubebound/2026-07-05-lua-marshalling-findings.md, APPROVED spec /root/kubebound/2026-07-05-lua-marshalling-t1-DESIGN.md (five byte-identical levers L-BT/L-VAR/L-ARG/L-PATH/L-RET, honest ceiling ~0.7-1.3% of process, plus a 2-counter createWrappedFunction split instrument), plan /root/kubebound/2026-07-05-lua-marshalling-t1-plan.md. State that nothing has shipped, with the five confirming sites (no feat/lua-marshalling-t1 branch, no lua.wrap.* counters, StarVariant.hpp unfolded, StarLua.cpp:1103-1108, StarBehaviorState.cpp:182).

### `#130` — STILL_OPEN_TEXT_STALE

*cluster: active · confidence: high*

**Claim.** The repo is live at the path the board names and is wired into the perf harness, but it has been dormant for a month, and the work that actually happened after STAGE 0 is an undocumented "by-reference blueprint library" arc (Tasks 1-7 of a 9-task plan) — not the "NEXT = HARDENING BACKLOG" items (2)/(3)/(4) the board says come next.

**Evidence.** `ls /home/apnex/base-in-a-box-reforged` — exists, so the board's path (and its "moved from /root" note) is correct; `/root/base-in-a-box-reforged` does NOT exist, confirming the move. `git log --format='%h %ad %s' --date=short` in that repo: 20 commits, EVERY one dated 2026-07-07; newest is 97b1dd6 "fix(recursion): capture a nested printer's name/description/icon with its ref"; `git branch -a` = master only; `git remote -v` = empty (no remote, nothing pushed anywhere); `git status --short` = clean; `git reflog --date=short` shows no entry after 2026-07-07. Today is 2026-08-08, so 32 days idle while marked active. 13 of the 20 commits implement /home/apnex/base-in-a-box-reforged/docs/2026-07-07-blueprint-library-plan.md (Tasks 1-9): 405a40b/e7c1b42/8092ccb (Tasks 1-3), 1bd4d9f (Tasks 5+6), 190c5a6 (Task 6b), 763311e explicitly "(Task 7 interim; proper console UI = follow-up)". Plan Task 8 (integration: self-ref corridor + 3-room hub, plan L218) and Task 9 (back-compat + OOM-floor regression, L231) have no commits. Neither the blueprint library nor its design/plan docs are mentioned anywhere in docs/board.md (`grep -ni 'blueprint library' docs/board.md` → 0 hits). The mod IS live in the OpenStarbound harness: /root/frackin/OpenStarbound/harness/sbinit-perf.config:6 lists "/home/apnex/base-in-a-box-reforged/mod" as an assetSource, corroborated by docs/superpowers/drafts/matrix-prereq-ledger.md:111.

**Board edit.** Keep status in_progress but rewrite the tail: after "Each = own design→fix→verify cycle." append — "STALLED 2026-07-07 (no commit since; 20 commits, branch master, no remote). What actually happened after STAGE 0 was NOT the hardening backlog: a by-reference blueprint-library arc was designed (docs/2026-07-07-blueprint-library-DESIGN.md) and planned as 9 tasks (…-plan.md), and Tasks 1-7 shipped (405a40b..97b1dd6), Task 7 as an explicit interim. Tasks 8 (integration corridor/hub) and 9 (back-compat + OOM-floor regression) are unstarted. The hardening backlog items (2)/(3)/(4) are therefore behind an unfinished library arc, not next. The mod is a live assetSource of the perf harness (harness/sbinit-perf.config:6), so changes to it perturb every matrix measurement."

### `#204` — STILL_OPEN_TEXT_STALE

*cluster: active · confidence: high*

**Claim.** The task is genuinely still open — the spec's own owed list is non-empty and #208 is pending — but every headline figure in its block has rotted, including the one a reader would act on ("Part I is 312 lines of placeholder", which #205 already superseded and the tree contradicts).

**Evidence.** OPEN, with instrument: docs/superpowers/specs/2026-08-01-target-state-system-architecture.md:6440 — "**15 of 53 components record something unresolved.** … Ratification requires it to be empty." #208 (pending) still holds register-fields/aggregate-review/gate-re-homing. STALE FIGURES, all measured at HEAD 849f97d2: `wc -l` on the spec = 6546, block says 4607. `python3 scripts/spec-model.py` → "53 components, 51 grant rows, 24 elements"; `python3 scripts/spec-derivations.py --check` → "OK -- ledger matches (53/53 components derived, 318/318 facets)"; block says 41 components. `scripts/ci/run-gates.sh` → "55 green, 1 SKIPPED and therefore UNVERIFIED: metrics_mutual" out of 56 declared steps in .github/workflows/gates.yml; block says "17 gates green". Part I is spec L64 ("# Part I — Foundations") to L939 = 876 written lines, not "312 lines of placeholder" — #205 (completed) already recorded 312→735. Section 10 is L3391–L4816 = 1426 lines, not 1691. Sub-task list "#205 spine, #207 levelling pass, #208 aggregate review" omits #256 TSSA-OBS (pending, commit 94f44d59, board L5878), a fourth TSSA sub-task filed 2026-08-07. Path claims that DO hold: spec path exact; survey envelope surveys/tssa-purpose-survey.md exists at repo root. SEPARATE LIVE DEFECT found while checking: two "of 41" figures survive all 56 gates because prose-claims.py's COUNT_DRIFT only matches `N of M components` (scripts/prose-claims.py:527,896) and CLOSURE_DRIFT only the table-row form (scripts/prose-claims.py:636) — spec L1929 "*Links N of 41* is the closure INCLUDING the entrypoint — `server` links 13 because it is itself plus 12 dependencies" against the generated block at L2745 "**server links 20 of 53 components.**", and spec L6043 "`client_headless` links 26 of 41" against generated L3123 "**client_headless links 35 of 53 components.**". These are stale, not historical: neither sentence is dated or hedged, and both are stating the current convention/constraint.

**Board edit.** Replace the first paragraph of #204's block with: "Branch integration (67 commits ahead of the locally-cached origin/integration). Spec at docs/superpowers/specs/2026-08-01-target-state-system-architecture.md, 6546 lines, 18 sections / 4 parts, 53 components, 55 of 56 gates green (metrics_mutual SKIPS — no GPU client on this machine)." Replace the REMAINING paragraph with: "REMAINING: content. Part I is written (876 lines, #205); the open work is §18's owed list — 15 of 53 components still record something unresolved, and ratification requires it to be empty." Change the sub-task line to "#205 spine, #207 levelling pass, #208 aggregate review, #256 TSSA-OBS." File a new task for the two ungated "of 41" figures at spec L1929 and L6043 — #209's COUNT_DRIFT/CLOSURE_DRIFT cannot see either form.

### `#207` — ALREADY_DONE

*cluster: active · confidence: high*

**Claim.** #207's only stated remaining item — celestial's vocabulary/lookup split — shipped on 2026-08-02 at 706c7f4a, and the blocker it names ("blocked on dividing StarCelestialDatabase.hpp") was found never to have been real, so an active task with no open work has been holding #208 blocked for 170 commits.

**Evidence.** `git merge-base --is-ancestor 706c7f4a HEAD` → true. 706c7f4a = "spec: split `celestial` -- 45 -> 48 components, and the split was already in the tree"; its body retires the named blocker in so many words: "THE TREE HAD ALREADY DONE IT. StarCelestialTypes/Parameters/Coordinate.hpp are the vocabulary; StarCelestialDatabase.hpp holds an abstract CelestialDatabase with CelestialMasterDatabase and CelestialSlaveDatabase beneath it." Confirmed in the tree: source/game/StarCelestialDatabase.hpp:17 `class CelestialDatabase`, :72 `class CelestialMasterDatabase : public CelestialDatabase`, :184 `class CelestialSlaveDatabase : public CelestialDatabase`. Confirmed in the spec at HEAD: `celestial` is VOCABULARY (L1293, L1960, kind table L3462 region), with `starmap` INTERFACE (L3359), `starmap_authority` BACKEND (L3360, L3439) and `starmap_participant` BACKEND (L3361, L3440) as separate register rows. #207's own next commit bfe38c92 is "board: export -- celestial split, 48 components [#207]" — i.e. the stored description contradicts its own commit list. Last [#207]-tagged commit is 2026-08-02; `git rev-list --count bfe38c92..HEAD` = 170. The levelling-pass claims themselves still hold: §3 carries N1.a-d, N2.a-d and N3.a-c (grep over spec L321-472); `python3 scripts/prose-claims.py --check` → "OK -- every checkable prose claim matches the registers", which is the gate that runs BARE_GOAL (scripts/prose-claims.py:332,386); §2's two tables are 4-column = 3 facets each (spec L255, L280); §6 is budgets 3 facets (L825 "| | budget | why this value | owner |"), owed 2 (L851), constraints 2 (L880), observability 2 (L906) — exactly as the block says, including "weakest is the constraints table … no failure-symptom column". Only the counts have rotted: block says 45 components / "18/18 gates green"; tree says 53 (spec-model.py) and 55 of 56 (run-gates.sh).

**Board edit.** Set #207 status to completed and unblock #208. Replace the final line "REMAINING under #207: `celestial`'s vocabulary/lookup split (blocked on dividing StarCelestialDatabase.hpp)." with "CLOSED 2026-08-02 at 706c7f4a: `celestial` split into `celestial` VOCABULARY + `starmap` INTERFACE + `starmap_authority`/`starmap_participant` BACKENDs. The named blocker was not real — StarCelestialDatabase.hpp already held the abstract base plus Master/Slave (source/game/StarCelestialDatabase.hpp:17,72,184); the register was carrying as one component what the source had separated into four." Update "(45 components)" → 53 and "18/18 gates green" → "55 of 56 gates green (metrics_mutual SKIPS)".

### `#4` — PARTIALLY_DONE

*cluster: stray · confidence: high*

**Claim.** Three of the task's four named items (B the four false comments, A the makeDoubled face leak, D the GlPass field bag) shipped under a DIFFERENT task id — [#160] "L1 FINISH (§9)" — in July 2026; only item C (the per-draw glTexParameteri hoist) is still present in the tree, and item E is half-stale. Every hpp:/cpp: line reference in the task body is dead: the code moved from StarRenderer_opengl.{hpp,cpp} to source/application/StarGlRenderSurface.{hpp,cpp} (083c6340 reorg + 14fb1192 GlFrameBuffer->GlSurface rename).

**Evidence.** ITEM B = DONE, commit 9f960e5e "L1 finish §9.1: the header stops teaching an architecture it does not implement". Instruments: (1) `grep -rn switchGlFrameBuffer source/` = 0 hits; the identifier survives only in docs/upstream/issues/{viewport,altclear,postprocess-passes}.md and docs/render/architecture-assessment.md:241 quoting the task. This is RENAMED-not-absent, established from the diff: `git show 9f960e5e | grep -c '^-.*switchGlFrameBuffer'` = 4, and the commit message states all four now name bindTarget. (2) `grep -rn 'entire argument for this component|needs the raw faces|reads pixels back out' source/` = 0 hits (matches only in docs/render/architecture-assessment.md:229-245). The replacement text is in-tree: StarGlRenderSurface.hpp:283 "(The header once made a STRONGER claim for this component...", hpp:165 "NOTHING outside this struct reaches the faces", hpp:167/262/425 "NO friend (§9 step 5)". (3) Gate run, read-only: `python3 scripts/comment-claims.py` -> "comment_claims: OK -- 0 file:line citations in 1018 source files" (this gate measures line-number citations in comments only, not comment truth — it covers B's line-ref half, not its semantic half).
ITEM A = DONE, commit fac31f86 "L1 finish §9.2: Maybe<Face> -- existence IS doubledness". StarGlRenderSurface.hpp:189 `Maybe<Face> back;   // the second face. Its EXISTENCE is doubledness. There is no `bool doubled`', hpp:136 `bool doubled() const { return back.isValid(); }`, hpp:97-107 Face has a move-only RAII dtor. StarGlRenderSurface.cpp:291-303: makeDoubled builds a LOCAL `Face second`, calls allocateFace on it, then `back.emplace(std::move(second))` — a throw from allocateFace destroys the local and reclaims the FBO, so the leak the task describes is no longer expressible. `grep -n 'bool doubled' source/` = 0 hits (member is gone, only the accessor remains).
ITEM C = STILL OPEN, unchanged. source/application/StarRenderer_opengl.cpp:1936 `for (auto const& vb : renderBuffer.vertexBuffers) {`; the four calls are at :1967-1971 (GL_TEXTURE_MIN_FILTER, MAG_FILTER, WRAP_S, WRAP_T) inside the nested `for (auto const& p : m_pass.effect()->textures)`; the loop ends at :1986 `glDrawArrays`. Args depend only on p.second (textureFiltering/textureAddressing), never on vb, exactly as filed. No hoist commit exists; §9's ordered step list (docs/render/architecture-assessment.md:373-386) never scheduled §6 C — it lists B, §8i, D+bind-key, §8iii, §8ii, residuals. So C is unscheduled, not silently done.
ITEM D = DONE, commit 7d210984 "L1 finish §9.3: the (target, face, size) bind key". StarGlRenderSurface.hpp:338 `void unbind(Vec2U const& screenSize) { bindTarget({}, screenSize); }`; `private:` sections now exist at hpp:382 and hpp:424 (the claim "ZERO private: keywords" is false at HEAD); the two hand-rolled inverse sites are now StarRenderer_opengl.cpp:510 and :553, both `m_pass.unbind(m_screenSize)` (grep '\.unbind(' over source/application/*.cpp returns exactly those two). The remaining public fields are the 8 flattened hot-path locations, deliberately so per hpp:290-298.
ITEM E = HALF-STALE. Its blocker sentence "FETCH UPSTREAM BEFORE FILING: our ref is the fork point, ~83+ commits stale" is no longer true: `git remote -v` shows an `upstream` remote fetched at c1886896, and board #144 (docs/board.md:1639) records "Merged 83 upstream commits (fc3d55e..2c7f972b)" at 854b4b7f; `git merge-base --is-ancestor 2c7f972b6 upstream/main` succeeds with `git rev-list --count 2c7f972b6..upstream/main` = 10, so the drift is now 10 commits, not 83. What is still open: both drafts still read "STATUS: DRAFT — NOT PUSHED, NOT OPENED" (docs/upstream/prs/postprocess-passes.md:1, shader-compile-recovery.md:1), and E's actual ask — declare in the PR that shader-compile-recovery is written against our already-refactored loadEffectConfig and is not a cherry-pick — is ABSENT: grep -i 'refactor|cherry-pick|upstream form|our tree|our fork|against upstream|GlEffects' over that 90-line file returns only the header line "Verified against upstream/main 2c7f972b6".

**Board edit.** Rewrite #4 in place rather than closing it. (1) Retitle to "L1-FIX residual: the per-draw glTexParameteri hoist (item C) + the two unfiled upstream PRs (item E)". (2) Mark items B, A and D DONE with their commits — B 9f960e5e (§9.1), A fac31f86 (§9.2), D 7d210984 (§9.3) — all shipped under [#160], not under this id; add a line saying so, since this task's id-space made the overlap invisible. (3) Delete every hpp:/cpp: line reference in the block (hpp:443, hpp:269, hpp:418, cpp:1034, cpp:2182, hpp:303-306, hpp:400) — the file was split and renamed; item C's live location is source/application/StarRenderer_opengl.cpp:1936-1986. (4) Replace item E's stale blocker sentence with: upstream fetched and 83 commits merged (854b4b7f, board #144); upstream/main now c1886896, 10 commits past the 2c7f972b6 the drafts were verified against; both drafts remain NOT PUSHED and the shader-compile-recovery draft still lacks the 'not a cherry-pick' declaration. (5) Note separately that docs/render/architecture-assessment.md §6.A (line 206) still carries the pre-fix `bool doubled` code and the sentence "It was available and was not taken", with no DONE marker, although fac31f86 took it — that doc paragraph is stale and reads as an open bug.

---

## Findings the skeptics refuted or corrected

Each of these was proposed by an auditor and then attacked by an independent read-only skeptic
instructed to default to *refuted* under uncertainty. Read the correction, not the original.

### `#252` — proposed STILL_OPEN_TEXT_STALE

**Proposed.** Nothing has moved out of core (all nine xName tables, both spec tables and Telemetry::snapshot() are still in source/core/StarTelemetry.cpp), so the task is open — but every measured number in it, including the two in its own subject line, has rotted upward since it was filed, and one of its two blockers is closed.

**Refutation / correction.** The verdict (task still open, every number stale) survives, but a load-bearing evidence claim does not, and the proposed board edit would write the error into the board. VERIFIED TRUE: HEAD 849f97d2 has StarTelemetry.cpp=597 (587 at 19fd9370), Reporter 90+30=120 (60+24=84), five scripts 813/401/372/489/185=2,260 (was 661/401/372/313/185=1,932), +obs-join 514 +metrics-sample 317=3,091; all nine xName tables (:342,:359,:365,:379,:385,:392,:399,:406,:414), c_ownerSpecs :469, c_ownerTotals :488 are still in source/core/StarTelemetry.cpp and grep for them outside that file returns ZERO hits, so nothing has moved to source/metrics/ and #252 is genuinely open; the +10 is entirely e368f046 (@@ -473,10 +473,20 @@, +11/-1, before snapshot()), eec79ca1 is net zero, 3a8c530e touched only the reporter; #235 completed, #255 pending; MetricDesc has exactly 11 fields and StarMetricDesc.hpp is 163 lines; the source vocabulary divergence is unchanged. VERIFIED FALSE: "Telemetry::snapshot() now starts at :495 and runs to EOF :597 ... snapshot() is still 103". snapshot() is 495-568 = 74 lines; 570-595 is `void Telemetry::reset()` (26 lines), and 597 closes the namespace. Same shape at the filing commit (snapshot 485-558, reset 560-585), so the board's "485-587 Telemetry::snapshot() ~103" was already a mislabel of a raw line-range, and the finding's "every board figure was exact when written" only holds if you read those figures as line ranges rather than as the function they name. reset() is registry mutation under the mutex -- store code the task itself files under "stays in core" -- so carrying it inside the projection total overstates the move: true projection at HEAD is tables+specs 342-493 = 152 plus snapshot() 74 = ~226, and ~226+120+60 = ~406 to move, not ~256/~436. The edit as written would re-inscribe "snapshot() 495-597 = 103" rather than fix it. Minor: "one of its two blockers is closed" is already rendered correctly -- docs/board.md:49 shows `blocked by #255` only (scripts/board-export.py:444 filters to live blockers); only the raw metadata at docs/board.md:5630 still lists #235, so that is a TaskUpdate cleanup, not stale board text.

**Corrected verdict.** STILL_OPEN_TEXT_STALE (verdict stands, accounting corrected): nothing has moved out of core and #252 is open; re-measure as StarTelemetry.cpp 597 with tables+specs 342-493 = ~152 and Telemetry::snapshot() 495-568 = 74 (NOT 495-597; 570-595 is Telemetry::reset(), store code that stays), Reporter 120, five scripts 2,260 / 3,091 with #253's two. That gives ~226 projection lines and ~406 to move, and it also corrects the board's original mislabel that counted reset() as part of snapshot(). Drop #235 from blockedBy metadata (task-store cleanup only; the rendered ranked row at docs/board.md:49 already shows `blocked by #255`).

### `#254` — proposed BLOCKER_RESOLVED

**Proposed.** The sole blocker (#253, the join) closed at HEAD and produced the exact input this task was waiting for, so #254 is now runnable; the work itself has NOT shipped — nothing in the tree renders anything.

**Refutation / correction.** The core verdict survives — #253 is `status: **completed**` (docs/board.md:5721), harness/profiles/obs-join-verify.joined.json exists (1,533,332 bytes) with exactly the claimed schema keys and 11 intervals, render-profile.sh:422 produces it per leg, obs-join.py really implements re-differencing (rediff() :112, DERIVED :63-65, COUNTER_SUFFIX :57) and the covered-span denominator (coveredS, :24-28), both selftests are green (11/11, 7/7), and no consumer of .series.json/.joined.json exists beyond `cp` in lever-matrix.sh:585-593. But the PROPOSED BOARD EDIT is factually wrong: it says the joined artefact holds "both sovereign readers + PMU + in-process on one epoch axis". Every interval in that file records `"pmu": {"available": false, "reason": "no PMU series supplied"}` and `sources.pmu` is null. This is structural, not a one-off: render-profile.sh:422 never passes --pmu, and the PMU is joined separately at run level by pmu-join.py (lever-matrix.sh:763-765), so NO per-leg .joined.json ever contains PMU. Writing "+ PMU" onto the board asserts a cell the artefact does not have. Second, smaller error: the cited grep is not reproducible — `grep -rEln 'matplotlib|plotly|svg|chart|plot' scripts/` returns six files, not three, additionally hitting scripts/arch-graph.py, scripts/composition-graphs.py and scripts/spec-consistency.py; and arch-graph.py:965-970 and :1118-1123 are LIVE code emitting `mermaid xychart-beta` bar charts, not prose. Nothing draws the joined file (that part holds), but "nothing in the tree renders anything" is false as written and it conceals an existing in-repo charting idiom relevant to how #254 should be built.

**Corrected verdict.** BLOCKER_RESOLVED — but the board edit must be corrected before it is applied. Clear `blocked by: #253`. Replace "The series JSON exists after [#235]; nothing draws it" with: the JOINED artefact now exists (harness/profiles/obs-join-verify.joined.json, 11 intervals, BOTH SOVEREIGN READERS — cpu.owner.*.busy_cores and gpu.engine.*.busy_ratio — plus in-process on one epoch axis) and is produced per leg by render-profile.sh:422; still nothing draws it. Do NOT claim the PMU is in it: obs-join.py supports --pmu but render-profile.sh never passes it, so every per-leg join records `pmu.available: false` and the PMU is joined separately at run level by pmu-join.py — getting the PMU onto the same axis is remaining work, not shipped work. Also note that re-differencing, the covered-span denominator AND the one-core-per-owner normalisation (DERIVED `.busy_cores`) are already implemented in scripts/obs-join.py, and that the repo already has a chart-emitting idiom in scripts/arch-graph.py (mermaid xychart-beta, :965 and :1118) that the renderer can follow — so this task is a renderer over an existing file rather than a pipeline.

### `#245` — proposed STILL_OPEN_TEXT_STALE

**Proposed.** The convergence sweep has NOT shipped, but three of the block's load-bearing figures are wrong at HEAD: the site count is 167 not 139, `unit` and `clock` are now populated at 56 production sites (so "ZERO production sites populate any of them" is false for two of the seven fields), and the "schema 3 to 4" migration already happened for an unrelated reason — the wire is at 4 today.

**Refutation / correction.** Two of the three headline figures reproduce exactly, but the third — the schema clause — is mischaracterized, and the board edit it produces is a fabricated instruction contradicted by the task's own spec.

VERIFIED (I re-ran everything): `scripts/metric-desc-lint.py --check` -> "167 MetricDesc site(s), 1 setting a prose field" (exit 0); board says 139; `git grep -c 'MetricDesc{' 0e7d610c -- source/` sums to 152. `--check-clocks` -> "109 of 166 ... (ceiling 109, slack 0). 57 DO declare one." Walking the tree with the script's own regexes: 57 sites carry MetricClock::, exactly one in source/test/telemetry_test.cpp, so 56 PRODUCTION sites, and the same 56 carry MetricUnit:: — the block's "ZERO production sites populate any of them" is indeed false for two of seven. The other five are production-zero (MetricSource::/MetricBoundedness:: outside StarMetricDesc.hpp are a static_assert at StarTelemetry.cpp:395,401 plus telemetry_test.cpp:67; prose only at telemetry_test.cpp:68-69). Landed in e368f046 (Sat Aug 8 09:58:08 +1000) and eec79ca1 (10:14:18), both "[#235]", both after the block's "verified 2026-08-07". #243 is done (docs/board.md:282, d3650330; detail block at 5185 reads "completed") and reg_ratchet is live at .github/workflows/gates.yml:253. MetricNode (source/core/StarTelemetry.cpp:13-40) carries a single `total`; no thread-CPU accumulator.

REFUTED: "the 'schema 3 to 4' migration already happened for an unrelated reason". It happened for exactly THIS reason. 9db54200's diff to source/core/StarTelemetry.cpp adds unit/clock/source/boundedness plus the three optional prose keys to Telemetry::snapshot() AND adds the 154-line block in StarMetricDesc.hpp headed "The fields below are the CONVERGENCE", citing docs/superpowers/specs/2026-08-06-metric-descriptor-convergence-design.md — the spec #245 names — and only then bumps 3 -> 4. e368f046's own commit message says so: "MetricClock has had the right enumerators since the descriptor convergence." So the bump IS #245's schema step, landed ahead of the task under #236's commit label, not an unrelated event.

Consequently the proposed retitle "this work is 4 -> 5" is invented and is contradicted in terms by the task's own spec, §7: "the bump is not the migration" — the migration is the consumer change plus site population. Nothing in the tree requires a further bump; populating already-emitted fields changes values, not wire shape. And part of the consumer half has ALSO shipped: scripts/telemetry-window.py:148-158 now forwards the whole descriptor ("FORWARD THE WHOLE DESCRIPTOR, not a hardcoded five") instead of ("type","domain","owner","cadence","role"), naming that exact defect. Writing "4 -> 5" onto the board would instruct a future implementer to bump a schema for no reason and would hide that the schema step is largely DONE.

Secondary overstatement: "all 167 MetricDesc sites" includes the 2 source/test/telemetry_test.cpp fixtures and disagrees by one with the clock ratchet's own 166 denominator (the two regexes differ by `\s*`). The production sweep is 165, and the spec explicitly requires excluding source/test/ ("a route to green that measures nothing").

**Corrected verdict.** STILL_OPEN_TEXT_STALE — with the schema clause rewritten. The count and field corrections stand: replace "all 139 MetricDesc sites" with "165 production MetricDesc sites (167 total via scripts/metric-desc-lint.py --check, of which 2 are test fixtures; the clock ratchet's denominator is 166)"; replace the last paragraph with "unit and clock are now declared at 56 production sites (e368f046 + eec79ca1, #235) and ratcheted — 109 of 166 sites still declare no clock, gate clock_ratchet, ceiling 109 slack 0. FIVE fields remain production-unpopulated: source, boundedness, measures, validWhen, whole (set only at source/test/telemetry_test.cpp:66-69)"; drop "blocked by: #243" (done, d3650330; reg_ratchet at gates.yml:253); drop the "COUPLED TO #235" sequencing paragraph (#235 completed without a second per-timer accumulator — MetricNode has one `total`). But do NOT retitle to "schema 4 -> 5". The schema step of this task is DONE: 9db54200 added the seven convergence fields to MetricDesc and to the wire and bumped 3 -> 4 (that commit's StarMetricDesc.hpp block is headed "The fields below are the CONVERGENCE" and cites this task's spec), and telemetry-window.py:148-158 already forwards the whole descriptor rather than the hardcoded five. Per the spec §7, "the bump is not the migration" — what remains under #245 is site population of the five unpopulated fields, the unit-aware consumer arithmetic (the hardcoded microsecond assumption), the named-descriptor-factory resolution filed at board.md:5688, and the cost-attribution half.

### `#251` — proposed STILL_OPEN_TEXT_STALE

**Proposed.** The central claim is exactly true and verified literally — nothing in the tree has ever read GL_GPU_DISJOINT_EXT — but two supporting numbers are wrong: the GPU's floor is 800MHz not 933MHz, and the artefact cited as showing the clock MOVING shows a constant 883 on 28 of 28 legs.

**Refutation / correction.** Partially refuted: the verdict and the 933->800 correction survive, but the finding's second half and its proposed board edit are wrong and would write a false weakening into #251.

REPRODUCED AND CONFIRMED: (1) `git grep -in disjoint` at 849f97d2 returns docs/board.md, docs/superpowers/specs/2026-08-05-sovereign-metrics-design.md:198-204, and three unrelated English uses (StarSectorArray2D.hpp:113, StarWorldClient.cpp:2144, entity_map_test.cpp:172,268) — no source/ code hit. (2) `git log --all --oneline -S DISJOINT -- source/` is EMPTY; the all-paths form hits only docs plus vendored lib/*/include/{GL/glew.h,SDL_opengl_glext.h,SDL_opengles2_gl2ext.h}, which merely DEFINE the constant and are untracked at HEAD. So never-written, not renamed or removed. Additionally source/ contains ZERO extension probing at all (GL_EXTENSIONS / glewIsSupported / SDL_GL_ExtensionSupported all 0 hits), which strengthens scope item 2. (3) The ring is real and unguarded at source/application/StarRenderer_opengl.cpp: poll :1197/:1200, glBeginQuery(GL_TIME_ELAPSED) :1215, glEndQuery :1226. Precedents exist: render.gputimer.dropped registered at :1059, BusyReading::unavailable at source/metrics/StarBusyReading.hpp:15. (4) sysfs: /sys/class/drm/card1/gt_RPn_freq_mhz = 800, gt_RP0_freq_mhz = 2350 (gt/gt0 rps_RPn = 800 confirms); the "933MHz floor" in the board is wrong, and 800/883/900 are all observed below it. (5) All 28 harness/profiles/matrix-20260807-160513*.json read gpuClockMhzStart == gpuClockMhzEnd == 883; scripts/render-profile.sh:307-311 defines read_gpu_mhz over gt_cur_freq_mhz, :319 takes GPU_START, :348 writes the sidecar with a second call — one sample each side, exactly as claimed.

WHERE IT BREAKS: the finding asserts the cited artefact "is not evidence that the clock MOVES during the runs we quote from" and proposes replacing the board sentence with "the in-window clock is UNOBSERVED, not observed steady." Sweeping every sidecar in harness/profiles/ contradicts that. Five files have start != end: allcaches-off.json 883->950; matrix-20260807-071454-r2-off-parallaxRefreshInterval.json 900->883; matrix-20260807-071454-r3-off-parallaxRefreshInterval.json 967->883; matrix-20260807-112809-r3-baseline.json 883->950; mutualcheck3.json 933->0. matrix-20260807-071454 IS the 28-leg matrix run of #239 — a run whose numbers we quote — and two of its legs observed the clock moving within the leg. Corpus-wide the sidecars record 800, 883, 900, 933, 950, 967, 1300 and 2350. So the board's CONCLUSION ("the clock MOVES during the runs we quote from") is true and evidenced; only its CITATION (a leg of 160513) is invalid. The finding treats an invalid citation as a refuted conclusion, and its replacement text would assert non-observation that the tree directly disproves — i.e. it would delete live evidence for the hazard rather than re-cite it.

No file was modified; all checks were read-only (git grep, git log -S, sed/cat, sysfs reads).

**Corrected verdict.** STILL_OPEN_TEXT_STALE — verdict itself stands (#251 is genuinely unstarted; GL_GPU_DISJOINT_EXT was never written in source/), but the proposed board edit must be amended. Keep: correct "933-2350MHz" to "800-2350MHz (sysfs gt_RPn/gt_RP0 on card1)" in title and body, and add the note that `git log --all -S DISJOINT -- source/` is empty so absent means never-written. Change the second edit: do NOT write "the in-window clock is UNOBSERVED". Instead re-cite it — "all 28 legs of matrix-20260807-160513 read 883MHz at both ends, but two legs of the matrix run at matrix-20260807-071454 moved WITHIN the leg (r2-off-parallaxRefreshInterval 900->883, r3-off-parallaxRefreshInterval 967->883), and the sidecar corpus spans 800-2350MHz. The clock does move during the runs we quote from. Note also that render-profile.sh samples gt_cur_freq_mhz only once before and once after each leg (scripts/render-profile.sh:307-311,319,348), so even a leg that reads equal at both ends is not evidence of a steady in-window clock."

### `#237` — proposed STILL_OPEN_TEXT_STALE

**Proposed.** Nothing has been built — there is still no headless-rendering client — but the block's residency measurements have rotted (177/2233 in the .cpp is now 184/2254), its own gate now has one line of headroom, and "a fifth entrypoint" does not match the TSSA, which named SEVEN ENTRYPOINTs both when the task was filed and today.

**Refutation / correction.** The STILL_OPEN half reproduces (no headless+GPU client entrypoint exists anywhere in the tree), but the TEXT_STALE half fails on its primary item and its proposed edit would inject a factual error into the board. (1) ORDINAL REFUTED: the board scopes its own ordinal in the quoted sentence -- "A fifth ENTRYPOINT in the TSSA's existing composition scheme (participant + presentation backend + driver)" (docs/board.md:4914) -- and the TSSA states at :1549 "Four compositions touch both arms... with four it is the shape of the staircase", enumerating client_opengl, client_sdl_gpu, client_headless, client_agent at :1545-1548 while putting server/world_sim/world_gen in the row that reaches "nothing -- they do not enter device/". The generated graphs confirm mechanically that server (:2745), world_sim (:2622) and world_gen (:2530) all list participant, presentation and rendering as NOT LINKED. So the proposal is the fifth member of the participant+presentation family, exactly as written; changing it to "eighth" counts three entrypoints that are excluded from the scheme by construction. (2) RESIDENCY: the block is explicitly dated ("PROPOSED 2026-08-06... all measured 2026-08-06") and its figures were exact on that date -- at dd878dbe the .cpp has 177 marker lines and 2233 total, the .hpp 36/296, matching 20027817's own commit message. Today's 184/2254 is drift in a dated historical snapshot, not a wrong live claim, and the headline "~8% resident" still holds (8.2%). (3) HEADROOM: reproduces (184/185) but is already recorded on the board at :4991-4992 with an explicit "defer #237 again" cross-reference. (4) The only sub-item with real merit is "~83 commits behind", which is indeed wrong -- but the tree already corrected it three weeks before #237 was filed, at docs/render/layer1-vs-vanilla-assessment.md:34-38, which also explains that the empty merge-base is an artefact of the player-save SHA purge, not evidence the claim is underivable. The finding's ancestry test also proves a nearby fact: the board claimed only that origin/main has commits touching that file, and git show --stat 4427a49b confirms it touches source/client/StarClientApplication.cpp.

**Corrected verdict.** STILL_OPEN — no board edit to the title or the residency figures. The task is correctly filed and correctly worded: "a fifth entrypoint" is the fifth composition in the TSSA's participant+presentation family (TSSA:1549 "Four compositions touch both arms"), and the 177/2233 + 36/296 figures are an accurate 2026-08-06 measurement inside an explicitly dated PROPOSED block. The single defensible correction is to drop or re-derive "the fork is already ~83 commits behind" (docs/board.md:4953), citing the existing in-tree correction at docs/render/layer1-vs-vanilla-assessment.md:34-38.

### `#171` — proposed STILL_OPEN_TEXT_STALE

**Proposed.** All six producer-side costs are still untimed, but every single line reference in the block is dead, and the "WHY IT IS NOT A SIMPLE FIX" blocker has been half-dismantled — a per-(owner,domain) totals table AND a MetricDesc.whole field now exist, the latter added explicitly crediting #171.

**Refutation / correction.** The verdict label (STILL_OPEN_TEXT_STALE) survives, but the finding's evidence and its proposed board edit do not, on two counts I reproduced from the tree.

(1) FOUR OF THE NEW LINE NUMBERS ARE WRONG, in an edit whose sole purpose is correct line numbers. `void WorldClient::render(` is StarWorldClient.cpp:549, not :550. `lighting.gpu.cpu_cost.us` with `MetricOwner::Frame` is StarGpuLightmapPass.cpp:120, NOT :124 — line 124 is `pointLightsDrawn`'s descriptor, `MetricOwner::Lighting`, i.e. the proposed ref lands on a different metric with a different owner, which is exactly the "ref points at unrelated code" failure the edit exists to fix. `char const* whole` is declared at StarMetricDesc.hpp:146; the cited 136-144 starts at `char const* measures` (136) and stops mid-comment, excluding the declaration. The serialization is StarTelemetry.cpp:518 (`if (n->desc.whole) m["whole"] = ...`), not :517.

(2) "THE SECOND-TOTAL OBJECTION IS SUPERSEDED" IS NOT SUPPORTED. All six of #171's costs are domain Cpu under owner Frame — a single (owner, domain) pair — so c_ownerTotals being keyed by (owner, domain) buys #171 nothing; the emitter at StarTelemetry.cpp:552-554 (`totals[String(domainName(t.domain))] = ...`) still lets a second row for the same pair silently overwrite. The tree states this itself at StarMetricDesc.hpp:141-145: "the (owner, domain) table permits exactly ONE whole per pair and a second row silently overwrites the first, so producer-side lighting CPU ... has no whole it can name." The only mechanism that would supersede the objection, MetricDesc.whole, is inert end to end: telemetry-window.py computes parts per (owner,domain) and reads `total_name = totals.get(dom)` (:657) then `whole = scaled[total_name]...` (:667), never a per-metric `whole`; `grep '"whole"' scripts/` hits only metric-desc-lint.py:82 (PROSE_FIELDS); the only setter in the tree is source/test/telemetry_test.cpp:69. So the board's operative sentence — attributing these to owner=Lighting would sum into `parts` without being in the `whole` and trip "parts exceed the whole" — is still literally true at HEAD.

WHAT I DID CONFIRM, so the correction is narrow, not a reversal: all six sites are genuinely still untimed (first TelemetryScope in StarWorldClient.cpp is :2279 inside lightingCalc, which spans 2211-2718; no Telemetry/LogMap call anywhere in render()'s body 549-700; the maxEmission scan at StarGpuLightmapPass.cpp:56-60 is called at :114, before cpuCostScope at :125), and every board line ref is indeed dead (WorldPainter.cpp:143-150 is renderInit, :233 is the ablation-mask comment, StarWorldClient.cpp:2211 is now the lightingCalc header). Correct current sites: 1/2/3 -> StarWorldClient.cpp:579-584, :596, :594 (render begins :549); 4 -> StarGpuLightmapPass.cpp:56-60 called :114, outside cpuCostScope :125, timer declared MetricOwner::Frame at :120; 5 -> StarWorldPainter.cpp:381-382 and StarTilePainter.cpp:38; 6 -> StarWorldClient.cpp:2729 in lightingMain.

**Corrected verdict.** STILL_OPEN_TEXT_STALE (label stands, edit must be corrected before landing). Renumber to: 1/2/3 -> StarWorldClient.cpp:579-584, :596, :594, render() begins :549; 4 -> StarGpuLightmapPass.cpp:56-60 called at :114, outside cpuCostScope at :125, billed to lighting.gpu.cpu_cost.us declared MetricOwner::Frame at :120 (NOT :124); 5 -> StarWorldPainter.cpp:381-382 and StarTilePainter.cpp:38; 6 -> StarWorldClient.cpp:2729 in lightingMain. In "WHY IT IS NOT A SIMPLE FIX", do NOT write "superseded". Write: the owner table is now keyed by (owner, domain) (c_ownerTotals, StarTelemetry.cpp:487-492, emitted :548-561) and telemetry-window.py:579/657 reads totals as a per-domain dict, so the board's old mechanics ("StarTelemetry.cpp:445-450 keys by owner name", "would TypeError on a list") are stale — but the OBJECTION ITSELF IS UNCHANGED: all six costs are (Frame, Cpu), one pair, which still admits exactly one whole and where a second row still silently overwrites, as StarMetricDesc.hpp:141-145 states verbatim. A wire field for the fix now exists (MetricDesc.whole, StarMetricDesc.hpp:146, serialized StarTelemetry.cpp:518, added crediting #171) and is INERT: telemetry-window.py never reads a per-metric whole (closure uses totals.get(dom) at :657 / :667), and the only setter in the tree is source/test/telemetry_test.cpp:69. Task stays pending.

### `#200` — proposed ALREADY_DONE

**Proposed.** All three deliverables of ARCH-1 exist at HEAD and the gate is green, so the task is done — but four of the block's supporting counts have rotted, three of them corrected by #203's own commits.

**Refutation / correction.** I reproduced the load-bearing evidence and it holds, but the finding's own claim ("four ... counts have rotted") is undercounted and its proposed block text is factually wrong, so the edit as written must not be applied.

WHAT I CONFIRMED FIRST-HAND (HEAD 849f97d2, branch integration):
- /root/frackin/OpenStarbound/scripts/arch-graph.py = 1485 lines; /root/frackin/OpenStarbound/docs/architecture/system-boundaries.md = 1102 lines. Both present.
- arch_graph_fresh: ADD_TEST at source/test/CMakeLists.txt:303, NoAssets list at :496, CI step at .github/workflows/gates.yml:74. Exact.
- `bash scripts/ci/run-gates.sh` ran to completion: `arch_graph_fresh OK`; final line "55 green, 1 SKIPPED and therefore UNVERIFIED: metrics_mutual". Exact.
- 11 ```mermaid fences; 13 `BEGIN GENERATED: scripts/arch-graph.py#...` blocks with exactly the 13 names cited.
- system-boundaries.md:387 "13 declared executables fall into **6 distinct link sets** ... **not strictly nested**". system-boundaries.md:632 (finding said 631) "`game` is **48% of the engine in one directory**", mass row `game` 500 / 115,658 / 48.1%. Retraction at :1065 "### The rule underneath, corrected". 5 unused grants and zero Root reads in core/base/application both still true.
- Baseline at 7d22d2bb really did say "12 declared executables ... 4 distinct link sets ... strictly nested", "44% of the engine", 10 mermaid / 9 generated. So the rot is real.
- Attribution spot-checks: 44%→48% (and game 338→500 files) came from 5e893499, a #203 commit, via the os.walk recursion fix; the retraction came from the same commit. The 12→13 executables / 4→6 / not-nested change did NOT come from #203 — it came from 8d5734f6 ("GM-1 Task 7: the metrics CLI"), which added the `metrics` binary.

WHY IT IS REFUTED — two independent defects, both in the part that would be written to the board:

(1) A FIFTH stale count the finding missed, and its edit would leave standing. The block asserts "Exactly 1 of 12 render inheritance edges leaves the subsystem: TilePainter : TileDrawer (#191)". system-boundaries.md:1012 now reads "Of 13 inheritance edges, 10 stay inside the render libraries, 2 take a base from a foundation library ... and **1 leaves the subsystem entirely**". The count went 12→13 in commit 52360eee — which is #202's commit, not #203's. The proposed edit sweeps four counts and announces the block is current, while leaving a demonstrably false number in it. That is exactly the "board edit overstates what shipped" failure.

(2) The proposed replacement string "eleven diagrams in thirteen generated blocks" is wrong on both halves. Of the 11 mermaid fences, only 10 sit inside generated blocks; the one at system-boundaries.md:79 (the four-tests decision flowchart) is still HAND-AUTHORED and outside every BEGIN/END GENERATED pair. And 3 of the 13 generated blocks (#tree, #shape, #presentation) contain no diagram at all — they are a code-fenced directory listing and two tables. The accurate statement is "eleven diagrams, TEN of them GENERATED, across thirteen generated blocks". The proposed wording also silently contradicts the block's own next paragraph, "METHOD DOCUMENTED (the hand-authored 10th diagram)", which is still true in substance (it is now the hand-authored one of eleven) and which the edit does not touch.

The status change itself is not what I am disputing: all three named deliverables exist, the gate is registered in both ctest and CI, and it runs green — #200 is genuinely finished, and #203/ARCH-2 is the separately-tracked follow-up. But the finding as submitted would write a block containing a false inheritance-edge count and a false diagram/generated-block characterisation, so it must be corrected before application.

**Corrected verdict.** ALREADY_DONE (status → done is correct), but the block edit must be revised: use "eleven diagrams, TEN of them GENERATED, across thirteen generated blocks" (the four-tests flowchart at system-boundaries.md:79 is still hand-authored; #tree/#shape/#presentation are generated tables, not diagrams), and additionally replace "Exactly 1 of 12 render inheritance edges" with "1 of 13" (system-boundaries.md:1012, changed by #202's 52360eee) — a fifth rotted count the finding did not list. Also note the executables correction (12→13, 4→6, not strictly nested) came from 8d5734f6 "GM-1 Task 7: the metrics CLI", not from #203.

### `#219` — proposed ALREADY_DONE

**Proposed.** All 35 B-rows carry recorded decisions, the LightTraits::multiply deletion is in the tree, D29 is declined and B08/B24/B25 folded into it — and the ledger's open count has since gone from the block's 15 to zero.

**Refutation / correction.** The substance checks out but the proposed board edit contains a demonstrably false provenance claim, and one evidence citation points at the wrong file.

VERIFIED TRUE: osb-pr570-decisions.json holds exactly B01..B35 (34 declined, B21 done commit 0a8fff9d, an ancestor of HEAD). `grep -rn "Traits::multiply" source/` returns nothing and StarCellularLightArray.hpp contains no `multiply` at all; repo-wide `LightTraits::multiply` is likewise empty. `spread` is live (defs at StarCellularLightArray.hpp:172 and :192; call sites hpp:442,443,445,446,465,466,468,469 plus the oracle mirror StarCellularLightingOracle.cpp:363). D29 is declined carrying the 2026-08-05 Director decision; B08 declined citing D29; B24 "Same single change as B08/D29, third enumeration"; B25 declined on the 32-tile window bound. `python3 scripts/pr570-ledger.py --check` prints "OK -- 155 rows, 0 open" with the tree clean, and the pr570_ledger gate really runs (/root/analysis/osb-pr570/raw exists, so it does not take its skip branch).

REFUTATION 1 - the board edit misattributes the closure. Reading the generated status table out of git at each commit: e3682234 open=15; 9ccb57a8 open is STILL 15 (it only moved 10 A-rows closed->declined: closed 88->84, declined 36->40); open first reaches 0 at 4e4ced84 ("ledger: decide the last 15 rows"), which is a task #220 commit and is already claimed in #220's own board block; c363e052 lands 2026-08-05, ~12 hours after open was already 0, and only flips D29 to declined. So "the 15 rows still open at e3682234 have since closed ... via 9ccb57a8 and c363e052" is false on both named commits and credits #219 with #220's work. Further, the 15 rows open at e3682234 were D/E rows - every B row was already settled by that same commit - so the whole-ledger open count is a NEARBY fact, not evidence about #219's B cluster.

REFUTATION 2 - wrong file cited. The finding places the spread call sites at StarCellularLightArray.cpp:442,443,445,446,465,466, but that file is only 103 lines; they are in the .hpp, and there are 8, not 6. (The block's own "12 call sites in calculateLightSpread" is also an overcount: 8 in-tree + 1 oracle mirror.)

REFUTATION 3 - minor: "pr570_ledger OK in run-gates.sh" is not accurate; the step is defined in .github/workflows/gates.yml:314 and run-gates.sh only discovers it from that YAML.

Also note the board's status vocabulary is "completed"/"pending" (156/34 occurrences); "done" is not a value it uses.

**Corrected verdict.** ALREADY_DONE on substance (status -> completed, the board's actual vocabulary), but ONLY with the appended text corrected. The false clause "via 9ccb57a8 and c363e052" must be dropped. #219's own closure evidence is: all 35 B-rows carry decisions in osb-pr570-decisions.json (34 declined, B21 done at 0a8fff9d), LightTraits::multiply is absent repo-wide while spread survives with 8 call sites in StarCellularLightArray.hpp plus the oracle mirror, and the D29 fold that B08/B24/B25 waited on was answered by the Director's 2026-08-05 decline recorded at c363e052. If the whole-ledger count is mentioned at all it must read: the ledger reports open=0 at HEAD (deferred 1, declined 45, done 25, closed 84), but the 15 rows still open at e3682234 were D/E rows closed at 4e4ced84 under #220, not #219 work.

### `#203` — proposed ALREADY_DONE

**Proposed.** Every deliverable is in the tree, but the block's ITEM 2 is now WRONG and was overturned by cfa7603b — a commit in this task's own citation list: game's declaration graph is acyclic, so the work is not breaking a 226-node cycle.

**Refutation / correction.** All the artefact evidence reproduces (shape/cohesion/scc/VENDORED_SUBTREES/recursive files_in in scripts/arch-graph.py at 569/676/633/179/252; "NO MEMBER COUNT HERE" at scripts/boundary-inventory.py:58-62; shape block 507-522 and cohesion block 663-694 in docs/architecture/system-boundaries.md with every cited figure intact; all 8 commits ancestors of HEAD), so the work did ship. But the proposed board edit overstates the tree on two verified counts. (1) The finding says the doc now says "the opposite" of ITEM 2's "the first deliverable is a feasibility study"; system-boundaries.md:1052-1055 says the reverse -- "the first deliverable is STILL a feasibility study, but now it is a study of which stratum to start with". The proposed replacement drops that clause plus item 4's two explicit caveats (Root removal only takes 86% to 66%; #include cannot see template instantiation or Root-database runtime coupling) and its stated "months" cost, turning a gated, months-long job into a clean incremental one. (2) cfa7603b did NOT fully overturn the old position in the tree: git blame -L 1064,1077 shows section 14's closing "The rule underneath, corrected" was written by 5e893499 and is untouched by cfa7603b (its diff contains no "rule underneath"/"at any price" hits), so line 1069-1070 still reads "`game` and `windowing` cannot be partitioned today at any price short of breaking their cycles", contradicting item 4's "nor breaking a 226-node type cycle (there isn't one)" seventeen lines above. Closing #203 with the proposed text freezes a self-contradiction created by this task's own two commits. Also minor: rewriting the historical 115,233 to 115,658 overwrites the recursive-scan defect's own before/after record (115,233 was correct when measured; the doc drifted later as source/game grew), and the cited line anchors (:508-518, :667-686, boundary-inventory.py:59) are off the real block bounds (507-522, 663-694, 58-62).

**Corrected verdict.** PARTIALLY_DONE — every deliverable is in the tree and the "pending" status is stale, but do not apply the proposed ITEM 2 rewrite as drafted. If ITEM 2 is restated, it must keep what the document actually says: the 226/264 cycle is translation-unit only and game's header graph is acyclic (§9, §14 item 4), AND the first deliverable is still a feasibility study — now of which stratum to start with — at a cost of months, because removing Root only moves the entanglement 86% -> 66% and #include cannot see template instantiation or Root-database runtime coupling. Leave 115,233 as the historical measurement (note "115,658 at HEAD" rather than replacing it). Before closing, file the residual defect this task left: system-boundaries.md:1069-1070 ("cannot be partitioned today at any price short of breaking their cycles", written by 5e893499, never updated by cfa7603b) still states the position §14 item 4 retracts.

### `#144` — proposed PARTIALLY_DONE

**Proposed.** Four of the block's named findings have since shipped and its central SYNTHESIS blocker has been dissolved by the FBO extraction work, while three file/field/line references it points a reader at no longer exist under those names.

**Refutation / correction.** Substance largely reproduces, but the exact claim does not. (1) The headline "Four of the block's named findings have since shipped" is an overcount: only THREE are shipped (viewport, shader fallback, process-trap script). The only fourth candidate — the setEffectTextureHalfRGB/setEffectTextureR8 unification — is NOT shipped: the finding's own evidence concedes the copy-paste is open, and the block asked for unification "at uploadTextureImage" while the tree unified at uploadLoneStorage, leaving the ~12-line preamble duplicated verbatim at StarRenderer_opengl.cpp:676-687 and :711-722. (2) The proposed board edit anchors "viewport finding CLOSED" at StarRenderer_opengl.cpp:504-511, which is the SCREEN branch (m_pass.unbind). The defect the block actually named — "an effect declaring frameBuffer + sizeDiv renders into a smaller FBO with a stale full-screen viewport" — is closed in GlPass::bindTarget at source/application/StarGlRenderSurface.cpp:708-716 (glViewport(0,0,vp) with vp = newTarget->size()), whose own comment states exactly that. The citation proves a nearby fact, not the stated one. (3) "854b4b7f is unreachable from integration" is misleading: git cat-file -t shows it is not an object at all, and neither are 4fcf71033 (#155), c1d4d0533 (#154), a1ec80598 (#157), nor 691392929 — the hash the finding's OWN board edit proposes to cite for the shader fix. Every pre-6db94ae8 (2026-07-19 reorg) board hash is dead, so this is a board-wide artefact, not a #144 staleness, and the edit would add one dead hash while flagging another. (4) Two named block items are never assessed: both UPSTREAM CONTRIBUTION items (the multisampled-opt-in PR; #157 records the draft as filed but NOT published). (5) docs/superpowers/drafts/matrix-prereq-ledger.md:465-471 — the doc #144 is cited in — still carries row O07 as PENDING against #144 and extends the process-trap ask beyond "a script exists": assert the harness asset tree matches the deployed one AT LEG START, and fix that StarClientApplication.cpp:547 calls setVboOrphan(config...) every frame, silently clobbering STAR_NO_VBO_ORPHAN. Confirmed independently: shader fallback CLOSED (StarGlRenderSurface.cpp:449-458, compileStage sig at :420, DefaultVertexShader raw GLSL at :8); deploy-install.sh --verify present (:22-24, :41, :61, :93-95, :143); altTexture gone and resize symmetric (:252-260, makeDoubled at :291); synthesis half-built (swap-aware share at StarRenderer_opengl.cpp:530 vs hardcoded writeFace at :646); targets[2] at source/rendering/StarGpuLightmapPass.cpp:143; setEffectTextureHalf at :671 with shared uploadLoneStorage TexSubImage fast path at :1837; NoVboOrphan global :26-29 / setVboOrphan :31-33 / lever-table.json:70-75 / witness comment :1626-1628; `undefined ||` guard survives at :526 (not :524).

**Corrected verdict.** PARTIALLY_DONE — keep #144 pending, but with THREE shipped items, not four: viewport CLOSED (cite GlPass::bindTarget, source/application/StarGlRenderSurface.cpp:708-716, not StarRenderer_opengl.cpp:504-511), shader-fallback dead code CLOSED (StarGlRenderSurface.cpp:449-458), process-trap script BUILT (scripts/deploy-install.sh --verify) — noting matrix-prereq-ledger.md:465-471 keeps row O07 open for the leg-start assertion and the STAR_NO_VBO_ORPHAN clobber at StarClientApplication.cpp:547. The setEffectTextureHalfRGB/R8 item is PARTIAL, not shipped: renamed to setEffectTextureHalf and the #127 TexSubImage fast path now reaches both callers via uploadLoneStorage (:698, :732, :1837), but the unification landed at uploadLoneStorage rather than uploadTextureImage and the per-setter preamble is still duplicated (:676-687, :711-722). Rewrite the SYNTHESIS blocker as proposed (altTexture gone; GlSurface::resize reallocates back at :252-260; residual = setEffectTextureFromTarget hardcodes writeFace() at :646 while switchEffectConfig is swap-aware at :530). Fix StarGpuLightmapPass.cpp:34 -> source/rendering/StarGpuLightmapPass.cpp:143. Re-frame NoVboOrphan as the declared renderVboOrphan witness lever (scripts/lever-table.json:70-75) needing a Director call. Do NOT add a "854b4b7f is unreachable" note singling out #144 — every pre-2026-07-19-reorg hash on the board is dead, including the 691392929 the edit itself cites. Both UPSTREAM CONTRIBUTION items remain unassessed and open.

### `#75` — proposed PARTIALLY_DONE

**Proposed.** Half of item (1) has been answered by machinery filed after this task — the proto-cache toggle is now one of the eight matrix levers and its OFF leg demonstrably reaches the engine — but item (2)'s magnitude is still unmeasured, and the block's two named instruments have both drifted: the quoted log string is not the string the code emits, and the ProtoCacheToggle tests live in a binary that CI never runs.

**Refutation / correction.** Every factual sub-claim reproduces exactly from the tree — lever-table.json:86-93 (key/baseline true/off false/witness script.proto.cache.loaded); the r1 witness flip loaded 2986->0 and bypassed 0->2746; tick.server.total.us means 1020.9/1207.1 (+18.2%), 1113.4/934.6 (-16.1%), 1047.2/1076.7 (+2.8%); render-profile.sh:25,204 writing the key to harness/storage-perf/starbound.config BEFORE launch with assert_baseline at lever-matrix.sh:185/:552; StarLuaRoot.cpp:24 emitting "Lua Proto cache (scriptProtoCacheEnabled): {}" as the ONLY Proto-cache log site in source/; ProtoCacheToggle at status_context_test.cpp:246/:270; CMakeLists.txt:524 and :65 verbatim with the base preset filtering LABELS NoAssets (:478, :599); f136c6bf not an ancestor of HEAD (reachable only from refs/tags/archive/l2-vao-bake); StarRootLoader.cpp:75 default true. The interpretation is what fails. I read `git show f136c6bf`: the commit touches ONLY the reload-listener lambda in LuaRoot::LuaRoot(). The boot-time read at StarLuaRoot.cpp:58 inside restart() — m_scriptCache->setProtoCacheEnabled(configuration()->get("scriptProtoCacheEnabled")) — PREDATES the fix and was not modified by it. The matrix leg writes the key to the config file and launches a fresh process, so it flows through restart(), i.e. code the fix never touched. The witness flip therefore proves nothing about the same-session setConfiguration + /serverreload path that item (1) exists to verify; it re-proves that the toggle has an effect at boot, already established by core_tests T1/T4 and the ProtoLoad bench. That is the "evidence proves a NEARBY fact rather than the stated one" failure. Consequently NO fraction of item (1) has been answered, item (2) is admittedly unmeasured, and nothing in the task completed — PARTIALLY_DONE mislabels a fully open task. The proposed edit compounds this: "what remains unverified is ONLY the SAME-SESSION path" reads as scope reduction when the same-session path is 100% of item (1); the citing ledger row (docs/superpowers/drafts/matrix-prereq-ledger.md:455-461) states item (1) was "Never done" and closes only by doing the /serverreload flip, which the matrix run did not do. The finding's three corrections are individually valid and worth keeping as annotations on an OPEN task: the quoted log string should be "Lua Proto cache (scriptProtoCacheEnabled): ENABLED|disabled" (the board quoted the commit MESSAGE, which used the shorter form); ProtoCacheToggle.* is in game_tests, which CI never runs; and "commit f136c6b" should be replaced by content sites since the SHA is unreachable from HEAD. But they do not make any part of the task done.

**Corrected verdict.** NOT_DONE — task #75 remains fully open on both items. Keep the annotations, drop the "half of item (1) answered" framing. (1) is UNVERIFIED in full: f136c6bf changed only the reload-listener lambda, so the matrix's config-at-boot leg runs restart() (StarLuaRoot.cpp:58), code that predates and is untouched by the fix — the same-session setConfiguration + /serverreload path has never been exercised (corroborated by matrix-prereq-ledger.md:455-461 row O06, "Never done"). Correct the quoted log string to "Lua Proto cache (scriptProtoCacheEnabled): ENABLED|disabled" (StarLuaRoot.cpp:24, the only such log site) and note the board quoted the commit message, not the code. Flag that the "already proven by ProtoCacheToggle.* regression tests" clause holds only on a manual run: those tests are at source/test/status_context_test.cpp:246/:270 in game_tests, which source/test/CMakeLists.txt:524 declares cannot run in CI. (2) STILL UNMEASURED, with a useful negative datum: the matrix ran a boot-time OFF/ON A/B three times at a non-dense scene and tick.server.total.us moved +18.2% / -16.1% / +2.8% (harness/matrix/matrix-20260807-160513) — sign-flipping scene noise, the same pathology #84 records; the dense-churn, E-core-pinned record+symbolize A/B item (2) specifies has not been run. Replace "commit f136c6b" with the content sites (StarLuaRoot.cpp:12-24 listener, :205-215 dirty-flush re-read, StarRootLoader.cpp:75 default true) — the SHA is unreachable from HEAD.

### `#104` — proposed STILL_OPEN_TEXT_STALE

**Proposed.** The decision itself stands and the gate has not opened, but three of the block's pointers have moved: its trailing action item is already done, its only data pointer is dead, and both instruments its reconsider-gate named as missing now exist in the perf harness.

**Refutation / correction.** Two of the three pointer claims reproduce, but the load-bearing GATE STATUS claim does not. CONFIRMED: (a) /root/kubebound/2026-06-26-fu-lua-offload-decision.md exists with DEFER, panel w46s8gaeh, the six corrected-framing points and the gate section, and docs/board.md:790 never cites it — so "Persist to kubebound FU-Lua decision note" is done and uncited; (b) /tmp/fulua/ does not exist and `find /root/kubebound -iname "*fulua*" -o -iname "*fu-lua*"` returns only that note; (c) FU is in the perf boot config (harness/sbinit-perf.config:38 = .../211820/729480149/contents.pak, 42 sources) which scripts/lever-matrix.sh:61 boots; (d) tick.server.total.us exists as owner sim's declared Total and the number reproduces exactly — r1-baseline count 4506, total 4600017 us, mean 1020.86, window 72.775 s => 61.92 ticks/s => 6.32%. REFUTED: the claim that the harness "runs UNCAPPED", which is one of the gate's two named preconditions. vsync:false is a config field, not a measured cap: in that same leg cpu.frame.total.us count 4499 over 72.775 s = 61.8 fps and cpu.frame.idle.us mean = 13,353 us of a 16,176 us frame; all 27 legs read 61.8-61.9 fps with 78-87% idle. The cause is in-tree — source/application/StarMainApplication_sdl.cpp:891-899 sleeps Thread::sleepPrecise(m_updateTicker.spareTime()) against TickRateApproacher(60.0f) (line 1441) — and the repo's own consumer states it at scripts/telemetry-window.py:696-698 ("The main loop is PACED: Thread::sleepPrecise(m_updateTicker.spareTime()) runs even with vsync off, so cpu.frame.total.us measures the frame PACE, not the frame COST"); board #173 records the same ("a loop that sleeps 72-84% of every frame"). So the harness runs in precisely the frame-pacing-limited, 7-8x-headroom regime the decision note's point (1) calls circular; the gate's purpose clause ("so vsync can't mask the sim") is NOT satisfied. Two secondary errors: tick.server.total.us is registered at source/game/StarWorldServerThread.cpp:255, not StarWorldServer.cpp:652-667 (those lines register the sixteen tick.server.compute.* PARTS), and its own registration comment says it is "BUSY WITH RESPECT TO THE PACING SLEEP, NOT WITH RESPECT TO BLOCKING ... must be read as busy+blocked", so 6.3% is an upper bound on occupancy rather than occupancy (true per-thread busy, cpu.owner.sim.busy_ns from source/metrics/StarThreadBusyReader.cpp, is absent from every leg file). The proposed board edit's closing sentence — "What is still missing is only the target-scale factory scene and a verified item-throughput number" — therefore overstates: a third precondition is unmet, and a future reader following that text would capture a factory scene under the same masking confound and believe the gate had been fairly tested.

**Corrected verdict.** STILL_OPEN_POINTERS_STALE (pointer half only). Apply just the pointer replacement: "Decision note PERSISTED at /root/kubebound/2026-06-26-fu-lua-offload-decision.md (the surviving record — /tmp/fulua/ is gone)." Drop the proposed GATE STATUS paragraph, or replace it with the accurate, narrower version: "GATE STATUS 2026-08-08: ONE of the gate's instruments now exists. The perf harness loads FU (729480149 in harness/sbinit-perf.config, booted by scripts/lever-matrix.sh:61) and sim-tick cost is readable as tick.server.total.us (owner sim's declared Total, registered StarWorldServerThread.cpp:255) — at Ark Ruins it reads 1020.9 us over a 16.15 ms tick = ~6% of one core, though that timer is busy+blocked wall time, an UPPER bound on occupancy, not busy. The UNCAPPED precondition is NOT met: vsync:false is a config flag only, and all 27 legs of matrix-20260807-160513 ran at 61.8 fps with 78-87% of every frame in Thread::sleepPrecise (StarMainApplication_sdl.cpp:891-899, TickRateApproacher(60.0f)) — the same frame-pacing-limited regime point (1) calls circular. Still missing: an actually-uncapped run, the target-scale factory scene, and an item-throughput metric. The DEFER holds."

---

## What the audit's own shape could not see

A completeness critic was asked what the cluster split made invisible. It found 10 things.

### `#208` — BLOCKER_NEW

*confidence: high*

**Claim.** A registered architecture ratchet is RED at HEAD and no task, no board entry and no green-tally covers it: `dedup_measure` exits 1 with "leak 614 exceeds ceiling 596" — the TSSA's one north-star deduplication instrument has been breached by 18 symbols, and it is deliberately excluded from `scripts/ci/run-gates.sh`, the instrument the project treats as "green IS the gate".

**Evidence.** `cd /root/frackin/OpenStarbound && python3 scripts/dedup-measure.py --check > f 2>&1; echo $?` → **RC=1**, tail: "dedup-measure: LEAK = 614 symbols outside {base, core, host, platform, presentation, scene} / dedup-measure: FAIL -- leak 614 exceeds ceiling 596". The measurement is NOT stale: the script's own guard (scripts/dedup-measure.py:194-199) did not fire, and only one build tree holds objects (`build/linux-release-clang`, 584 .o, mtime Aug 8 11:53, HEAD is Aug 8 12:00; `build/linux-release` holds 0 .o). Ceiling declared at scripts/dedup-measure.py:79 `CEILING = 596`, with the comment at :73 "This ceiling may only go down" and at :78 "MEASURED 2026-08-01"; introduced at 9882a77a. Registered as a ctest at source/test/CMakeLists.txt:343 and in `star_noassets_tests` at :498. Absent from .github/workflows/gates.yml (grep `dedup` → no match) and therefore absent from run-gates.sh, which reads gates.yml — my full run printed "55 green, 1 SKIPPED" with dedup_measure not among the 56 names. The exclusion is deliberate and documented at source/test/CMakeLists.txt:340-341 ("Not in the Gates workflow: that job runs scripts without building") — so the omission is honest and the RED is still invisible to the only gate command anyone runs. Per-component breakdown of the 614: game 236, rendering 98, frontend 79, gpu_opengl 72, participant 51, host_sdl 41, windowing 19, platform_pc 18.

**Edit.** File a new task: "DEDUP-RATCHET BREACH: dedup_measure is RED at 614 vs ceiling 596". Body must record that run-gates.sh does not cover it by design, so the standing 'green IS the gate' rule has a hole a build-requiring ratchet falls through, and that either the leak is paid down or the breach is explained and the ceiling re-recorded with a reason. Cross-ref #208 (which already lists dedup_measure under GATE RE-HOMING) and #204.

### `#219` — BLOCKER_NEW

*confidence: high*

**Claim.** The `pr570_ledger` gate step has the exact fall-through-to-exit-0 skip path that commit dc722dee identified and fixed for `metrics_mutual` — and it was already in the file when that fix landed, so the sweep missed it. On any machine or CI runner without `/root/analysis/osb-pr570/raw` the step prints a SKIPPED notice, exits 0, and is counted toward run-gates.sh's green tally having verified nothing.

**Evidence.** .github/workflows/gates.yml:314-320 — `if [ -d /root/analysis/osb-pr570/raw ]; then python3 scripts/pr570-ledger.py --check; else echo "pr570-ledger: SKIPPED -- raw outputs not present on this machine"; fi` — no `exit 77`. Contrast the metrics_mutual step at gates.yml:402-411, whose else-branch ends `exit 77` with the comment "77, not a fall-through. This else-branch used to end here and exit 0, so run-gates.sh printed OK and counted a comparison that never happened toward the green tally." dc722dee's own message: "THE HOLE WAS IN THREE PLACES... the gates.yml step's `else` branch echoed a NOT RUN notice and fell off the end, exiting 0 -- the guard was itself a hole." `grep -n "else" .github/workflows/gates.yml` returns exactly two branches: 318 (falls through) and 406 (exits 77). Ancestry: `git merge-base --is-ancestor 82170188 dc722dee` → true; 82170188 (2026-08-04, the commit that added the pr570_ledger step) predates dc722dee (2026-08-06). CI reach: gates.yml runs on ubuntu-latest via actions/checkout, where /root/analysis cannot exist, so this step has read permanently green in CI since it landed. It ran for real on THIS host only because /root/analysis/osb-pr570/raw happens to exist here ("pr570-ledger: OK -- 155 rows, 0 open").

**Edit.** File a new task: "GATE-SKIP-2: pr570_ledger's guard is the same hole GATE-SKIP-1 closed — exit 77, not fall-through". One-line fix (`exit 77` in the else branch), plus a standing check that every guarded step in gates.yml exits 77 rather than 0 on its did-not-run path.

### `#204` — BLOCKER_NEW

*confidence: high*

**Claim.** The TSSA's own live thread — dissolving `platform`, a Director-ratified standing selection with a five-stage plan of which stages 3, 4 and 5 are not started — exists on NO board task. The word `ugc` appears zero times in the entire 454KB board.

**Evidence.** `grep -ci "ugc" docs/board.md` → 0. `grep -ni "dissolv" docs/board.md` → 4 hits, all retrospective log lines inside #204/#207 recording stages 1 and 2 as done (board.md:3559, :3718). #204's block (board.md:3671) lists its sub-tasks exhaustively: "Sub-tasks: #205 spine, #207 levelling pass, #208 aggregate review" — no fourth. #208's block (board.md ~3790-3810) scopes register fields / completeness stopping rule / aggregate review / gate re-homing — not the dissolution. HANDOVER.md §3 gives the five-stage table (stages 3 `ugc`, 4 `desktop`, 5 rename `platform_pc`→`vendor` all "not started") and §7 says "Then either continue at stage 3 (`ugc`) using §3.2 as the checklist, or pick up #208" — one of those two options has a task id and the other does not. Tree confirms not-started: no `ugc` or `desktop` INTERFACE rows in the register (grep of the component table returns only `platform`, `platform_pc`, `platform_null`, `statistics*`), and source/platform/ still holds all four contract headers including StarUserGeneratedContentService.hpp and StarDesktopService.hpp. #209's block even anticipates the work — "Stages 2-5 of the `platform` decomposition no longer require hand-editing any closure figure" — while no task owns stages 3-5.

**Edit.** File one task (or three) for the platform dissolution: "TSSA-STAGE-3 (ugc)", "TSSA-STAGE-4 (desktop) + platform_pc→vendor rename", "TSSA-STAGE-5 (delete platform / platform_null)", each pointing at HANDOVER §3.2's eight-item checklist as the template. Rank them relative to #208. Without this the only pointer to a ratified, half-finished architecture thread is a point-in-time note 167 commits stale.

### `#204` — STILL_OPEN_TEXT_STALE

*confidence: high*

**Claim.** docs/superpowers/HANDOVER.md — #204's resume document and the only place the platform-dissolution plan is written down — is 167 commits stale and every headline figure in its §2 state table has rotted, including one that names a component list a reader would act on.

**Evidence.** `git rev-list --count 21ba119a..HEAD` → 167; the doc's own staleness command `git log --oneline 21ba119a..HEAD -- docs/superpowers/specs/ scripts/` prints 50+ commits. §2 says "gates | 21 of 21 green"; actual is 56 declared steps, `scripts/ci/run-gates.sh` → "55 green, 1 SKIPPED and therefore UNVERIFIED: metrics_mutual" (exit 0). §2 says components 52 / grant rows 50 / elements 24; `python3 scripts/spec-model.py` → "53 components, 51 grant rows, 24 elements". §2 says "open `owes` | 14 of 52"; the generated ledger at TSSA line 6440 reads "**15 of 53 components record something unresolved**". §6 then names the 14 by name and the fifteenth — `metrics` — is missing from that list, which matters because `metrics`'s owed item ("The split... owed under a stated validity condition") is the live observability thread, not background. §7's resume instruction "expect 21/21 and exit 0 before touching anything" is now unsatisfiable as written.

**Edit.** Under #204, add: "HANDOVER.md is point-in-time at 21ba119a and is 167 commits behind HEAD; §2's figures (21/21 gates, 52 components, 50 grants, 14 of 52 owes) all read wrong at HEAD (56 gates/55 green+1 skip, 53, 51, 15 of 53) and §6's named list omits `metrics`. Refresh it or stamp it superseded before it is quoted again."

### `#245` — PARTIALLY_DONE

*confidence: high*

**Claim.** #245's closing load-bearing claim — "unit, clock, source, boundedness, measures, validWhen, whole exist in MetricDesc and ZERO production sites populate any of them (verified 2026-08-07)" — is refuted at HEAD for two of the seven fields: `unit` and `clock` are populated at 57 production sites across nine files, shipped by #235. The audit filed no finding on #245 at all.

**Evidence.** `grep -rn "MetricUnit::" source/ --include=*.cpp --include=*.hpp | grep -v StarMetricDesc.hpp | grep -v source/test/ | wc -l` → 57; same count for `MetricClock::`. Files: source/application/StarMainApplication_sdl.cpp, StarRenderer_opengl.cpp, source/client/StarClientApplication.cpp, source/core/StarTelemetryReporter.cpp, StarTelemetry.cpp, source/rendering/StarGpuLightmapPass.cpp, StarWorldPainter.cpp, StarBackdropPass.cpp, StarWorldPass.cpp. Example: source/application/StarRenderer_opengl.cpp:1074 `MetricCadence::Frame, MetricRole::Detail, MetricUnit::Microseconds, MetricClock::GpuTimeline`. Origin: `git log -S "MetricUnit::Microseconds" -- source/application/StarRenderer_opengl.cpp` → e368f046 "GPU-CLOCK: a timeline span is not work... [#235]", plus eec79ca1 CLOCK-RATCHET. The other five are genuinely still zero (`grep -rn "\.boundedness *=\|\.measures *=\|\.validWhen *=\|\.whole *="` outside StarMetricDesc.hpp/test → 0 hits; `.source =` hits are unrelated — StarAssets.cpp:338, two liquid-tile assignments). A ratchet already governs one of them: `clock_ratchet` (gates.yml:275, `metric-desc-lint.py --check-clocks`) pins the count of sites declaring no clock. Also affected: #252's derived figure "MetricDesc now has ELEVEN fields, and #245 populates seven more across 166 sites" — it is five more, and `metric_desc_lint` counts 167 sites, not 166.

**Edit.** Under #245: replace "ZERO production sites populate any of them" with "`unit` and `clock` are populated at 57 production sites across nine files (shipped by #235, e368f046 + eec79ca1) and `clock_ratchet` already pins the undeclared count; the remaining five — source, boundedness, measures, validWhen, whole — are still at zero production sites." Also correct #252's "seven more" to "five more". Note the blocker #243 is closed, which the Next table already reflects.

### `#252` — STILL_OPEN_TEXT_STALE

*confidence: high*

**Claim.** The adversarial pass corrected the rotted "1,932 lines of Python" inventory in #250's block but not the byte-identical copy of the same figure in #252's block, where it is the number that defines what is explicitly OUT of scope.

**Evidence.** board.md #252 block: "The 1,932 lines of Python in scripts/ -- telemetry-window (661), gputimer-brackets (401), pmu-join (372), metric-desc-lint (313), pmu-engine-sample (185) -- are consumers and fold into the sovereign modules... NOT this task." `wc -l` at HEAD: telemetry-window.py 813, gputimer-brackets.py 401, pmu-join.py 372, metric-desc-lint.py 489, pmu-engine-sample.py 185 → **2,260**, not 1,932. Two of the five per-file figures are individually wrong (661→813, 313→489). #253 then added two more consumers to the same unowned surface: obs-join.py 514 and metrics-sample.py 317 → the real unowned Python consumer total is 3,091, a 60% understatement. #252's own framing ("By line count that is the largest sovereignty gap") depends on this number.

**Edit.** Under #252, apply the same correction the audit applied to #250: 1,932 → 2,260 for the five named files (telemetry-window 813, metric-desc-lint 489 are the two that moved), and add the 831 lines #253 introduced (obs-join 514, metrics-sample 317) for a 3,091-line unowned consumer surface.

### `#219` — ALREADY_DONE

*confidence: medium*

**Claim.** #219 is `pending` with no open work in its own terms: its instrument reports the entire ledger settled, both named survivors are resolved, and the second survivor's parked decision (D29) was declined by the Director on 2026-08-05. The audit's arch-ci cluster filed nothing on it.

**Evidence.** `python3 scripts/pr570-ledger.py --check` (raw inputs present on this host) → "pr570-ledger: OK -- 155 rows, 0 open"; the generator run reports "155 rows, 84 closed, 45 declined, 1 deferred, 25 done". #219's body: SURVIVOR 1 "rm-lighttraits-spread-multiply. DONE" (0a8fff9d, in the block's own commit list); SURVIVOR 2 "point-light-cap-and-cull... Folded into D29 for the Director's decision". docs/superpowers/drafts/osb-pr570-decisions.json D29 → `"status": "declined"`, `"reason": "DIRECTOR DECISION 2026-08-05, on the evidence the deferral was waiting for..."`, and the closing commit c363e052 "ledger: D29 declined by Director decision; B08 closed with it" is in #219's own citation list.

**Edit.** Close #219, or state in its body what remains. Its own instrument reports 0 open rows and its only deferred item (D29) was declined 2026-08-05 at c363e052. If it is being held open as a home for the pr570_ledger gate defect above, say so explicitly rather than leaving it reading as unfinished ledger work.

### `#256` — STILL_OPEN_TEXT_STALE

*confidence: medium*

**Claim.** RANK ADVICE — #256 holds rank 9 in a ranked do-next queue while declaring itself non-terminating, so it permanently occupies a queue slot it can never vacate and pushes four real deliverables down one place each forever.

**Evidence.** board.md #256 block, final line: "NOT A REVIEW TASK. Stays open across the observability arc, one entry per completed item." Its metadata is `{"rank": 9}` and the Next table renders it at position 3, "ready". Everything below it (#252 rank 10, #250 11, #245 12, #251 13, #237 14) is a bounded deliverable. Precedent that ranks are meant to clear: HEAD commit 849f97d2 records "Two completed tasks were still holding a rank (#235, #253); cleared. The board's own integrity check had been reporting it" — the same hygiene applies to a task that by construction never completes.

**Edit.** I would clear #256's rank (`TaskUpdate(metadata={"rank": null})`) and keep it as an unranked standing accumulator, promoting the ranks of #252/#250/#245/#251/#237 by one. If a rank is wanted, rank only its two decidable open items (T8 the forbid-column conflict, T9 the missing `telemetry` register row), which are Director calls with an end state.

### `#251` — STILL_OPEN_TEXT_STALE

*confidence: medium*

**Claim.** RANK ADVICE — #251 (rank 13) is the only task asking whether GL_TIME_ELAPSED samples were ever valid, and it sits five places below #255 (rank 8), the 47-minutes-per-scene 28-leg matrix that will bank per-leg profiles as "the regression baseline" for #252 and #250. That is the ordering #249 already paid for once.

**Evidence.** Premise verified as never-existed rather than renamed: `grep -rn "DISJOINT" source/` → zero matches; `grep -rni "disjoint" source/ scripts/` returns only four unrelated hits (SectorArray2D columns, a gather scratch-buffer comment, two entity_map_test comments). #251's own scope item 3 is retroactive — "Re-read the retained GPU numbers in light of whatever the disjoint rate turns out to be" — i.e. it can invalidate a table after the fact. #255's block: "RUN IT BEFORE THE REFACTOR, NOT AFTER... A completed matrix is the regression baseline those changes get compared against" and costs "~47 minutes unattended for one scene at 3 repeats". The precedent is #249, "RE-MEASURE: every GPU busy number from 2026-08-07 was mis-aligned; re-run both scenes with the fixed stamps" — a measurement-validity defect found after banking, which forced a full re-run. #251's fix is cheap and has a shipped precedent named in its own body: `render.gputimer.dropped` already exists as an eagerly-registered rejection counter (#247).

**Edit.** I would move #251 above #255 (i.e. into the 8-9 band) and note in #255's body that its GPU-side numbers inherit whatever #251 finds. Counter-argument to record if it is left where it is: the matrix's per-lever GPU busy comes from the out-of-process i915 PMU (scripts/lever-matrix.sh:510-528 starts pmu-engine-sample.py per run), not from GL_TIME_ELAPSED — so only the per-pass gpu_us in each leg's telemetry profile is exposed, not the headline PMU table.

### `#250` — STILL_OPEN_TEXT_STALE

*confidence: medium*

**Claim.** RANK ADVICE — #250 (rank 11) defines the sample type that #254 (rank 7) is the first consumer of, per #256's own T7, so the board is ordered to build the consumer four places before the vocabulary it is supposed to consume.

**Evidence.** board.md #256, T7: "The precondition is now MET: the join has been performed, so what the consumer must carry is known rather than guessed. It is four things... (a) the counter-vs-gauge distinction; (b) THREE distinct unavailabilities; (c) the covered-span denominator (T11); (d) the method declaration (T10). **All four are properties of a SAMPLE TYPE, which is what [#250] is for. That is the shape.**" #254's block independently requires three of those four by name — "MetricBoundedness decides whether a point may exist at all" (a), "RESAMPLE BY RE-DIFFERENCING RAW CUMULATIVE COUNTERS, never by interpolating a rate" (d). Both are ranked ready in the Next table (#254 position 1, #250 position 5). #250 scope item 1 is the enabling move and is pure relocation: "samplesFor() -- the one function that turns a reader result into MetricSamples -- lives in metrics_main.cpp, i.e. in the EXECUTABLE. A second consumer must reimplement it or shell out and parse JSON."

**Edit.** Either raise #250 to sit immediately below #254, or add a line to #254's body stating explicitly that it may hand-roll the four sample properties and that #250 will absorb them — so the duplication is a recorded decision rather than a discovery. Do not leave the dependency stated only in #256's T7, where a reader working the ranked queue will not see it.

---

## What the audit could not check

Could not check: (1) whether `dedup_measure` is currently red in GitHub Actions — build_linux_clang does run on push (build.yml:280) and does run ctest (testPreset 'linux-release'), but HANDOVER §5 forbids running `gh`/polling CI, so I verified only the local tree fact; (2) when the dedup leak crossed 596 — that would need rebuilds at older commits, which the E-core-pinned build rule makes expensive and which is out of scope for a read-only pass; (3) the Windows job colour for #201, same reason; (4) `metrics_mutual` remains the one gate that has never run anywhere (needs a live GPU client + CAP_PERFMON) — it is honestly reported as SKIP by run-gates.sh, so I did not file it, but note that lever-matrix.sh:516 records \"metrics_mutual is the gate that was supposed to catch that, and it has SKIPPED every run to date,\" which means the project's designated cross-check on every GPU number has zero executions on record; (5) `link_sweep` also exits 1 here, but only with \"STALE -- sources are newer than starbound, starbound_server; rebuild\" — that is a rebuild-needed state, not a defect, so I did not file it; (6) the four Director-perceptual items in #136 are unverifiable without hardware, same as the original audit found; (7) `matrix_prereq_ledger` reports 20 of 57 rows UNVERIFIABLE (no signature) with no ceiling on that number — gates.yml:373 declares this deliberate (\"That last number is the point\"), so I treated it as an honest measurement rather than a defect, but a growing unsignatured share is the one way that gate degrades silently.

---

## What was done with it

**Closed** (verified by hand before flipping, because a wrongly-closed task disappears forever):
`#200` `#201` `#202` `#207` `#219`.

**Corrected in place** (the live observability cluster): `#245` `#250` `#252` `#254` `#255`.

**Filed as new defects with no prior home**: `#257` (dedup ratchet red *and* invisible to the
runner), `#258` (`pr570_ledger` falls through to exit 0), `#259` (the per-leg join's PMU column is
never filled), `#260` (the ratified platform dissolution is on no task), `#261` (HANDOVER.md is 167
commits stale).

**Recorded and deliberately not applied**: the pointer rot in the dormant render / sim-lever /
older-arch tasks, catalogued in `#262`. Those tasks are all genuinely still open; what has rotted is
the file:line references a reader would follow, and nobody is following them while the Director's
no-lever-hunting ruling stands.

# TSSA levelling analysis — Sections 1, 2, 3 and 6

Source: read-only workflow `w7d1hgn52`, four analysis agents, 2026-08-02, against the spec at
`c16a0f07`. 54 findings.

**AN ENTRY HERE IS A CLAIM, NOT A FACT.** Three have been verified by hand and acted on — the
false `link_sweep` gate claim, the `N1.e` clause-vocabulary hole, and Section 3's missing N2/N3
clauses. The rest are unverified. Verify before acting: this file is a worklist, and a worklist
that reads as a findings register is the same defect the document keeps catching in itself.


## §1. What OpenStarbound is

**What the section is.** Section 1 (L69-198) is the only section of the spec that no instrument can read: it contains five markdown tables and not one `<!-- TABLE: -->` marker, so `spec-model.py` — "THE ONE READER of the target-state architecture's tables" — parses exactly three tables in the document (components L2752, grants L2932, elements L4638) and none of them is here. That matters more here than anywhere because Section 1 asserts the document's strongest cross-section rule: "a component register that uses a word this section has not defined is naming something the domain does not contain" (L74-75). That claim is (a) unchecked by all 18 gates, and (b) false as literally written — of 45 register component names, exactly 4 (`universe`, `world`, `content`, `participant`) are words Section 1 defines; 9 if compounds like `world_view`/`worldgen` count. Taken at its word the sentence condemns 36-41 of 45 rows. `scripts/prose-claims.py:113-116` writes the opposite fact — "components are what the CODE is made of, and the two vocabularies are deliberately different" — so the document and its only prose gate are two writers for one fact, disagreeing. I verified all findings by reading; I did not attempt to write or run any new instrument. The units of this section are NOUNS — 12 of them (universe, system, world, entity, authority, participant, device, player, content, mod surface, process, composition) — and they currently answer five different facet sets depending on which table they landed in: L86-91 asks "what it is · how many · persists?", L106-110 asks "what it does · how many · needs the others?", L126-131 asks five different questions of two nouns, L150-153 asks two, and `process`/`composition` get no table at all. That is the levelling defect: five shapes for one kind of unit, so a missing answer is invisible. The compositions table (L175-182) is not a unit set — Section 3 L332 already says "the components are the vocabulary and the compositions are sentences" — and it is the source of two of the four contradictions below; it should be demoted to illustration or deleted, not levelled.

**The repeating unit.** The unit is a DOMAIN NOUN, and there are 12: universe, system, world, entity (L86-91) · authority, participant, device (L106-110) · player (L126-131, contrasted with participant) · content, mod surface (L150-153) · process, composition (L163-169, prose only). The compositions table at L175-182 (graphical client, dedicated server, listen server, headless client, recorder, agent) is NOT a unit set — those are "sentences" per L332 — and it should be marked as illustration rather than given a facet shape, since two of the four register contradictions found live in it.

**Already at depth.** ['The `player` vs `participant` table (L126-131) is the only already-levelled unit in the section: two columns, five facets, every cell answered, and it is the single Section-1 statement the tooling depends on — `scripts/prose-claims.py:123` keeps `player` out of UNKNOWN_NAME with the reason "an ENTITY in a world, emphatically not a participant". Whatever shape replaces the current tables, these five answers must survive verbatim.', 'The composition insight at L171-191 — "**\\"Client\\" and \\"server\\" are not kinds of thing**" and "Read down that right-hand column and the taxonomy dissolves" — is load-bearing downstream and is reproduced almost verbatim by Section 10\'s `server` derivation (L4250-4258, "a server is not a subset of a client, which is what \'not a headless client\' means arithmetically"). The *idea* must be kept even though the illustrative table beside it (L175-182) is where two of the contradictions live.', 'The mod-surface rule at L155-159 — "**The bindings a script may call are a function of what its host composed**" — is the one Section 1 claim that is verified downstream rather than merely asserted: Section 5 generalises it (L677-680, "a binding is the public surface of a component, so a composition that does not link the component does not have the binding") and the register implements it (`script` duty: "hosts Lua; owns no bindings"; `client_headless` supplies 10 of 11 groups, L665-666).', '"**A server has neither.**" (L138-143) and its consequence, "Any design that gives a server a participant has confused watching with owning, and will eventually ask an authority to correct itself" — reproduced as the `rejected` facet of `server` at L4255. The two agree only because this section stated it first; it is the section\'s clearest demonstration of why the domain seals before the model.', 'The seed/persistence argument at L93-95 — "**a world that has never been visited does not exist yet, and costs nothing.**" — which is what `worldgen`, `world_gen` and the save-size claim rest on.', 'The scoping refusal at L193-198 ("It does **not** say how many processes there are, which machine each runs on, or what crosses between them"). The rule is right and is the reason Section 1 can seal before Sections 3 and 5 exist; only its enforcement is missing.', 'The domain/design framing at L71-75 ("Nothing here is a choice anyone made about software") and the standing intent behind the vocabulary claim. The claim needs to be made checkable and narrowed — not deleted; it is the correct ambition, and facet 5 above is the cheapest way to keep it.']

**Proposed facet shape.**

- **what it is** — one clause, no "and". Earns its slot because A3's own Law-of-One test (L230: forbids "'and'/'also' in a duty") is the document's stated defect detector and three of the twelve definitions currently fail it: participant = "holds a *view* of a world, predicts against it, and asks the authority to change it" (L109) is three duties; authority = "owns the truth ... and answers to nobody about it" (L108) is two. Nothing checks this today — `spec-consistency.py:502` IMPLEMENTS_ARITY is the only "Law of One" instrument and it counts `==>` edges, not conjunctions.
- **how many, and per what** — a cardinality with an explicit denominator, drawn from `spec-model.py:57` CARDINALITIES (PROCESS · PARTICIPANT · UNIVERSE · WORLD · DEVICE) so the answers are machine-comparable with Section 11's clock table and Section 13's element register. Earns its slot because the column exists today for 9 of 12 nouns and is broken in both directions: `universe` says "exactly one" (L88) with no denominator at all, while `device` says "zero or more, per participant" (L110) with the wrong one.
- **what it requires — and the composition that would falsify it** — the other Section-1 nouns that must exist, plus the observation that would show the answer false. Earns its slot because the two most load-bearing claims in the section are exactly the two with no falsifier: "An authority requires neither of the others" (L112) and device "needs a participant" (L110). The falsifier for the second already exists in the document — L344's replay verifier, "`world` authority + `transcript`, no participant" — and nobody connected them. This is the facet that makes Section 2's fact-vs-decision test (L211) runnable on Section 1's own sentences.
- **what it outlives** — the save file, the process, the visit, the tick. Earns its slot because it is the facet that already does the section's hardest work (it is what separates player from participant at L129-130: "it is in the save file" vs "it dies with the process"), it is asked of only 6 of 12 nouns today, and it is what `storage` ("durable state, and migrating it forward") and the seed argument at L93-95 are derived against. Ask it of `content`, `mod surface`, `authority` and `composition` and the register's persistence duties get an owner.
- **how it meets the register** — the component name(s) that realize this noun, or "role only — no component bears this name", plus the register word that looks like it and is not it. Earns its slot because it is the only facet that turns the section's own claim (L74-75) into something a script can read: a two-column noun→component mapping, checked in the direction that matters. It is also the facet that would have caught both silent failures found here — `system` (L89), which is realized by no component, no clock and no element anywhere in 5,993 lines, and `device`, which is a hardware role at L110 and a code ZONE at L899/L1360 with the same word and the same gloss.

**Findings.**

- `high/verified` Section 1's own governing claim is unfalsifiable in practice and false as literally written, and no gate checks it. L74-75: "a component register that uses a word this section has not defined is naming something the domain does not contain." Of the 45 names in the components table, only `universe`, `world`, `content` and `participant` are words this section defines. `core`, `base`, `platform`, `host`, `transport`, `scene`, `sound`, `net`, `storage`, `presentation`, `game`, `worldgen`, `celestial`, `windowing`, `interaction`, `script`, `colocation`, `frontend`, `rendering`, `mixing`, `transcript`, `gpu`, `audio` and every backend and entrypoint are not.
  - **evidence** spec:L74-75 vs `python3 scripts/spec-model.py` components() = 45 rows; 4 whole-word matches, 9 counting compounds. `scripts/prose-claims.py:113-116` states the contrary rule directly: "`authority`, `device` and `player` are what the SYSTEM is made of; components are what the CODE is made of, and the two vocabularies are deliberately different." `scripts/ci/run-gates.sh` → 18/18 OK.
- `high/verified` Section 1 contains no `<!-- TABLE: -->` markers, so all five of its tables are invisible to spec-model.py — the section that defines the vocabulary is the one section written entirely in the form A2 and A4 forbid. L42 promises "every rule lands in a register or a table where an instrument can read it, and nothing normative lives in prose alone", yet Section 1's normative rules (device needs a participant; an authority requires neither; a register word must be defined here; a placement claim here is a defect) all live in unmarked tables and prose.
  - **evidence** `grep -n "TABLE:" spec` returns exactly 6 lines: components L2752/L2800, grants L2932/L2978, elements L4638/L4665. spec-model.py:22-26 — "TABLES ARE DELIMITED, NOT DETECTED ... A MISSING MARKER IS A HARD FAILURE". Section 1's tables at L86, L106, L126, L150, L175 have none.
- `high/verified` L112-114 files a decision as a domain fact, which is exactly the error Section 2's own test exists to catch. "**An authority requires neither of the others.** ... Section 2 states it as a domain fact rather than a design goal." Section 2 states no such fact: F1 is "Devices are optional" (L254) and covers devices only; F2/F3/F4 are about time, steps and content. The participant half is **D9** — "What ticks must not be derived from who is watching" (L447) — explicitly a decision, and Section 5 L573 says "D9 costs a replacement, not a deletion."
  - **evidence** spec:L112-114 vs L254 (F1 text names only "display, speaker or input device"), L447 (D9), L573. Section 2's own separator test at L211: "If an implementation can violate the claim and still be Starbound, the claim is **not** a fact" — and L572 records that the superseded implementation did violate it and was still Starbound.
- `high/verified` The device row's cardinality is falsified by the document 200 lines later. L110 declares device "zero or more, per participant" / "needs a participant". L344 composes one without: "| a replay verifier | `world` authority + `transcript`, no participant | none |" — `transcript` is a recorder, and L1360 glosses `device/` as "a display, a speaker, a file, a recorder". L345's offline map renderer (`worldgen` + `world_view` + `rendering`) is a second instance.
  - **evidence** spec:L110 vs L344-345; `transcript` is zone DEVICE per spec-model components(); L1360 zone gloss.
- `high/verified` Three rows of the compositions table contradict Section 10 and the grant table. L180 says "a headless client | a participant, and no device at all"; L181 gives "a recorder" a separate row with "a file device"; L182 says an agent is "the same shape as a headless client". The register says `client_headless` IS the recorder and links `transcript`, and that agent and headless are deliberately different products.
  - **evidence** spec:L180-182 vs L2972 ("`client_headless` | core, participant, colocation, host_null, windowing, frontend, transcript, transport_tcp | the only place the recorder is named"), L3837 ("plays and **records**"), L3841 ("`client_agent` links exactly `client_headless` minus `windowing`, `frontend` and `transcript`"), L3846-3848 ("Those are two products, not two readings of one").
- `medium/verified` `system` is defined as a first-class noun with its own simulation and its own cadence, and is realized nowhere — and the forward citation names an instrument that does not exist. L99: "Section 11 gives both a name and a rate". Section 11's five-clock table (L4380-4386) is driver · participant sim · universe · world · audio. There is no system clock, no `system` component, no system element.
  - **evidence** spec:L89, L97-100 vs L4380-4386. `grep -n "systemTick\|system clock\|per system"` over the whole file returns one hit, L90, inside Section 1 itself. spec-model components() has no `system`; CARDINALITIES (spec-model.py:57) has no SYSTEM.
- `medium/verified` The `device` role and the `DEVICE` zone are one word with two meanings, and this is precisely the near-collision the section promises to resolve. L78: "where two nouns are nearly the same thing the difference is stated explicitly". L110 defines a device as the hardware, "something outside the simulation entirely"; L899 and L1360 use the same gloss for a directory of nine components (`rendering`, `mixing`, `transcript`, `gpu`, `audio`, ...) that are our code facing hardware, none of which is a display or a speaker. Meanwhile the window lives in `host_sdl`, zone MACHINE. "Role" is itself overloaded three ways (L102 domain roles, L1336 "Two roles, two names" for backends, L996 a C++ interface role).
  - **evidence** spec:L78, L110 vs L899 ("**DEVICE** | outside seam 1; meets hardware or a recorder") and L1360 ("| **`device/`** | a display, a speaker, a file, a recorder | 9 |"); L177 asks a composition to compose "a display device", which no composition can do — it composes `rendering` + `gpu_opengl`.
- `medium/verified` L191 "Section 7 turns it into a placement rule" — Section 7 contains no role-keyed placement rule. Section 7's rules are KIND rules (L878-885) and ZONE rules (L897-901) about seams, not about which role owns a duty; the word "role" appears in Section 7 only for backend roles (L1336) and C++ interface roles (L996). The rule Section 1 promises ("which role owns X — and then any composition containing that role has it") is never restated as a rule anywhere.
  - **evidence** spec:L189-191 vs L824-1413 grepped for role/authority/participant: no placement rule; the nearest instrument is `spec_consistency`'s PLACEMENT verdict (L419), which checks "a process, thread or machine it may not name" — a different rule.
- `medium/verified` Three of Section 1's definitions carry the conjunction A3 forbids, and no instrument reads a duty or definition for "and". participant: "holds a *view* of a world, predicts against it, and asks the authority to change it" (three duties); authority: "owns the truth ... and answers to nobody about it". The register inherits it: `participant`'s duty is "owns the participant's clock and composes its parts".
  - **evidence** spec:L108-109, L230 ("god objects, dual-purpose modules, \"and\"/\"also\" in a duty"), L239; register duty from spec-model components(). Only Law-of-One instrument in the tree is `scripts/spec-consistency.py:502` IMPLEMENTS_ARITY, which counts `==>` edges on BACKENDs.
- `low/verified` The one gate that touches this section's nouns cites it by a section number that no longer exists and by a sentence it does not contain. prose-claims.py exempts `authority`/`device`/`player` from UNKNOWN_NAME as "section 0" roles and justifies the exemption with "it is the reason section 0 says a role is not a component in so many words" — Section 1 never says that. The word "component" appears exactly once in L69-198, in the claim at L74.
  - **evidence** `scripts/prose-claims.py:113,117,120,121,122,123` (six "section 0" references) vs spec:L69 "## 1. What OpenStarbound is"; `awk 'NR>=69 && NR<=200' | grep component` returns one line (L74). This is the same class prose-claims.py:216 names against itself: "THE GATE'S VOCABULARY OUTLIVED THE DOCUMENT'S."
- `low/verified` L73 inverts the document's own direction metaphor in the section's opening justification: "It seals first because everything above it cites it". The preamble at L32 fixes the direction the other way — "each layer may cite only the layers above it: **the domain** (Section 1) ... **axioms** (Section 2)" — under which nothing is above Section 1 and the sentence should read "everything below it".
  - **evidence** spec:L73 vs L31-36.
- `low/likely` The section states a falsifier for itself with no instrument behind it, and arguably fails it. L198: "A reader who finds a placement claim in this section has found a defect", after L193-195 disclaims saying "how many processes there are". L179 then says a listen server is "an authority *and* a participant, **in one process**", and L128-129 bind participant lifetime to the process. Nothing runs this test.
  - **evidence** spec:L193-198 vs L179, L128-129. No gate in .github/workflows/gates.yml reads Section 1 for placement claims.
- `low/verified` The `universe` row is the only one of the four whose "how many" has no denominator: "exactly one" (L88), against "many per universe", "many per system", "many per world". Under N1 (worlds and participants on different machines) the scope of "one" — per save, per deployment, per process — is exactly the thing that is not obvious, and Section 11's clock table answers it circularly ("universe | one per **universe**").
  - **evidence** spec:L88-91 vs L4383.
- `low/verified` Two sentences narrate the document's drafting history rather than stating the design: L79 "because every one of those near-collisions has already produced a defect in this document" and L123 "This is the near-collision that has cost the most". Section 10 has a dedicated `history` facet for exactly this content; in Section 1 it is unlabelled prose mixed into the definitions.
  - **evidence** spec:L78-79, L123 vs the six-facet shape at e.g. L3293 (`| **history** |`).


## §2. Axioms

**What the section is.** Section 2 is two tables that LOOK levelled and are not: the facts carry a real three-facet shape (claim · how it is known · what it forbids), the axioms carry a three-column shape whose middle column does not answer its own header. The header says "why it binds this system"; for six rows the cell is a bare validity tag and for A0 it is the word "umbrella", which is not a tag at all. The section states the adoption rule as "a system is bound by an axiom if and only if its architecture satisfies that axiom's tags" (line 222-223) and then discharges only `any-system` — `stateful` (A1) and `declarative` (A2) are never claimed of this system anywhere in 5,940 lines. Not one of the twelve units names an instrument: every axiom's "what it forbids here" is prose that nothing reads, including A3's "and"/"also" test, which is the document's single most-cited rule and is enforced by no gate. On the facts: F1's and F2's instruments exist but prove less than the sentence says, F3's names no artifact at all, F4's names a one-off sweep no script performs — and F2's across-processes clause is contradicted by the live tree, where `UniverseTimeUpdatePacket` carries an absolute remote timestamp that the receiver writes straight into its own monotonic clock. Levelling here is mostly about adding two facets the axioms never had (a discharged binding, and a falsifier/instrument), not about rewording what is there.

**The repeating unit.** 12 units in two tables. EIGHT adopted axioms — A0, A3, A4, A8, A9, A14 (unconditional set), then A1 and A2 (conditional set) — each a row of: name+gloss · validity tag · what it forbids here. FOUR domain facts F1-F4, each a row of: the fact · how it is known · what it forbids. The two tables do not share a shape today: the facts' middle column is evidence, the axioms' middle column is a tag. Four prose paragraphs sit between and after them (the fact-vs-decision test, the A3/A8 emphasis, the multi-agent exclusion, and the F1+F2 / F3 / F4 narrowing notes); those are commentary on units, not units.

**Already at depth.** ['The fact-vs-decision test, stated FIRST and as a block quote, with its consequence named: "If an implementation can violate the claim and still be Starbound, the claim is **not** a fact. It is a decision, and it belongs in Section 5 with a cost beside it" (lines 211-212), followed by "five claims that read as self-evident properties of the domain are in Section 5 instead, each with the price of choosing it named" (216-218). This is the section\'s load-bearing move and it is what D9-D13 hang from; it must survive any rewrite at the top, before the tables.', 'The narrowing paragraphs on F3 (264-267) and F4 (269-273), which state what each fact does NOT say. F3\'s "It does **not** say the step *content* is a pure function of state and inputs — that is a far stronger claim, it is false of the implementation this fact was verified against" is the reason D12 exists as a priced decision; F4\'s instance/kind split ("Adding an instance of a known kind is a content change; adding a *kind* is an engine change") is the reason D13 exists. These are not commentary — they are the guard rails that stop downstream citation from inheriting a stronger claim than was verified.', 'The overclaim warning that governs the whole facts table: "Where a narrower statement is what survives verification, the narrow one is what is stated — an overclaimed fact is worse than no fact, because everything citing it inherits the overclaim silently" (lines 249-250). Section 10 line 4016 explicitly cites this sentence as the reason F4\'s count is recorded rather than glossed.', 'F4\'s exceptions enumerated BY NAME with the role of each — `human` (species fallback), `money` (currency), `perfectlygenericitem` (item-recovery fallback) — rather than a count. The names are what make the fact re-checkable by hand; a levelling pass that compressed them to "three exceptions" would destroy the only re-runnable thing in the row.', 'F2\'s deliberate two-width structure, with the stated reason "because the fact is cited at both widths". Both widths are genuinely used downstream — process width at lines 5130 and in Section 13, device width at 3158, 3323, 3480, 4373 and 5111. If a Law-of-One split separates F2 into two facts, both halves must keep their downstream citations; collapsing to one width would orphan half the uses.', 'The adoption-by-domain-of-validity rule itself (lines 222-223). It is the right rule — it is only undischarged for two tags. The fix is to discharge `stateful` and `declarative`, not to drop the rule for a flat "these are our axioms" list.', 'The explicit, reasoned exclusion of A5-A7 and A10-A13 (242-244). The rationale needs replacing with a tag-based one to match the adoption rule, but the exclusion must remain visible and named: a reader who knows the canonical set needs to see that the absence was decided rather than overlooked.']

**Proposed facet shape.**

- **the claim** — the rule (axiom) or the property (fact), in one sentence with no conjunction. Earns a slot because it is the unit's identity, and because A3's Law of One is a test on a DEFINITION as much as on a duty: the document applies it to `celestial`'s duty at line 4195 and to `transport` at 5703 but never to its own axiom glosses. Forcing one clause per unit exposes A3 ("one concern per module, composed without leaking internals"), A4 ("expansion over summarisation, structure over prose"), F2 (which concedes it: "verified in two halves") and F4 ("opaque, with three named exceptions") as compounds.
- **why it binds HERE — tag plus discharge.** Not the tag alone: the tag AND the sentence asserting this architecture carries it. Earns a slot because the section states adoption as an iff over tags (line 222-223) and then discharges exactly one tag for all eight axioms. With this facet, A1's `stateful` and A2's `declarative` become visible holes rather than plausible-looking cells, and A0's "umbrella" is exposed as an answer to a question nobody asked. Facts answer the same facet with the property of Starbound they rest on.
- **what it forbids HERE** — one concrete, Starbound-specific prohibition, phrased so a reader can point at a candidate violation in this architecture. Earns a slot because it is the only facet that makes an adopted axiom actionable inside this document, and because three current entries forbid things about documents in general (A4 "prose that carries a rule no instrument can read", A14 "a finding that lives only in a conversation", A0 "additions with no line of descent back to stated intent") rather than anything about the system being designed. One prohibition per unit also kills the current doubling in A4's and A8's forbid cells.
- **what would show it false, and what reads it** — a named instrument (gate name, script path, source file:line) or an explicit "one-off, not standing, and here is what a re-run costs". Earns a slot on the document's own terms: A2 forbids "a rule that exists in prose and nowhere a machine can read" and A4 forbids "prose that carries a rule no instrument can read", and Section 2 is at present entirely prose. Section 4 already gives P1 and P2 this facet (`grant_sweep`, `render_layering`, `spec_consistency`'s PLACEMENT verdict) and Section 10 gates 270/270 on a `falsified` facet, so this is the section that is behind, not a new idea. It converts F3's "verified in the step loop" and F4's "verified by sweep" into things a reader can open or re-run.
- **who cites it downstream** — the sections, decisions or components that stand on this unit. Earns a slot because the section's own second sentence claims it ("Everything downstream cites them by name", line 207) and the claim is false for three of eight axioms; because it is mechanically checkable with the regex `spec_consistency` already compiles at spec-consistency.py:485; and because an axiom that nothing cites is either dead weight or a warrant nobody wrote — a distinction this facet forces someone to make.

**Findings.**

- `high/verified` F2's across-processes verification clause is contradicted by the current tree: a wire packet carries an absolute remote timestamp that the receiver writes directly into its own monotonic clock.
  - **evidence** Spec line 255 states the fact's evidence as "**Across processes:** no wire message is ever interpreted in the receiver's clock domain — remote timestamps are echoed to their originator or consumed purely as differences." But source/game/StarUniverseServer.cpp:783 builds `make_shared<UniverseTimeUpdatePacket>(m_universeClock->time())` and broadcasts it on an interval (`sendClockUpdates`, gated by `clockUpdatePacketInterval`), and source/game/StarUniverseClient.cpp:754 does `m_universeClock->setTime(universeTimeUpdatePacket->universeTime);` — absorbed absolutely, not echoed and not differenced. `Clock` is physical time: source/core/StarTime.hpp:43-45, "Keeps track of elapsed real time since a given moment. Guaranteed monotonically increasing", with `setTime` documented at :64-65 as "Override actual elapsed time with the given time." The document's own clock register counts this one as ours: spec line ~4363, "| **universe** | `universe` | FREE | **universe** |". The words "universeTime", "UniverseTimeUpdate" and "time base" appear nowhere else in the spec, so the case is not addressed and then set aside — it is simply absent. Either F2's evidence clause needs the exception named (as F4 names its three), or the universe clock has to be reclassified out of the clock table.
- `high/verified` F2's device half names an SDL call that does not do what the sentence says in a configuration the tree ships: `SDL_GL_SwapWindow` blocks on refresh only with vsync on, and vsync is a runtime toggle.
  - **evidence** Spec line 255: "`SDL_GL_SwapWindow` blocks on the display's refresh, so the panel decides when a frame lands ... both run when they run", stated unconditionally as the verification of a FACT. source/application/StarMainApplication_sdl.cpp:1253-1264 is `setVSyncEnabled(bool)`: enabled tries `SDL_GL_SetSwapInterval(-1)` then `(1)`, disabled calls `SDL_GL_SetSwapInterval(0)`. The same file's own comment at :819-821 spells out both regimes — "With vsync OFF this blocks only when the GPU queue is full ... With vsync ON it blocks for the frame pace instead ... which is why meta.vsync is written into every snapshot." With vsync off the panel does not decide when a frame lands, so the instrument is real but measures the claim in only one of its two supported states. The audio half is clean: `SDL_OpenAudioDeviceStream` at StarMainApplication_sdl.cpp:620 and :681 is genuinely a pull.
- `high/verified` A1 and A2 are adopted under a rule the section never discharges for them, and A0's binding cell answers a different question entirely.
  - **evidence** The rule is stated at line 222-223: "a system is bound by an axiom **if and only if** its architecture satisfies that axiom's tags." The discharge follows at 223-225 and covers exactly one tag: "Both the architecture described here and the practice of describing it satisfy `any-system`." A1's cell is `stateful` (line 235) and A2's is `declarative` (line 236); grep over the whole 5,940-line document returns those two tokens ONLY inside those two rows — the document never says this system is stateful or that it is declarative. A0's cell is the word "umbrella" (line 229), which is not a tag, so by the section's own iff there is nothing to satisfy. Under the column header "why it binds this system", three of eight rows do not answer.
- `medium/verified` The section's own claim that every adopted axiom is cited downstream is false for three of the eight.
  - **evidence** Line 207: "Everything downstream cites them by name." Counting occurrences of each token across the whole file: A0 = 1, A4 = 1, A14 = 1 — each appearing only in its own table row at lines 229, 231 and 234 respectively. By contrast A3 = 9 occurrences, A8 = 5, A1 = 5, A2 = 3, A9 = 2. Three adopted axioms are load-bearing for nothing in the document, which either falsifies the sentence at 207 or marks three warrants nobody wrote.
- `medium/verified` No axiom row has a falsifier or an instrument, including A3's "and"/"also" test — the document's most-cited rule, enforced by no gate.
  - **evidence** The axiom table has three columns and none is evidence; every "what it forbids here" is prose. A3's forbid is "god objects, dual-purpose modules, \"and\"/\"also\" in a duty" (line 230) and Section 4 raises it further: "an adopted axiom already says it, in stronger form: *\"and\"/\"also\" in a description is a violation*" (line 399). The document then applies that test by hand three times — `celestial` at line 4195 ("Its duty reads *\"the star map's vocabulary **and** its lookup interface\"* — an \"and\", which is A3's own test"), `transport` at 5703, and the repeat at 5919. Nothing in scripts/ checks it: spec-consistency.py's Law-of-One check is IMPLEMENTS_ARITY, which counts `==>` edges (spec-consistency.py:502-522), and a grep of the duty-handling paths (lines 118, 243-296, 413-425) finds no conjunction test on the duty text. So the two known violations were caught by a reader and recorded as owed, exactly the outcome A2 and A4 forbid.
- `medium/verified` A2's forbid and A4's forbid are the same rule written twice in adjacent rows — two writers for one fact, in the section that owns the concept.
  - **evidence** Line 231, A4: forbids "prose that carries a rule no instrument can read". Line 236, A2: forbids "a rule that exists in prose and nowhere a machine can read". Same prohibition, two owners, two places to keep in sync — and Section 4 removed five principles for precisely this reason: "leaving them here made the document state the same rule twice — which is the drift this section is supposed to forbid" (line 394-395), plus "One writer per fact" re-homed to A1+A2 at line 402.
- `medium/verified` The multi-agent exclusion is justified by a rule different from the adoption rule, is contradicted by A14's own presence, and has no falsifier — the gate that could enforce it accepts the excluded axioms.
  - **evidence** Adoption is by tag (line 222-223) and explicitly extends to "the practice of describing it" (line 224). The exclusion at 242-244 switches rule: "A5–A7 and A10–A13 bind systems with multiple agents, an LLM in the loop, or autonomy. They govern how this document is *made*, not what it describes" — a reason of kind, not of tag, and one that A14 fails while being adopted, since A14's forbid is "a finding that lives only in a conversation" (line 234), a rule about how the document is made. No instrument holds the line: spec-consistency.py:485 compiles `CITES = re.compile(r'\*\*(A(?:[0-9]|1[0-4])|F[1-4]|N[1-3](?:\.[abc])?|P[1-2]|D(?:[1-9]|1[0-3]))\b')`, so a warrant citing **A5** or **A12** satisfies the UNWARRANTED check today. The comment directly above that line warns about this exact failure — "A gate whose vocabulary outlives the document's is worse than no gate: it certifies the stale" — and then the regex admits nine axioms the document does not adopt. I checked: no warrant currently cites A5-A7 or A10-A13, so this is a latent hole rather than an active violation.
- `medium/verified` F4's "verified by sweep" names no standing instrument, and the number three is owned by no generator — in the one fact whose count has already been wrong once.
  - **evidence** Line 257: "verified by sweep: no material, liquid, monster, object, dungeon or biome instance name appears in the game sources. **Three do**". No script in scripts/ performs such a sweep — grep across scripts/*.py for the exception names or any content-name scan returns nothing, and none of the 18 registered gates in .github/workflows/gates.yml reads content instance names. Section 10 records that this exact count was previously wrong: line 4016, "Calling `perfectlygenericitem` the *single* counter-example is the tempting form and a one-line sweep refutes it; the count is recorded here rather than in prose because F4's own preamble warns that everything citing an overclaimed fact inherits the overclaim silently." The claim does hold today on spot checks — `"dirt"`, `"water"`, `"lava"` each return 0 hits in source/game/, and the 4 hits for `"grass"` are a biome placement KIND (StarBiomePlacement.cpp:11,32,68) not an instance — but a fourth leak would be caught by nobody.
- `medium/verified` F3's "how it is known" cites no artifact at all — the weakest of the four columns — and the only step-loop instrument in the tree does not measure what F3 claims.
  - **evidence** Line 256: "verified in the step loop; the counter, not the clock, is what subsystems are scheduled against." No file, no counter name, no gate. The candidate instrument, loop-inventory.py, is about something else: its docstring says it exists to reconcile the COUNT of cadence loops ("A count nobody can recompute is a claim, not a measurement"), with verdicts UNCLAIMED / STALE / SPEC DRIFT — nothing about whether subsystems key on a step counter versus elapsed time. The underlying evidence does exist and I located it (source/game/StarWorldServer.cpp:705 `++m_currentStep`, :741 `m_scheduledWakes.maybeTake(m_currentStep)`, :828 `entity->nextEngineWakeStep(m_currentStep)`), which is why this is a citation defect rather than a false fact: the reader is given a claim where F1, F2 and F4 are each given something to open.
- `low/verified` F1's named instrument proves the first half of its own evidence sentence and not the second.
  - **evidence** Line 254: "proven at link time: the simulation libraries link **and tick** with no rendering, windowing or application objects present at all" (repeated verbatim in Section 10's `sound` derivation, line 3370). Ticking is not a link-time property. The link half is real and checkable — source/server/CMakeLists.txt:29 names only `star_extern`, `star_core`, `star_base`, `star_game` for `starbound_server`, and scripts/link-sweep.py exists to attribute binary symbols to components — but link_sweep is deliberately NOT one of the 18 gates (registered as a ctest only, source/test/CMakeLists.txt:343-345, Linux-only, and it reads a build tree). Nothing in CI or in the register demonstrates a device-free tick, and task #199 records "a real headless client is its own engineering effort".
- `low/verified` F4's gloss for `money` is narrower than the tree it was measured from.
  - **evidence** Line 257 describes it as "`money` (the currency the quest-reward path names)", repeated at line 4016 as "the quest-reward currency". The quest path is there (source/game/StarQuests.cpp:276, :736), but so is a second, unrelated caller: source/game/StarPlayer.cpp:751-752, `m_inventory->currency("money") * modeConfig().reviveCostPercentile` and `consumeCurrency("money", ...)`. The fact — three names leak — survives; the parenthetical that explains WHY this one leaks does not, and it is stated twice so it drifts twice.
- `low/verified` The paragraph justifying F1+F2 is written twice, near-verbatim, in two sections — the doubling the document elsewhere treats as a defect class.
  - **evidence** Section 2, lines 259-262: "**F1 and F2 are why a distributed Starbound is reachable at all.** If a simulation needed a display, there would be nothing to place on a headless machine; if two processes shared a clock, no seam between them could be honest." Section 3, lines 309-313: "**F1 and F2 are why N1 is reachable at all** ... If a simulation required a display there would be nothing to place on a headless machine; if two processes shared a physical clock, no seam between them could be honest about time." Same claim, same two clauses, two owners, and no instrument compares them — while Section 4 deleted five principles specifically because "leaving them here made the document state the same rule twice" (line 394-395).
- `low/verified` Adjacent evidence that the instrument-claim class is still live in the tree, though this instance is mid-repair and sits outside Section 2.
  - **evidence** I ran `python3 scripts/prose-claims.py --check` (read-only): it FAILS right now with "UNREGISTERED_GATE the document says something is gated by `link_sweep`, which is not a step in gates.yml". Spec line 5267 currently reads "Gated as `link_sweep`". Commit 78a8a4b5 ("gates: an instrument the document names must be one that runs", 2026-08-02, claiming "18/18 gates green") fixed that row BY HAND inside a `<!-- BEGIN GENERATED: spec-measures coverage -->` block and did not touch the generator; scripts/spec-measures.py:199 still emitted "Gated as `link_sweep`", so the next regeneration reinstated the false claim. The working tree has an uncommitted fix to spec-measures.py, so this is being repaired — I flag it only because it is the same defect class the caller asked about, and because it demonstrates the mechanism: a fact with a generator AND a hand-editor has two writers and will drift back.


## §3. North star

**What the section is.** Section 3 (lines 280-367) is three goals at three different levels of finish. N1 is fully levelled: four labelled clauses in a fixed four-column table, each with a falsifier, and ten register warrants cite them individually. N2 is two paragraphs with no clauses, no falsifier, no instrument, and it restates A3 and P1 verbatim — the exact drift Section 4 removed five principles to fix (line 399). N3 has a genuinely good falsifiable test but its evidence table has drifted from the register: of five rows, row 1 predates `transport`, row 2 contradicts Section 8's device staircase at line 1327, row 3 composes a scene sink with no scene producer, and three of five rows are already declared ENTRYPOINT components in the 45, which makes the "new components needed: none" column false by the document's own taxonomy. N1.d's headline claim — "countable ... gateable rather than aspirational" (306-307) — names an instrument that does not exist: no script counts round trips, Section 15 lists the ratchet as live (5537), and Section 18 admits it "has no metric and no starting ceiling" (5938). N1.d also warrants zero of 45 components and is not in the gate's citable vocabulary (spec-consistency.py:486 accepts only .a/.b/.c). READ-ONLY: I edited nothing; all gates were run only in --check mode and were green.

**The repeating unit.** The unit is a NORTH-STAR CLAUSE, not a goal. There are three goals and eight clauses' worth of content, but only four clauses exist as labelled, citable units — N1.a through N1.d. N2 and N3 are not subdivided at all, and both should be: N2 carries three separable claims in two sentences (one duty per component / dependencies declared and build-enforced / no god objects, no ambient singletons) and N3 carries two (components are the exhaustive vocabulary / compositions are open-ended sentences). Unsubdivided, neither can be cited precisely — and the register proves the cost: warrants cite N3 seventeen times and N2 once across 45 components, because a warrant citing N3 cannot say which half of N3 it means. Target: 3 goals × 2-4 labelled clauses = 8-10 units, N1.a-d kept as-is, N2.a-c and N3.a-b added. Two nested levels, as today, but with the clause table mandatory at every goal instead of only under N1.

**Already at depth.** ['N1\'s four-column clause table (297-302) is the model the rest of the section should be levelled to. Do not lose the columns or the wording: "| | the clause | what it forbids | how you would know it failed |" with entries like "a shared handle in a payload — it compiles, it works co-located, and it is a machine boundary that cannot be crossed". It is the same discipline as Section 10\'s six facets and Section 16\'s four, applied one layer up.', 'The N1.a-N1.d labelling scheme itself, and the sentence justifying it (293-295: "A warrant that says \\"N1\\" without saying which clause has not said much"). Ten register warrants cite a specific clause; the labels are load-bearing and citable, and the counts prove the mechanism works.', 'N1.d\'s substance, independent of its false gateability claim: "**N1.d grades the shape of an interface rather than merely its existence** ... A seam can satisfy every other clause and still be unusable across a network if it demands twenty round trips per frame" (304-306). The clause is right; only the sentence claiming an instrument is wrong. Rewrite the instrument claim, keep the clause.', 'N3\'s falsifiable test as a *formulation* (336-338): "Name a capability the system does not have. Ask whether it needs new *components* or only new *wiring*." This is the strongest goal-level falsifier in the document. The table under it has drifted, but the test is what makes N3 the most-cited goal in the register (17 warrants) and must survive intact.', 'N3\'s vocabulary/sentences framing (332-334): "the components are the vocabulary and the compositions are sentences. The document enumerates the vocabulary exhaustively and the sentences only by example, because an architecture that can express a fixed list of aggregates has not achieved anything a build flag could not." This is what licenses the register to be closed and the composition list to be open, and Section 9\'s generated composition graphs depend on it.', 'The N1/N3 unification paragraph (351-355): "**N1 and N3 are one property seen at two scales.** Composition is placement inside one process; distribution is placement across several ... satisfy it dishonestly — with components that only compose in the arrangements someone anticipated — and N1 is a rewrite wearing a config file." It is the reason both goals coexist rather than collapsing, and it explains why P2 ("Placement is wiring") is their runtime twin.', 'The goal-vs-fact definition (282-285): "Goals are what we *want* ... a goal can be abandoned, and abandoning one would change the architecture rather than merely disappoint someone." This is the test that exposes N2\'s duplication defect, so it must be kept exactly — it is the section\'s own instrument.', 'The F1/F2 grounding (309-313, minus its final drafting-history clause): "If a simulation required a display there would be nothing to place on a headless machine; if two processes shared a physical clock, no seam between them could be honest about time. Both were *verified* rather than assumed." This is what distinguishes N1 from an aspiration and it cites verified facts by name.', 'The concrete hardest-placement case (359-367): "the whole graphical front end on one machine, the participant and the authority on another, a network between them" and "**A seam only proves itself when something runs with nothing on the other side of it**". The case must survive levelling even though its packaging should change — it is the design driver for the presentation seam and for `client_headless`.', 'The rule that a decision serving no goal does not belong in the document (282-283). The rule is currently false in practice, but it is the right rule and it is the one facet 5 ("who cites it") would make countable rather than aspirational — the same repair N1.d needs.']

**Proposed facet shape.**

- **the clause** — one sentence, imperative, and no "and". Earns its slot because A3's own test is that "and"/"also" in a description is a violation (line 399), and N2's title ("A sovereign, comprehensible engine") plus its second sentence ("Every dependency is declared, and the declaration is enforced by the build") both fail that test today. Forcing one sentence per clause is what performs the subdivision rather than merely asking for it.
- **what it forbids** — the concrete design move this clause rules out. Earns its slot because N1's existing column is the strongest thing in the section ("a shared handle in a payload — it compiles, it works co-located, and it is a machine boundary that cannot be crossed") and N2/N3 have no equivalent, so a reader cannot tell what N2 would reject. Keep N1's column verbatim; it is the model.
- **how you would know it failed** — the observation, not the worry. Earns its slot because it is already Section 16's second facet (5554: "If there is none, the entry is deleted rather than softened") and because N2 has no falsifier at all — its test, "can one person hold a component in their head", is unobservable, while P1 and P2 one section later both name instruments (416-419). A goal with no falsifier is the defect class the prompt names, and Section 3 contains exactly one instance.
- **what counts it** — the named gate, or the words "none — owed at Section 18 item N". Earns its slot because this is the facet whose absence produced the section's worst defect: N1.d asserts it is "countable ... gateable rather than aspirational" while no instrument exists, and the contradiction is only discoverable by reading Section 18. A mandatory column forces "none" to be written in the clause's own row, where a reader comparing N1.a (grant_sweep-adjacent) with N1.d (nothing) sees the hole immediately.
- **who cites it** — generated, like the owed list: the count of Section 5 decisions and Section 9 register warrants naming this clause. Earns its slot for two reasons. First, Section 3 already asserts the property ("Every decision in Section 5 is justified against at least one of them by name", 282) and it is false today — D1 through D9 name no goal. Second, the counts are already computable and already lopsided (N3 17, N1.b 4, N1.a 2, N1.c 2, N2 1, N1.d 0); generating them turns "a goal nobody cites" from an invisible fact into a visible zero, exactly as spec-derivations.py#owed does for owed items.

**Findings.**

- `high/verified` N1.d claims it is "countable" and therefore "gateable rather than aspirational", but no instrument counts round trips — and Section 18 says so explicitly, making Sections 3/15 and Section 18 two writers disagreeing about one fact.
  - **evidence** Line 306-307: "It is also **countable** — and that is what makes it gateable rather than aspirational, because a count can ratchet." Line 5537 (Section 15, constraints carried forward): "The round-trip ratchet of Section 3 and the existing `boundary_ratchet` 213 both only go down" — `boundary_ratchet` is a real ctest (source/test/CMakeLists.txt:256), so the sentence pairs a fictional ratchet with a real one. Line 5938-5942 (Section 18): "**The round-trip ratchet has no metric and no starting ceiling.** ... *round trip* is not yet defined precisely enough to count ... Until the metric exists the ratchet is a policy with no instrument." I grepped every script for a round-trip counter: `grant-sweep.py` counts include crossings ("%d crossings remain to be removed across %d declared edges"), which is compile-time coupling, not seam round trips. Section 15's own round-trip evidence is hand-read prose ("Zero round trips, already batched", 5243; "Four of the seven ... return values", 5252-5254) and Section 15 admits "neither is visible to any existing instrument" (5247-5249).
- `high/verified` N3 row 3 — "a replay verifier | `world` authority + `transcript`, no participant" — is not wiring; it is a scene sink with no scene producer. Removing the participant removes the only component that produces `scene`.
  - **evidence** `transcript` is "records instead of drawing" (2786) and Section 18 confirms "what it records is `scene`" (5926). The scene producer is `world_view`: its boundary is "one participant's prediction of one world, **and every entity's appearance**" and its excludes row "Names `base`, `core`, `game`, `scene` and `sound`" (Section 10, ~3210-3213). The authority side does not touch it: the generated composition ledger says "**world_sim links 13 of 45 components.** Not linked: ... `scene` ... `world_view`" (2309) — and `world_sim` is exactly "one world authority, placed alone". Compounding it, Section 18 records that `transcript` owes "**The assertable account, which record mode promises and cannot deliver**" (5926), which is precisely what a *verifier* needs — so the row's answer "none" contradicts an owed item that the same document generates.
- `high/verified` N3 row 2 — "a load generator: 500 participants, no devices | `participant` × N, no `device` at all" — is contradicted by Section 8's device staircase, and its compose column omits the two components the generated graph says it needs.
  - **evidence** Section 8, line 1327: "| `client_agent` | `presentation` **only**: the contract, and no implementation of it — because nothing is assembled to send |", and line 1332: "`client_agent` reaches the contract and no implementation of it". `presentation` is INTERFACE / **DEVICE** zone (2771), so a device-less participant still enters `device/` — only `server`·`world_sim`·`world_gen` reach "nothing" (1328). The generated composition graph for the corresponding entrypoint shows direct edges `client_agent --> host_null` and `client_agent --> transport_tcp` (2460-2461) and "**client_agent links 22 of 45 components**" (2545); the row names neither host nor transport.
- `high/verified` N2 restates A3 and P1 rather than stating a goal — the exact duplication Section 4 deleted five principles to eliminate, left standing one section earlier.
  - **evidence** N2, lines 317-318: "Every duty is owned by exactly one component. Every dependency is declared, and the declaration is enforced by the build rather than by discipline." Sentence 1 is A3: "**Sovereign Composition** — one concern per module ... god objects, dual-purpose modules, \"and\"/\"also\" in a duty" (230). Sentence 2 is P1: "**Dependencies point down and are declared.** ... the build — `INCLUDE_DIRECTORIES`, so a violation is a compile error rather than a review comment" (382). Section 4's removal table says why this is a defect: "| One duty per component | **A3**, Law of One | an adopted axiom already says it, in stronger form ... Restating it here made the document, not the axiom, the authority |" (399) and "| One writer per fact | **A1** + **A2** |" (402). Sharper still: Section 3 defines a goal as abandonable — "a goal can be abandoned, and abandoning one would change the architecture" (284) — but abandoning N2 would change nothing, because A3 and P1 would still hold.
- `medium/verified` N3 row 1 is stale after transport was added today: "one `world` authority + `net`, placed alone" describes a shard nothing can connect to, because `net` is a vocabulary and packets cross via `transport`.
  - **evidence** Row at line 342. `net` is "VOCABULARY | DOMAIN | what a replicated field is" (2768) — it defines replicated fields, it does not carry them. Carrying is `transport`: "INTERFACE | MACHINE | how packets cross" warranted by **N1.b** (2763), with backends `transport_local` (2764) and `transport_tcp` (2765). The register's own convention is visible in the generated ledgers: `server` ("hosts a universe for remote players") links `transport` and `transport_tcp` — only `transport_local` is in its not-linked list (2414) — while `world_sim` links none of the three (2309). A row describing a remotely-reachable shard that names `net` and no transport was written before `transport` existed and was not revisited.
- `medium/verified` N2 is the one rule in the section with no falsifier and no instrument, and its own test is unobservable — while the adjacent Section 4 principles both name instruments.
  - **evidence** N2's test, lines 321-322: "**can one person hold a component in their head, change it, and know what they have not broken?**" No observation, no counter, no gate. Contrast N1, where every clause carries a "how you would know it failed" column (297-302), and contrast Section 4's instrument table one page later: "| **P1** | `grant_sweep`, `render_layering` | ... | **P2** | `spec_consistency`'s `PLACEMENT` verdict |" (416-419). N2's title also fails A3's own test by naming two properties — "A sovereign, comprehensible engine" — which have different falsifiers: sovereignty is countable (implements-arity, duty strings), comprehensibility is not.
- `medium/verified` The section's opening rule — every Section 5 decision is justified against a goal by name — is false, and nothing counts it.
  - **evidence** Line 282-283: "Every decision in Section 5 is justified against at least one of them by name, and a decision that serves none of them does not belong in this document." Grepping Section 5 (lines 433-709) for `**N1**`/`**N2**`/`**N3**` returns exactly four hits, all "serves" rows in the D10-D13 derivations. D1-D8 are single table rows with no goal citation (437-451), and D9's derivation (562-583) is prose with no "serves" row at all, unlike the four-facet tables D10-D13 use. So 9 of 13 decisions name no goal. The gate that could catch it does not look here: spec-consistency.py:486 applies its CITES regex to component warrants only (`for name, v in sorted(comp.items())`, 487).
- `medium/verified` N1.d warrants zero of 45 components and N2 warrants one — a clause and a goal that do no work in the register they are supposed to justify.
  - **evidence** Counted over the register rows (2755-2799) by tallying bolded citations in the warrant column: N3 17, N1.b 4, N1.c 2, N1.a 2, **N2 1** (only `platform`, 2757), **N1.d 0**. N1.d appears in exactly one derivation and only as a falsifier, not a warrant: "One value per step, one direction, or the boundary is misplaced (N1.d)" (3138). The section calls N1.d "a stronger test than 'does it compile without the other side'" (304-305), yet nothing in the model is justified by it.
- `medium/verified` The gate's citable vocabulary knows three N1 clauses; the spec defines four. N1.d passes only by regex accident, and a warrant citing a nonexistent N1.e would pass identically.
  - **evidence** scripts/spec-consistency.py:483 comment: "# N1-N3 the north star, optionally .a/.b/.c" and line 486: `CITES = re.compile(r'\*\*(A(?:[0-9]|1[0-4])|F[1-4]|N[1-3](?:\.[abc])?|P[1-2]|D(?:[1-9]|1[0-3]))\b')`. `**N1.d**` matches only because the optional group fails and `N1` alone satisfies the trailing `\b` at the dot — so the gate cannot distinguish N1.d from N1, and cannot reject an invented clause. This is the same failure the file's own comment confesses for axioms: "This regex accepted A1-A6 throughout, so every warrant citing one of the five stayed green while naming something that had ceased to exist ... A gate whose vocabulary outlives the document's is worse than no gate: it certifies the stale" (476-479). (Adjacent, outside my section: the error message at 499 still says "P1-P7" while the regex allows `P[1-2]` and Section 4 has two principles.)
- `medium/verified` "new components needed: none" is false by the document's own taxonomy for three of the five rows, because those aggregates are already declared ENTRYPOINT components inside the gated 45.
  - **evidence** Row 1 is `world_sim`: "ENTRYPOINT | COMPOSITION | ticks one world with no participant | **N1.b** — one world placed alone" (2798). Row 2 is `client_agent`: "ENTRYPOINT | COMPOSITION | a participant with no devices" (2795). Row 4 is `world_gen`: "ENTRYPOINT | COMPOSITION | generates terrain and never ticks it" (2799). All three are register components with derivations — `python3 scripts/spec-derivations.py --check` reports "45/45 components derived, 270/270 facets", and `spec-consistency.py --check` reports "45 components". The table's own framing acknowledges the tension without resolving it ("A few, none of which is a shipped entry point today", 337-338): the honest answer is "no new LIBRARY, BACKEND or INTERFACE — one new ENTRYPOINT, which is wiring by kind", which is what Section 10 already says of `world_sim` ("Around the wiring `world` + a configured residency, and nothing else", 3613).
- `medium/likely` N3 row 4's "nothing ticking" cannot produce pixels, and its compose list omits the GPU stack the register requires for `rendering`.
  - **evidence** Row at line 345: "an offline map renderer | `worldgen` + `world_view` + `rendering`, nothing ticking | none". `rendering` is "BACKEND | DEVICE | turns a scene into pixels" (2784), and Section 8's staircase says a composition that reaches it also reaches "`presentation`, `rendering`, `gpu`, a GPU backend" (1325). The element register makes the tick mandatory: "| **`presentTick`** | TICK | DERIVED | **PROCESS** | `rendering` | `driver` | resample, camera, assemble, paint |" (4650), derived from a loop owned by a host. The row also inherits row 3's defect — `world_view` is "one participant's picture of one world" (2778) in an aggregate declared to have no participant.
- `low/likely` N3 row 2's "`participant` × N" rests on a multiplicity the register never states, so the row cannot be checked against the model.
  - **evidence** The CARDINALITY table spells the multiplicity out for worlds and not for participants: "| **WORLD** | **a resident world — N per universe** | `worldLoop`, `worldTick` |" versus "| **PARTICIPANT** | a participant | `clientLoop`, `clientTick`, `fixedTick` |" (4574-4576). The surrounding prose explains why WORLD was made explicit — "It was added after the Director asked whether N worlds are N instances of the one `world` component ... 'One per resident world' lived in a note, as prose, where no gate could read it" (4579-4582) — which is exactly the state N participants-per-process is in now.
- `low/verified` Four passages narrate the document's own drafting rather than stating the design.
  - **evidence** Line 312-313: "**N1 is therefore an engineering problem rather than a research one** — a considerably stronger position than this document could claim before those two were checked." Line 293-295: "they are labelled because the rest of the document cites them individually." Line 365: "That case is the reason the presentation seam is where this design started." Line 337-338: "A few, none of which is a shipped entry point today." Each states a fact about the writing process, not about the architecture; the design content survives their deletion.
- `low/verified` The closing "hardest placement" passage is a fifth thing under N1 with no label, so it cannot be cited, and it adds nothing the four clauses do not already say.
  - **evidence** Lines 359-363: "**The hardest placement N1 must survive** ... Everything crossing that line is subject to all four clauses at once — values only (N1.a), a nameable unit on each side (N1.b), a co-located path that is the same contract (N1.c), and few enough crossings per frame to be worth doing (N1.d)." It is the section's only worked example and is genuinely load-bearing for the presentation seam, but it sits outside the clause table, carries no identifier, and re-enumerates the four columns instead of adding a testable fifth. Under a levelled shape it becomes either a labelled clause or a worked example attached to N1's row.


## §6. Requirements and given constraints

**What the section is.** Section 6 (lines 709-813 of TSSA.md = /root/frackin/OpenStarbound/docs/superpowers/specs/2026-08-01-target-state-system-architecture.md) is the only normative table-bearing section in Part I whose rows carry no falsifier and no instrument, and it is read by zero gates: I grepped scripts/spec-model.py, spec-consistency.py, spec-measures.py and prose-claims.py for any Section-6 reader and found none, while the doc's own bar at TSSA.md:42 is "every rule lands in a register or a table where an instrument can read it, and nothing normative lives in prose alone" and A2's stated violation at TSSA.md:236 is "a rule that exists in prose and nowhere a machine can read". Three defects are of the exact classes named in the brief: an instrument named that cannot make the measurement asked of it (752-753), a duty claimed to be "named in the register" when the register's 45 components contain no such duty (811; verified by executing spec-model.components()), and an acceptance test whose second assertion is passed by the empty loop the paragraph above it says it exists to exclude (767 vs 761-762). One thing to note as a positive control: the section's one routed gap — the missing system-world element — IS really recorded against `universe` in the generated owed ledger (742 -> 5927), so the pattern the rest of the section needs already exists two lines away from where it is missing. I could not check whether run-gates.sh passes today (it reads .github/workflows/gates.yml and executes builds; I did not run it, per read-only).

**The repeating unit.** The repeating unit is a NORMATIVE ROW — one figure the design must meet, one fact it had no freedom over, or one obligation it must expose. There are 17 today, in five sub-tables with five different column shapes: 4 budget rows (TSSA.md:726-729, columns value/why/owner), 4 owed figures (752-755, columns figure/why-owed/who-can-close), 5 constraints (781-785, columns name/constraint/why-it-is-one), 3 observability obligations (807-809, columns requires/forbids), and 1 acceptance test (765-767) which is a blockquote with no shape at all. No two shapes share a column set, so a reader cannot compare a budget with a constraint, and a missing answer is invisible rather than a hole: nothing in the section's own layout can show that the 60 Hz row has no falsifier while the throughput row does.

**Already at depth.** ["The rates-are-CHOICES argument (TSSA.md:717-722), including its two-part refutation — 'the source figure is a *mutable process-global* rather than a constant' and 'a fact nobody chose is a fact nobody may change'. This is the D7 discipline doing real work and it is the strongest paragraph in the section.", "The per-instance statement at TSSA.md:731-734: 'Both rates are per-instance construction parameters, not globals ... there is no process-global to write', with the explanation that this is why the owner column exists at all. It is corroborated downstream at TSSA.md:3194 and TSSA.md:5171.", "The split of the owed figures by WHY they are owed (TSSA.md:744-755): 'not yet measured' vs 'not yet measurable', with different closers. Explicitly a correction of a draft that gave one excuse for four rows, and it is the right distinction — keep the two-class structure even when the instrument column is fixed.", "'A fact about the current directory tree is never a constraint — that is inertia wearing a constraint's hat' (TSSA.md:775-777). This is the section's contribution to D7 and nothing else in the document states it.", "The OBJECT-libraries row and its consequence (TSSA.md:785-790): 'every object of a library enters every consumer; there is no per-object pruning' therefore 'a component is enforceable if and only if it is its own directory'. This is the one constraint that shapes the model, it is correctly flagged as such, and it is load-bearing for the whole register.", "The routing of the system-world gap (TSSA.md:739-742) — I verified it lands at TSSA.md:5927 in the generated ledger. This is the section's own worked example of what every other owed row should look like; do not flatten it while levelling.", 'The ratio framing of the throughput budget and the naming of the empty-loop hazard (TSSA.md:761-763). The diagnosis is right even though the test written under it does not yet discharge it.', "Observability stated as an architectural obligation rather than a feature request, with the P1 argument at TSSA.md:799-803: 'An architecture whose own diagnosis requires a boundary violation cannot be diagnosed in the target state'. The three requires/forbids pairs at TSSA.md:807-809 are the right three; what they lack is an owner, not content."]

**Proposed facet shape.**

- **the rule** — the value or the prohibition, stated so it can be quoted in one clause. Earns its slot because it is the unit's identity and is the only facet all five current shapes already carry; making it column 1 everywhere is what lets a budget and a constraint sit in one reading order.
- **warrant, by ID** — the A/F/N/D it descends from, named by identifier not by paraphrase. Earns its slot because the register demands exactly this descent of every component (TSSA.md:36 'A boundary that cites none of them is not a boundary, it is a preference') while Section 6's rows give prose reasons with no ID, so nobody can tell that the 60 Hz row leans on 'vanilla compatibility' in a sense wider than the constraint row at 782 defines it.
- **owner** — a name that exists in the register or the generated drive table (a component or an element), never a role noun. Earns its slot because it is the facet this section already does best and the two places it slips are both real defects: 'the device' at 729 contradicts Section 11's 'the host this process happens to have' (4365), and 'the composition under test' at 729 names no component when `world_sim` was available.
- **falsifier** — the observation that would show the row false. Earns its slot because the document already ships this column one section earlier (N1's 'how you would know it failed', TSSA.md:297) and gates it per component in Section 10 ('falsified'), so Section 6 is the outlier rather than the innovator; three of four budget rows and all five constraints have none today.
- **instrument, or the ledger the gap is owed to** — the script or generated block that reads this row, or an explicit 'none — recorded against X in Section 18'. Earns its slot because it is the single facet that would have caught all three headline defects: it forces 752-753 to name a runtime instrument instead of a static-anchoring class, it forces 807-809 to name a component or admit there is none, and it forces the four owed figures into a ledger the ratification rule actually reads.

**Findings.**

- `high/verified` The acceptance test's second assertion is satisfied by the empty loop the sentence three lines above says it exists to exclude. The test asserts (a) wall-clock <= N and (b) 'same start state, same tick count, same end state on a re-run' — both of which an empty loop passes trivially (its end state equals its start state, deterministically). Nothing in the written test asserts that any FU machine changed anything.
  - **evidence** TSSA.md:761-762 'A test that asserts only *the simulation advanced* is not a test of the budget above it — an empty loop advances.' then TSSA.md:767 '> Assert the machines advanced — same start state, same tick count, same end state on a re-run.' and TSSA.md:769-770 'the machines-advanced assertion is what makes it about *Starbound* rather than about an empty loop.' The em-dash operationalises 'machines advanced' as re-run determinism, which is a different property. Section 10's original form (TSSA.md:3662 'assert the machines advance') did assert progress; the sharpening lost it.
- `high/verified` Two owed figures name an instrument class that cannot make the measurement. 'An anchoring instrument, per Section 15' is pointed at two runtime, against-a-running-server measurements, but Section 15 defines anchoring as a static document-vs-tree check and every anchoring instrument it lists is an include-closure/parse gate.
  - **evidence** TSSA.md:752-753 '| worlds resident per universe | ... | an anchoring instrument, per Section 15 |' against TSSA.md:5266 '| **anchoring** | where a target name covers files that exist today, the grant row matches a measured *transitive* include closure |' and the anchoring rows at TSSA.md:5441-5455 (`grant_sweep`, `loop_inventory`, `spec_consistency`, `composition_graphs`, `link_sweep`, `dedup_measure`). None reads a running process. The repo does own a runtime instrument — /root/frackin/OpenStarbound/scripts/telemetry-window.py, 'Difference two cumulative telemetry snapshots into a per-frame cost table' — and it is not named here; the doc's own number-ownership rule is TSSA.md:5394 'Measured from the tree by `scripts/spec-measures.py`, which owns exactly the figures a design decision rests on.'
- `high/verified` The observability subsection asserts that an instrument is a register component with a named owner. No such component exists. I executed scripts/spec-model.py's components() over the spec: 45 components, and not one has a telemetry, tracing or observability duty. The generated tree-map puts the instruments in `scripts/`, a directory the generator explicitly declares NON_COMPONENT.
  - **evidence** TSSA.md:800-801 'an instrument is a component, it appears in the register, and it may not be granted something its subject is not' and TSSA.md:811 'The duty and its owner are named in the register'. Verified against the parsed register: closest duties are `transcript` 'records instead of drawing' and `net` 'what a replicated field is'; nothing owns a trace identity. /root/frackin/OpenStarbound/scripts/tree-map.py:44-49 lists ('scripts/', 'the instruments -- every gate in Section 6 lives here') under NON_COMPONENT, injected at TSSA.md:2867. So all three obligations at TSSA.md:807-809 bind nobody, and the promised identity that 'survives a seam crossing' has no owner to build it.
- `high/verified` The four owed figures are recorded in no ledger the ratification rule reads, so the section's own sealing condition is enforced by nothing. Section 18 holds the two lists ratification checks; the four figures appear in neither, and the strings occur nowhere else in the document.
  - **evidence** TSSA.md:757-759 'A ratified section carries zero owed rows ... until both are closed this section is not sealed' against TSSA.md:5906 '**Ratification requires both to be empty**' where 'both' = the generated per-component owes ledger (TSSA.md:5911-5929, 11 of 45 components) and the 5-item hand-authored design-owes list (TSSA.md:5932-5960). grep for 'worlds resident', 'participants per universe', 'split-path' returns only TSSA.md:752-755. Contrast the positive control at TSSA.md:742 ('recorded against `universe` in the generated owed ledger, where the ratification rule can see it') which I confirmed lands at TSSA.md:5927.
- `medium/verified` Three of the four budget rows have an owner and no falsifier. The section opens by promising the figures 'exist to be measured against', but only headless throughput states an observation that could show it wrong; 60 Hz, 20 Hz and the frame row state reasons, not tests. The falsifier column already exists in the document one section earlier, so this is an omission rather than an unavailable shape.
  - **evidence** TSSA.md:713 'the figures below exist to be measured against, and a row with no number is marked as owing one' — but the table at TSSA.md:724-729 has columns 'budget | why this value | owner' only. Compare TSSA.md:297 '| | the clause | what it forbids | how you would know it failed |' (N1) and the gated `falsified` facet of all 45 derivations (TSSA.md:3005 'a component with no falsifier is decoration — the instrument rule, one level down'). Nothing states what observation would show 60 Hz or 20 Hz wrong.
- `medium/verified` Two writers, one fact, disagreeing on who owns the frame clock. Section 6 assigns it to 'the device'; Section 11 assigns it to the host and reserves 'the device' for the one EXTERNAL clock (audio) — a distinction Section 11 makes load-bearing.
  - **evidence** TSSA.md:728 '| **frame** | the device's rate, not ours | ... the presentation clock is owned by the device and is not a simulation budget at all | the device |' against TSSA.md:4365 '| **driver** | the host this process happens to have | DISPLAY, or FREE when headless | **process** |', TSSA.md:4337 '**Presentation never owns a clock.** Its cadence always comes from the driver in its process', and TSSA.md:4369 '| *audio* | **not ours** — the device, via `audio_sdl` @ 44100 Hz | EXTERNAL |'. A reader taking Section 6's owner column literally looks for a device-owned presentation clock that Section 11 says does not exist.
- `medium/verified` The two rates are owned by no generator and appear in no field an instrument reads, then are restated in prose in two further sections. The generated drive table has a cadence column whose values are FIXED/FREE/DISPLAY/EXTERNAL/DERIVED and no rate, so nothing can catch a drift between the three statements.
  - **evidence** TSSA.md:726-727 ('60 Hz — 16.67 ms', '20 Hz — 50 ms') restated at TSSA.md:4354 'fixed 60 Hz' and TSSA.md:5088/5091 'one world step (16.67 ms)' / 'one system step (50 ms)'. The generated drive table header is TSSA.md:4692 '| element | kind | cadence | driven by | how | receives | duty |' — `worldLoop` is FIXED with no number. TSSA.md:5394 concedes 'Every other number in this document is prose and is the author's to keep true.' Against the section's own bar at TSSA.md:42 and A2's violation at TSSA.md:236.
- `medium/verified` The acceptance test is not buildable from what is on the page: N has no value, no reference machine or build configuration is named, 'a world containing Frackin Universe automation' names no asset that exists in this tree (FU is a third-party mod; the only 'frackin' hits in the repo are path strings in render-gate.sh / render-motion.sh / deploy-install.sh), and 'same end state' requires the state hash of Section 15's composition oracle, which is designed and not built.
  - **evidence** TSSA.md:765-767 for the test text; TSSA.md:5404-5409 for the oracle's runs A-D ('hash the world state every step'), listed as '**designed, not built**' at TSSA.md:5446-ish in the instrument table. It is also stated three times with three different assertion sets — TSSA.md:761-771, TSSA.md:3661-3662, TSSA.md:5430-5433 — and the Section 15 copy credits Section 10 ('Section 10 already states it'), i.e. the weaker form that Section 6 opens by calling insufficient. Three writers, one fact.
- `medium/verified` The 60 Hz warrant leans on a constraint wider than the constraint row defines. The budget justifies 60 Hz by vanilla compatibility; the constraint row scopes vanilla compatibility to things loading, which no tick rate can break.
  - **evidence** TSSA.md:726 '60 Hz is chosen to match the rate the content was authored and balanced against, so vanilla compatibility survives' against TSSA.md:782 '| **vanilla compatibility** | assets, protocol and saves keep loading | the players exist and their saves are real |'. Either the constraint must say behaviour as well as loading, or the budget must cite something else.
- `medium/verified` A constraint row is three constraints behind one name, which is the shape the document gates against elsewhere. 'Assets, protocol and saves keep loading' has three different falsifiers (an asset that will not parse, a protocol version bump, a save that will not migrate) and one row.
  - **evidence** TSSA.md:782 as quoted, against A3's stated test at TSSA.md:231 ''and'/'also' in a duty' and TSSA.md:401 'an adopted axiom already says it, in stronger form'. The register records exactly this defect for `platform_pc` ('Steam, Discord and P2P services — three things behind one name, and the plainest Law-of-One violation in the register', TSSA.md:5923) and for `celestial` and `storage`; constraints get no equivalent check.
- `medium/verified` The owner of the throughput budget is a role noun, not a name. Every other owner cell names a thing that can be pointed at ('each world', 'each system-world', 'the device'); this one names whatever is being tested, so the row cannot be assigned.
  - **evidence** TSSA.md:729 '| **headless throughput** | ... | the composition under test |'. The register offers two nameable owners: `world_sim` ENTRYPOINT 'ticks one world with no participant' and `client_headless` ENTRYPOINT — and Section 10's `world_sim` derivation already claims the test (TSSA.md:3661).
- `low/verified` A generator injects a stale cross-reference that points readers at this section for the gate catalogue. The gates are catalogued in Section 15 ('The full instrument set'); Section 6 contains no instrument table at all. Because the string is hard-coded in the generator, it is re-injected on every run and the freshness gate keeps it green.
  - **evidence** /root/frackin/OpenStarbound/scripts/tree-map.py:47 ('scripts/', 'the instruments -- every gate in Section 6 lives here') producing TSSA.md:2867 'scripts/ # the instruments -- every gate in Section 6 lives here'. The catalogue is at TSSA.md:5437-5455. Likely a survivor of the TOC renumber recorded in task #206 (TSSA-1).
- `low/verified` Drafting-history narration inside the section, unfenced. The document has a convention for this — HTML HISTORICAL markers — used three times elsewhere, including for a structurally identical 'the numbers had all drifted' confession in Section 15.
  - **evidence** TSSA.md:746-748 'A previous draft called four figures owed and gave one excuse for all of them. The excuse was wrong for half' — no marker. The convention appears at TSSA.md:1347/1352, 3908/3913 and 5273/5279, e.g. '<!-- HISTORICAL -->' wrapping '**Every number in that table was hand-written, and every one had drifted**'. Also TSSA.md:757-759 ('Marking a gap is honest; *shipping* the mark is not') is authoring process rather than design.


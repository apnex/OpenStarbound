# TSSA audit — Section 7, stale vocabulary, rule purity, tables (workflow wdk04ei07)

107 agents: 6 read-only finder lenses, 101 adversarial verifiers. 103 findings raised,
**57 refuted, 44 upheld**. Only the upheld are listed. An entry is still a CLAIM — three were
verified by hand and fixed in `ed5e7125`; the rest are unverified by me.

**Already fixed in ed5e7125:** Section 7's KIND table (all six kinds, machine-read, gated by
`KIND_RULE`), the metered-size exemplar moved to Section 17, `KINDWORD` derived from
`spec-model.KINDS`, and the 41→45 arithmetic in the zone table, five composition histories,
Section 15's derivation state and Section 16's R-claim.


## s7-kinds (11 upheld)

- `high` **L970** Section 7's KIND table — the table that says which rule governs each kind — still has a CONTRACT row and has NO INTERFACE row and NO VOCABULARY row. spec-model.py:47 declares KINDS = (FOUNDATION, INTERFACE, VOCABULARY, BACKEND, LIBRARY, ENTRYPOINT) and would hard-fail on a register row saying CONTRACT; the mermaid diagram 200 lines below labels 8 nodes INTERFACE and 3 VOCABULARY. So 2 of the 6 live kinds have no rule in Section 7 at all, and the only kind whose row states what its .cpp may contain and what it may name is the retired one.
  - **fix** Replace the CONTRACT row with two rows. INTERFACE: 'declares an interface others implement; may name only foundation types and other interfaces. Any `.cpp` holds value-type constructors and trivial defaults only — never a backend's behaviour — and its size is metered: `platform` 0, `host` 41, `gpu` 89 lines, ratcheting down. It is the only legal target of `==>`.' VOCABULARY: 'declares a shared type language everybody names and NOBODY implements — nothing implements `Drawable`. May name only foundation types; may never be the target of `==>` or of a `--o`; has no backend, so no arity rule applies to it.'
  - **verifier corrected to** Line 984 (the finding cites 970; the quote text itself is verbatim). Section 7's normative KIND rule table at lines 981-987 — introduced at 979-980 with "The kind says what sort of thing it is, and therefore which rule governs it" — was not updated by the 2026-08-02 CONTRACT split. It still carries a **CONTRACT** row and has no INTERFACE row and no VOCABULARY row, five rows for six live kinds. spec-model.py:47 declares KINDS = (FOUNDATION, INTERFACE, VOCABULARY, BACKEND, LIBRARY, ENTRYPOINT) and line 108 raises SystemExit on anything outside it; running spec-model.components() over the spec returns 45 components as INTERFACE 8, VOCABULARY 3, BACKEND 12, LIBRARY 13, ENTRYPOINT 7, FOUNDATION 2 — so 11 live components are governed by a table row labelled with a kind the reader can no longer find, while the mermaid diagram 209 lines below (1193-1241) labels those same 11 nodes INTERFACE and VOCABULARY. In substance the CONTRACT row is the INTERFACE rule under a retired name (its meter list "platform 0, host 41, gpu 89" names three INTERFACEs and zero VOCABULARYs); the sharper defect is that VOCABULARY has no rule anywhere in the 6000-line spec — nothing matches "implementable", "nothing implements", or "shared type language", and the whole rationale ("Only an INTERFACE can be implemented. A VOCABULARY declares types everybody uses and nobody implements: nothing implements Drawable") survives only as a source comment at scripts/spec-model.py:98-107, never carried into the document. Two claims in the original finding are slightly strong: CONTRACT is not the only row stating what a kind may name (FOUNDATION "names nothing above itself", BACKEND "named only by an ENTRYPOINT", ENTRYPOINT "the only place a backend may be named") — it is uniquely the only row stating what its .cpp may contain. Fix as proposed: replace the CONTRACT row with an INTERFACE row (carrying the existing may-name-only-foundations clause, the .cpp value-type-constructors-only clause, the metered line budget, and "the only legal target of ==>") and a VOCABULARY row (declares a shared type language everybody names and nobody implements; may name only foundation types; may never be the target of ==> or --o; has no backend, so no arity rule applies). Note the same staleness recurs in the arrow rule at line 1096 ("it points at a CONTRACT") and in the ZONE prose at 1004 and 1464, which are separate instances of the ~24 remaining CONTRACT uses.
- `high` **L1445** The zone table's n column is stale for machine/: the register has 14 MACHINE components (base, content, core, host, host_null, host_sdl, platform, platform_null, platform_pc, script, storage, transport, transport_local, transport_tcp), and the GENERATED tree at 'source/ machine/ # 14 components' says 14. The other three rows (14/9/8) are right, so the table sums to 41 while the same section says 45 twice ('every one of the 45 components', 'zero exceptions across 45 components').
  - **fix** 10 -> 14. The four new MACHINE components since the count was written are `platform_null`, `transport`, `transport_local`, `transport_tcp`.
  - **verifier corrected to** docs/superpowers/specs/2026-08-01-target-state-system-architecture.md line 1459 (not 1445): "| **`machine/`** | the OS, the vendor, the asset store, the disk | 10 |". The n column for machine/ is stale. spec-model.py reports 45 components with MACHINE=14 (base, content, core, host, host_null, host_sdl, platform, platform_null, platform_pc, script, storage, transport, transport_local, transport_tcp), and the GENERATED tree says the same at line 2926 ("machine/ # 14 components"). The other three rows are correct and match the tree (domain/ 14 at 2941, device/ 9 at 2956, composition/ 8 at 2966), so the table sums to 41 while line 1476 in the same subsection states "zero exceptions across 45 components". Fix: 10 -> 14. Cause: the four MACHINE components added since the count was written are platform_null, transport, transport_local, transport_tcp. Note the finding's supporting quote "every one of the 45 components" is at line 957, a different section; the in-subsection second mention of 45 is line 1451, inside a HISTORICAL block. Related but separate staleness: line 2916 calls the register "a 41-row table".
- `high` **L990** The worked example that justifies ZONE as a separate axis is false against the register on both halves of its first clause: `scene`'s register kind is VOCABULARY, not CONTRACT, and CONTRACT is not a kind any more. As written it is exactly the conflation the split was made to prevent — it puts an implementable interface and a non-implementable vocabulary under one word to argue they share a kind and differ only by zone.
  - **fix** Rewrite with two same-kind components in different zones, e.g. '`platform` is an INTERFACE in `machine/` while `celestial` is an INTERFACE in `domain/`, and `host_sdl` is a BACKEND in `machine/` while `rendering` is a BACKEND in `device/`.'
  - **verifier corrected to** Line 1004 (quote is exact; the finding's "line 990" is drift): "`platform` is a CONTRACT in `machine/` while `scene` is a CONTRACT in `domain/`, and `host_sdl` is a BACKEND in `machine/` while `rendering` is a BACKEND in `device/`." Two distinct defects in the first clause, both verified: 1. CONTRACT is not a KIND. /root/frackin/OpenStarbound/scripts/spec-model.py:47 — `KINDS = ("FOUNDATION", "INTERFACE", "VOCABULARY", "BACKEND", "LIBRARY", "ENTRYPOINT")`. spec-model.py:108 raises SystemExit for any register kind outside that tuple, so "CONTRACT" is a token the one table reader would reject. 2. The pair no longer exhibits one kind in two zones, which is the only thing the sentence is there to do. Register (TABLE: components, lines 2863-2911): - 2868: `| **`platform`** | INTERFACE | MACHINE | ...` - 2877: `| **`scene`** | VOCABULARY | DOMAIN | ...` Even after mechanically substituting CONTRACT -> INTERFACE, half two stays wrong: `scene` is VOCABULARY. Post-substitution the sentence reads "an INTERFACE in machine/ ... an INTERFACE in domain/" while the register says one is a VOCABULARY — so this is not a pure find-and-replace fix, it needs a different component. Scope correction to the finding (it is right to confine itself to the first clause): the second clause is accurate and still does the job on its own — 2870 `| **`host_sdl`** | BACKEND | MACHINE |`, 2895 `| **`rendering`** | BACKEND | DEVICE |`. Minor framing overstatement, not enough to refute: the sentence is an illustration of "Zone is not a synonym for kind" (line 1003), while the actual justification of the axis is the measurement that follows at 1005-1007 ("only 8 of 45 components deviate from their kind's default ... the axis earns its place on those eight"). So the axis does not fall over; the worked example does. The proposed fix is correct as stated: 2887 `| **`celestial`** | INTERFACE | DOMAIN |` makes `platform`/`celestial` a genuine one-kind-two-zones pair. ROOT CAUSE THE FINDING MISSES, and fixing line 1004 alone leaves the document incoherent: the KIND table 20 lines above is itself stale. Line 984 reads `| **CONTRACT** | declares an interface and may name only foundation types. ...` — the table at 981-987 lists FOUNDATION / CONTRACT / BACKEND / LIBRARY / ENTRYPOINT and contains no INTERFACE row and no VOCABULARY row at all. Line 1004 is locally consistent with that table; it is the table that contradicts the register. Any repair of 1004 that leaves 984 alone introduces INTERFACE and VOCABULARY into prose that the governing taxonomy table two paragraphs earlier never defines. The spec-model.py comment at 98-107 is the missing content: "Only an INTERFACE can be implemented. A VOCABULARY declares types everybody uses and nobody implements." Confidence: high on both defects (register rows, KINDS tuple, and KIND table all read directly).
- `medium` **L1082** The implements-arrow rule still names the retired kind, and after the split it is not merely a rename: it must exclude VOCABULARY. A VOCABULARY is not implementable, so `X ==> scene` must be illegal — spec-model.py:98-107 says exactly this is what the split exists to prevent ('IMPLEMENTS_ARITY would have accepted `X ==> scene`'). Every `==>` target in the diagram is in fact an INTERFACE (host, platform, transport, celestial, presentation, audio, gpu, content).
  - **fix** '...and it points at an INTERFACE. Never at a VOCABULARY: nothing implements a shared type language.'
  - **verifier corrected to** Line 1096 (not 1082) states the implements-arrow rule as "**every BACKEND has exactly one**, and it points at a CONTRACT". CONTRACT is a retired kind — spec-model.py:47 lists KINDS as FOUNDATION, INTERFACE, VOCABULARY, BACKEND, LIBRARY, ENTRYPOINT — and the spec's own line 989 requires kinds to be greppable uppercase tokens, so the rule cites a token that matches nothing. The correct target is INTERFACE only: spec-consistency.py:526-530 already enforces this ("`%s ==> %s` implements a %s; `==>` may only point at an INTERFACE -- a VOCABULARY has nothing to implement"), and spec-model.py:98-107 records that a single CONTRACT kind "would have accepted `X ==> scene`". All eight `==>` targets in the compile diagram (host, platform, transport, celestial, presentation, content, audio, gpu) are INTERFACE; net/scene/sound (VOCABULARY, lines 1214-1216) are never targets. Fix: change "it points at a CONTRACT" to "it points at an INTERFACE — never a VOCABULARY: nothing implements a shared type language." The same stale clause must also be fixed at line 1160 ("BACKEND has exactly one `==>` and that it points at a CONTRACT"), and the KIND table at lines 981-987 still defines **CONTRACT** with no INTERFACE or VOCABULARY row at all.
- `medium` **L1146** The prose describing the IMPLEMENTS_ARITY verdict still states the target as a CONTRACT. The gate it describes (scripts/spec-consistency.py, KINDS tuple at line 61) knows only INTERFACE and VOCABULARY, so the document describes its own instrument in a vocabulary the instrument rejects.
  - **fix** '...has exactly one `==>` and that it points at an INTERFACE.'
  - **verifier corrected to** Line 1160 (not 1146): "BACKEND has exactly one `==>` and that it points at a CONTRACT" misdescribes the gate it names. spec-consistency.py:520-530 checks two clauses — arity (`len(got) != 1`) and target kind (`!= "INTERFACE"`, error text: "`==>` may only point at an INTERFACE -- a VOCABULARY has nothing to implement"). The prose gets clause 1 right and clause 2 wrong: CONTRACT is not in KINDS (spec-consistency.py:61, spec-model.py:47) and spec-model.py:108 would reject it in a kind cell; worse, read as the pre-split umbrella it would admit VOCABULARY, the exact case clause 2 exists to reject. Fix as proposed: "...has exactly one `==>` and that it points at an INTERFACE." Two further instances should be fixed in the same pass, since fixing 1160 alone leaves the section self-contradictory: line 1096, the arrow table 64 lines above, states the same rule the same wrong way ("**every BACKEND has exactly one**, and it points at a CONTRACT"); and line 984 is the root cause — the "| KIND | the rule it carries |" definition table still lists **CONTRACT** as a kind beside FOUNDATION/BACKEND/LIBRARY/ENTRYPOINT and has no INTERFACE or VOCABULARY row, so the document still formally defines the very kind its register and gates no longer accept.
- `medium` **L971** The rule (restated at line 973, 'ENTRYPOINT | an executable; the only place a backend may be named') is falsified three times by the grant table it governs: `colocation` (LIBRARY) is granted `transport_local` (line 3065), `host_sdl` (BACKEND) is granted `platform_pc` (line 3034) and `host_null` is granted `platform_null` (line 3035). The colocation one arrived with the new transport components; the section states the rule absolutely and records no exception, and no gate checks it.
  - **fix** State the rule with its actual boundary, e.g. 'BACKEND: implements an INTERFACE; interchangeable with its siblings; selected by an ENTRYPOINT. The three exceptions are declared: a host backend names its vendor backend (`host_sdl` -> `platform_pc`, `host_null` -> `platform_null`) and `colocation` names `transport_local` because the co-located pair IS its subject.' Amend line 973 to match, and add a spec-consistency verdict so the exception list cannot grow silently.
  - **verifier corrected to** DEFECT (confirmed, medium): the KIND table states the backend-naming rule absolutely and its own grant table contradicts it three times, with nothing checking. Rule, L985: "| **BACKEND** | implements a contract; interchangeable with its siblings; named only by an ENTRYPOINT |", restated L987: "| **ENTRYPOINT** | an executable; the only place a backend may be named |". Under the spec's own vocabulary "names" = "is on its grant list" (L1121: "`A --> B` | **A includes B** — B is on A's grant list"). The three non-ENTRYPOINT rows that name a BACKEND (exhaustive — I parsed both machine-read tables; there are exactly three): - L3048 `host_sdl` (BACKEND) -> `platform_pc` (BACKEND) - L3049 `host_null` (BACKEND) -> `platform_null` (BACKEND) - L3079 `colocation` (LIBRARY) -> `transport_local` (BACKEND) Two corrections to the filed version: 1. "records no exception" holds for Section 7 only. Section 10 already justifies two of the three: L3479 (`host_null` excludes) "It names the null vendor backend for the same reason `host_sdl` names the real one: **a host is what hands a composition its platform services**, and handing over nothing is still handing over", and L3501 (`platform_pc` excludes) "Named only by `host_sdl`, which is what makes it omissible: one consumer, one line to cut". Only `colocation` -> `transport_local` is recorded (L3973) with no reason offered against the rule. So the gap is a rule stated wider than its own derivations, not an undocumented grant. 2. The proposed fix — an inline three-item exception list inside the KIND summary row — conflicts with the spec's own L3109 "one fact gets one writer"; the derivations are already the writer for two of the three. The minimal correct repair is to narrow the rule text at L985/L987 (e.g. "named only by an ENTRYPOINT, or by the backend whose own duty is to hand that service over") plus one sentence in `colocation`'s excludes facet, and a spec_consistency check that enumerates non-ENTRYPOINT grants on BACKENDs against a declared list so the set cannot grow silently. The finding also understates one consequence worth recording: because `host_sdl` compile-names `platform_pc`, the *host* fixes the vendor backend rather than the entrypoint, so no composition can pair `host_sdl` with `platform_null`. That bears directly on L3502 "**falsified** | If a composition without vendor services fails to build" and on `host`'s register warrant "a composition picks its host; SDL and null are peers".
- `medium` **L992** This measurement cannot be reproduced from the current register under any definition of 'kind's default'. Taking each kind's majority zone the deviations are INTERFACE 4 + BACKEND 6 + LIBRARY 3 = 13 of 45 (71%, not 80%); taking the intuitive defaults (BACKEND->DEVICE, LIBRARY->DOMAIN, interface placed by what it abstracts) it is 9. Worse, BACKEND is now split exactly 6 MACHINE / 6 DEVICE, so the kind has no majority default left for the sentence to appeal to. The same figure is repeated at line 1437 inside the HISTORICAL block.
  - **fix** Recompute against the register and state the definition of 'default' being used, or drop the numeral: e.g. 'ZONE is ~71% determined by KIND — 13 of 45 components sit outside their kind's majority zone, and BACKEND now has no majority at all (6 MACHINE, 6 DEVICE), which is the strongest form of the argument that the axis is not redundant.'
  - **verifier corrected to** Lines 1005-1007 (live prose, present tense): "**ZONE is 80% determined by KIND — only 8 of 45 components deviate from their kind's default**, and the axis earns its place on those eight." Both numerals are stale and unreachable. Recomputed from the components register via scripts/spec-model.py, taking each kind's majority zone as its default: INTERFACE 4 + BACKEND 6 + LIBRARY 3 = 13 of 45 deviations, so ZONE is 71.1% determined by KIND. 13 is the MINIMUM over every possible assignment of one default zone per kind, so no definition of "kind's default" yields 8 (or 9). BACKEND is now split exactly 6 MACHINE / 6 DEVICE and has no majority default at all. Cause: the four components added on 2026-08-02 (platform_null, transport, transport_local, transport_tcp) are all MACHINE; without them the minimum is 10 of 41. The denominator was updated to 45, the numerator was not. Fix the live sentence only — for example: "ZONE is ~71% determined by KIND: 13 of 45 components sit outside their kind's majority zone, and BACKEND has no majority at all (6 MACHINE, 6 DEVICE). The axis earns its place on those thirteen." Note that the corrected figure strengthens rather than weakens the case for keeping ZONE, so the trailing clause at line 1007 — "That measurement is why the zones were cut from five to four" — needs rewording too, since the recomputed number no longer supports "mostly restating something already stated." Scope correction to the original finding: do NOT recompute line 1451. It sits inside a fenced <!-- HISTORICAL --> block in the past tense and deliberately records the measurement as it stood when five zones were cut to four; recomputing it against today's register would falsify the history. Related drift found while verifying (separate defect, same section): the zone table at lines 1456-1462 lists machine/ = 10, domain/ = 14, device/ = 9, composition/ = 8, summing to 41. The register has machine/ = 14, total 45.
- `medium` **L1122** The register gives `client_opengl` 11 grants (audio_sdl, colocation, core, frontend, gpu_opengl, host_sdl, mixing, participant, rendering, transport_tcp, windowing) — `transport_tcp` is one of the recent additions. Five matches neither the grant row nor `openglWiring`'s composition list, which names four (host_sdl + participant + rendering + gpu_opengl).
  - **fix** '`client_opengl` names eleven components and executes nothing after wiring' (or say 'names eleven and constructs four' if the wiring list is the intended figure).
  - **verifier corrected to** Line 1136: replace "names five components" with "names eleven components" — "`client_opengl` names eleven components and executes nothing after wiring." (Eleven is the grants-table figure at line 3082 and the excludes facet at line 3556; if the wiring list was intended instead, the accurate phrasing is "names eleven and constructs four", per line 4768's `openglWiring` row.)
- `medium` **L1111** `participant` has more edges than `client_opengl` on both readings: 13 grant rows vs 11 in the register, and in the transitively-reduced Section 7 diagram 12 incident edges (8 out + 4 in) vs 8. `participant` grew `transport` and `client_opengl` grew `transport_tcp`, but the ordering was already the other way.
  - **fix** 'the most edges of any ENTRYPOINT' — or name the true maximum: `participant` at 13 grants.
  - **verifier corrected to** Line 1125, in the compile-time-vs-runtime-graph table: "| `client_opengl` | the most edges of any component | **not present at all** after construction |". The superlative is false under every available metric, not merely relative to one rival. Grant rows (Section 9, lines 3081-3082): `participant` 13, `client_opengl` 11; on incident edges `core` 43, `game` 21, `participant` 17, `client_opengl` 11. Section 7 compile projection (lines 1173-1415): `client_opengl` has 8 incident edges, behind `core` 26, `base` 18, `game` 18, `universe` 12, `participant` 12 (8 out + 4 in), `colocation` 10 and `client_sdl_gpu` 9. `client_opengl` grew `transport_tcp` and `participant` grew `transport`, but the ordering predates both. The filed fix ("the most edges of any ENTRYPOINT") does NOT work either: `client_sdl_gpu` ties at 11 grants (line 3085) and draws 9 diagram edges to `client_opengl`'s 8, since `csg --> core` is drawn at line 1374 with no `cgl --> core` counterpart; closure ties as well at 35 of 45 for both (lines 2035, 2260). The cell needs a claim actually true of `client_opengl` that still carries the compile/runtime contrast - e.g. "eleven grants, and thirty-five of forty-five components in its closure" - rather than any superlative.
- `medium` **L1033** The element register has 6 LOOP elements (frameLoop, headlessLoop, clientLoop, universeLoop, superviseLoop, worldLoop) while Section 11's clock table counts 4 owned clocks plus 1 external. The identity fails twice by construction: frameLoop and headlessLoop are two implementations of the single per-process driver clock, and `superviseLoop`'s own duty string says it 'waits for shutdown; ticks nothing'.
  - **fix** State the rule so it survives the register: 'every clock this codebase owns is a `*Loop` element, but not every `*Loop` is a distinct clock — the two host drivers are alternative implementations of the one per-process driver clock, and `superviseLoop` owns none. Six loop elements, four clocks; both counts are checkable.'
  - **verifier corrected to** Line 1047-1048 states a false identity and calls it checkable: "So **the number of `*Loop` elements is the number of clocks this codebase owns**, and the count is checkable rather than asserted." The register (lines 4752-4757) has SIX LOOP elements; Section 11 counts FOUR owned clocks (line 4472 "Five clocks, four of them ours"; line 4487 "Four clocks are owned here, not two"). The map from loop elements to counted clocks is not one-to-one, for two distinct reasons: (a) `frameLoop` and `headlessLoop` are two host implementations of the single per-process driver clock — line 4429 "There is exactly one per process, it comes from whatever host that process has"; the driver row at 4476 is owned by "the host this process happens to have ... one per **process**"; line 4497 places "`headlessLoop` / `frameLoop`" in one cell. (b) `superviseLoop` is excluded from Section 11's table because its poll paces no simulation and drives no tick — NOT because it owns no clock. Lines 4615, 4639 and 4659 all say it does own one (LOOP "owns a clock"; FREE = "we own the clock, so we must state its pacing"; pacing "100 ms"). Any fix must not assert `superviseLoop` owns no clock, or it creates a fresh three-way contradiction. Suggested repair for line 1047: "So **every clock this codebase owns is a `*Loop` element**, though the map is not one-to-one: `frameLoop` and `headlessLoop` are two host implementations of the one per-process driver clock, and `superviseLoop`'s 100 ms supervision poll paces no simulation, so Section 11 counts four clocks against six loop elements. Both counts are checkable — `loop-inventory` reconciles the element list against every cadence loop in the tree."
- `low` **L1110** `client` is not a component in the register — the component is `participant`, which is what both other cells of this row name. It is a retired name surviving inside backticks in a compile-graph claim; prose-claims cannot catch it because the backticked token contains an arrow and spaces, so its component-name check never fires. The same stale token appears again at line 5047.
  - **fix** `participant --> host`. Fix line 5047 identically.
  - **verifier corrected to** Stale retired component name `client` survives inside a compile-graph claim at two places, both of which should read `participant --> host`. Line 1124: "| `host_sdl` ↔ `participant` | `host_sdl --> host`, and `client --> host` | `host_sdl`'s `frameLoop` **calls** `participant` |" — the row is about host_sdl vs participant and the other two cells both name `participant`; `participant --> host` is a real grant while `client` is not a register component at all. Lines 5060-5061: "- **The host calls the client.** `frameLoop ==> clientTick` runs opposite to `host_sdl --> host` and\n `client --> host`. Reading the compile arrows as call direction inverts the system." — same substitution. Fix both to `participant --> host`. Why the gates miss it (corrected from the original filing, which named only the second half): scripts/prose-claims.py:455-456 builds its scan corpus by dropping every line that starts with "|", " ", "```", "<!--" or "%%" — line 1124 is a table row and line 5061 is a space-indented bullet continuation, so neither reaches the UNKNOWN_NAME check in the first place. Independently, the check's regex at prose-claims.py:457 is `` `([a-z][a-z0-9_]{2,})` ``, which requires the entire backticked span to be one bare snake token, so `client --> host` would not match even on an eligible line. Two independent escapes, not one.


## s7-zones (7 upheld)

- `high` **L1459** The ZONE table's `n` column says machine/ holds 10 components. The register holds 14 MACHINE components (base, content, core, host, host_null, host_sdl, platform, platform_null, platform_pc, script, storage, transport, transport_local, transport_tcp) and the GENERATED tree block in Section 9 prints `machine/ # 14 components`. The column now sums to 10+14+9+8 = 41, not 45 — exactly the four components added since (platform_null, transport, transport_local, transport_tcp). Nothing gates this table: it carries no `<!-- TABLE: -->` marker and tree_map/prose_claims both pass green while it contradicts the generated tree 44 lines away.
  - **fix** Change the machine/ row's `n` to 14, and add a note that the column must sum to the register total (45).
  - **verifier corrected to** Line 1459 of docs/superpowers/specs/2026-08-01-target-state-system-architecture.md: the ZONE table row "| **`machine/`** | the OS, the vendor, the asset store, the disk | 10 |" understates the machine zone by four. The components register (lines 2863-2911) holds 14 MACHINE components -- base, content, core, host, host_null, host_sdl, platform, platform_null, platform_pc, script, storage, transport, transport_local, transport_tcp -- and the GENERATED tree-map block at line 2926 prints "machine/ # 14 components". The other three rows (domain 14, device 9, composition 8) match the register, so the column sums to 41 rather than 45: precisely the pre-platform_null/transport/transport_local/transport_tcp snapshot. The contradiction is local, not remote: line 1476, seventeen lines below the table, states "**Every grant edge points down that order, at zero exceptions across 45 components**". (The original finding's "generated tree 44 lines away" is wrong -- that block begins at line 2923, roughly 1464 lines below -- but the point stands either way.) Nothing gates the table: it carries no <!-- TABLE: --> marker (markers exist only at lines 88, 1602, 2863, 3043, 4749) and tree-map, prose-claims and spec-consistency all report OK. Fix: change the machine/ row's n to 14, and either add a note that the column must sum to the register total (45) or -- better, since the numbers are already derivable -- fold the n column into a generated block so a fifth stale count cannot recur.
- `high` **L984** The KIND rules table — the document's only statement of "the rule each kind carries" — still has a CONTRACT row and has no INTERFACE row and no VOCABULARY row. CONTRACT is not in `spec-model.py`'s KINDS tuple, while INTERFACE and VOCABULARY are used by the register rows, the tree, the tally sentence, the facets ledger and every mermaid diagram. Grepping the whole spec for VOCABULARY returns only table cells and diagram labels: the rule that a VOCABULARY is a shared type language nobody implements exists only in a code comment in scripts/spec-model.py, nowhere in the prose. This is the load-bearing case of "used elsewhere, never defined in Section 7."
  - **fix** Replace the CONTRACT row with two rows: INTERFACE (declares an implementable interface; may name only foundation types; the only thing a `==>` may point at) and VOCABULARY (declares types everybody uses and nobody implements — nothing implements `Drawable`; may name only foundation types).
  - **verifier corrected to** CONFIRMED as filed, with scope slightly wider than stated. The KIND rules table at lines 981-987 — the spec's only statement of "the rule it carries" per kind, and not inside any generated block — still carries a CONTRACT row (line 984) and has neither an INTERFACE nor a VOCABULARY row, while spec-model.py:47's KINDS tuple has dropped CONTRACT and the register (2868-2899), the tree (2928-2949), the tally (2986), the facets ledger (3181-3183) and every mermaid diagram use INTERFACE and VOCABULARY. VOCABULARY's actual rule — only an INTERFACE is implementable; a VOCABULARY declares types everybody uses and nobody implements — appears nowhere in the spec's prose; it exists only as a comment at spec-model.py:106-107 and as an enforced gate at spec-consistency.py:526-529. Minor correction to the finding's wording: VOCABULARY does appear once in running prose, in the tally sentence at line 2986 ("three VOCABULARYs"), but as a count, not a rule. Two additions the finding under-states: line 1004 makes the ZONE-vs-KIND deviation argument in the dead token — "`platform` is a CONTRACT in `machine/` while `scene` is a CONTRACT in `domain/`" — collapsing the exact distinction it illustrates (platform is INTERFACE, scene is VOCABULARY); and lines 1096 and 1160 state the `==>` rule as "it points at a CONTRACT", whereas spec-consistency.py:526 rejects any `==>` target that is not an INTERFACE. 30 uppercase CONTRACT tokens remain in the file. The proposed fix (replace the CONTRACT row with an INTERFACE row and a VOCABULARY row) is the right shape; lines 1004, 1096 and 1160 should be corrected in the same pass.
- `medium` **L1006** Not reachable from the current register under any choice of per-kind default. Grouping the 45 rows by kind gives FOUNDATION {MACHINE 2}, INTERFACE {MACHINE 4, DEVICE 3, DOMAIN 1}, VOCABULARY {DOMAIN 3}, BACKEND {MACHINE 6, DEVICE 6}, LIBRARY {DOMAIN 10, MACHINE 2, COMPOSITION 1}, ENTRYPOINT {COMPOSITION 7}. Picking each kind's modal zone — the best case — leaves 4+6+3 = 13 deviations, so the figure is 13 of 45 (71% determined), not 8 of 45 (80%). BACKEND alone is a 6/6 tie split across machine/ and device/, so it contributes 6 deviations however the default is chosen. The same "8 of 45" is repeated at line 1451 inside the HISTORICAL block.
  - **fix** Recompute from the register and state the defaults explicitly, e.g. "ZONE is 71% determined by KIND — 13 of 45 components deviate from their kind's modal zone, and BACKEND does not have a default at all (6 in machine/, 6 in device/)". Update the HISTORICAL repeat at line 1451 or scope it to the component count of that era.
  - **verifier corrected to** Lines 1005-1006 assert a figure that the current register cannot produce under any per-kind default. Grouping the 45 register rows (via scripts/spec-model.py) by kind yields FOUNDATION {MACHINE 2}, INTERFACE {MACHINE 4, DEVICE 3, DOMAIN 1}, VOCABULARY {DOMAIN 3}, BACKEND {MACHINE 6, DEVICE 6}, LIBRARY {DOMAIN 10, MACHINE 2, COMPOSITION 1}, ENTRYPOINT {COMPOSITION 7}. Modal defaults — the best case, since modal maximises agreement — leave 4+6+3 = 13 deviations, so the true figure is 13 of 45, 71% determined, not 8 of 45 / 80%. BACKEND is a 6/6 tie across machine/ and device/ and so contributes 6 deviations however its default is chosen; it effectively has no default. The stale number is inherited from the pre-split, five-zone era, which is also why the same paragraph still illustrates the point with "`platform` is a CONTRACT in `machine/` while `scene` is a CONTRACT in `domain/`" even though platform is now INTERFACE and scene is VOCABULARY — post-split those two no longer share a kind, so the illustration no longer illustrates. Suggested repair at 1005-1006: state the defaults explicitly, e.g. "ZONE is 71% determined by KIND — 13 of 45 components deviate from their kind's modal zone, and BACKEND has no default at all (6 in machine/, 6 in device/)", and replace the platform/scene example with a within-kind pair such as `platform` (INTERFACE, machine/) vs `net` or another INTERFACE outside machine/. The repeat at line 1451 sits inside the HISTORICAL block (1448-1453) and describes the abandoned five-zone taxonomy, whose per-component zones are not in the register; it should be scoped to its era (component count and zone set of that time) rather than recomputed to 13.
- `medium` **L1470** This is the worked application of Section 7's contract-placement rule, and it enumerates 10 of the 11 contracts in the register: `transport` (INTERFACE, MACHINE) is missing. The same omission shows in the machine/ zone's `faces` cell one table up — "the OS, the vendor, the asset store, the disk" (line 1459, and the identical Z_MACHINE cluster label at line 1227) never names the network, although transport_tcp's duty is "the wire" and transport is the interface that abstracts it. So `transport` is the one component of 45 whose zone Section 7's own rule text does not account for.
  - **fix** Add transport to the machine sentence — "`host`, `platform`, `content` and `transport` abstract the machine" — and extend the machine/ faces cell and the Z_MACHINE cluster label to include the network/the wire.
  - **verifier corrected to** Section 7's zone-placement material is stale in three places after transport/platform_null were added, and the strongest evidence is numeric, not prose. 1) Line 1470-1471, the worked application of the rule at line 1468 ("A contract lives in the zone of what it abstracts, not in a zone of its own."): "`host`, `platform` and `content` abstract the machine. `scene`, `sound`, `net` and `celestial` are the domain describing itself. `gpu`, `audio` and `presentation` abstract devices and their sinks." — names 10 of the register's 11 contracts; `transport` (INTERFACE, MACHINE) is missing. Fix: "`host`, `platform`, `transport` and `content` abstract the machine." 2) Line 1459, the machine/ row of the faces table, is stale in BOTH cells — and the n cell is the hard defect the original finding missed: "| **`machine/`** | the OS, the vendor, the asset store, the disk | 10 |". The generated tree at line 2926 says "machine/ # 14 components" and lists all 14 (including transport/, transport_local/, transport_tcp/, platform_null/), and spec-model.py counts 14 MACHINE rows. 10 is exactly the pre-transport, pre-platform_null count. Domain 14, device 9 and composition 8 all still match the tree; only this row does not. So line 1459 directly contradicts a GENERATED block in the same document. 3) Line 1227's Z_MACHINE cluster label repeats the same stale faces text verbatim ("MACHINE — the OS, the vendor, the asset store, the disk"), in a mermaid cluster that itself contains transport, transport_local and transport_tcp (lines 1241-1243). The original finding's superlative — "transport is the one component of 45 whose zone Section 7's own rule text does not account for" — is false. transport_local and transport_tcp are MACHINE components whose stated duties are "the co-located pair" and "the wire" (lines 3053-3054); the faces cell's four nouns account for neither, so at least three components are unaccounted for, not one. The original also proposed only the prose fix and left the 10-vs-14 contradiction unfiled.
- `medium` **L1004** The point of this sentence is to hold KIND constant and vary ZONE, proving the two axes are independent. After the split it no longer does: `platform` is INTERFACE and `scene` is VOCABULARY in the register, so the first pair now varies both axes and cites a kind (CONTRACT) that no longer exists. The BACKEND pair is still correct.
  - **fix** Use a same-kind pair that survives the split, e.g. "`platform` is an INTERFACE in `machine/` while `presentation` is an INTERFACE in `device/`" (or `celestial`, INTERFACE in `domain/`), and keep the host_sdl/rendering BACKEND pair.
  - **verifier corrected to** Line 1004 is a genuine defect, and the filed version is correct as far as it goes; it is narrow rather than wrong. Accurate statement: the sentence at lines 1003-1005 proves ZONE and KIND are independent axes by giving two same-KIND/different-ZONE pairs. After the 2026-08-02 CONTRACT split the first pair is broken in two ways — it names a KIND that no longer exists (spec-model.py:47 KINDS has no CONTRACT), and its two members no longer share a kind at all (`platform` INTERFACE/MACHINE at line 2868 vs `scene` VOCABULARY/DOMAIN at line 2877), so it varies both axes and demonstrates nothing. The BACKEND pair (`host_sdl` BACKEND/MACHINE line 2870, `rendering` BACKEND/DEVICE line 2895) is unaffected. Fix as proposed: replace the first pair with a surviving same-kind pair, e.g. "`platform` is an INTERFACE in `machine/` while `presentation` is an INTERFACE in `device/`" (line 2882) or `celestial` in `domain/` (line 2887), keeping the BACKEND pair. ADDITION the filed finding missed: line 1004 is downstream of a larger stale instance in the same section — the KIND definition table at lines 981-987 still enumerates five kinds including "| **CONTRACT** | declares an interface and may name only foundation types...", omitting INTERFACE and VOCABULARY entirely. That table carries no `TABLE:` marker so it is not machine-read and the register gate will not catch it. Both should be fixed together; fixing 1004 alone leaves the section defining a kind the model does not have. (A third instance sits at line 1096: "it points at a CONTRACT" in the edge-semantics table — same class, worth folding into the same pass.)
- `medium` **L1160** The verdict exists and runs (gates.yml step `spec_consistency`, scripts/spec-consistency.py lines 502-529), but it does not check what this sentence says. It checks `comp.get(target).get("kind") != "INTERFACE"` and reports "a VOCABULARY has nothing to implement" — i.e. it deliberately rejects three of the eleven things Section 7 still calls contracts. As written the prose licenses `mixing ==> sound`, which the gate would fail. The arrow table at line 1096 makes the same claim ("it points at a CONTRACT").
  - **fix** Say INTERFACE in both places: "...and that it points at an INTERFACE — a VOCABULARY has nothing to implement, which is why the two are separate kinds."
  - **verifier corrected to** Lines 1160 and 1096 name a KIND the register no longer has, and the substitution is not cosmetic. The IMPLEMENTS_ARITY verdict's first clause matches the prose exactly (spec-consistency.py:520, `v["kind"] == "BACKEND" and len(got) != 1`), but its second clause is strictly narrower than "CONTRACT": spec-consistency.py:526 requires `comp[target]["kind"] == "INTERFACE"` and reports "`==>` may only point at an INTERFACE -- a VOCABULARY has nothing to implement". Section 7 uses CONTRACT as the umbrella over all eleven declaration components — line 1004 calls the VOCABULARY `scene` "a CONTRACT in `domain/`", and line 1471 lists `scene`, `sound` and `net` under the contract-zone rule — so "it points at a CONTRACT" reads as permitting the three VOCABULARYs that the gate rejects. Fix: say INTERFACE in both places, e.g. "...and that it points at an INTERFACE — a VOCABULARY has nothing to implement, which is why the two are separate kinds." Two caveats on the original filing: the gate does check the arity half verbatim, so "it does not check what this sentence says" applies only to the second clause; and `mixing ==> sound` is an illustration, not a live violation (the diagram draws `mixing ==> contract` at line 1330 with `mixing --> sound` as a plain grant at 1331, and the gate currently passes). Root cause worth reporting separately: the KIND table at lines 982-986 still lists FOUNDATION / CONTRACT / BACKEND / LIBRARY / ENTRYPOINT and has no INTERFACE or VOCABULARY row, so it contradicts the KINDS tuple in scripts/spec-model.py:47 and leaves every downstream "contract" in Section 7 with no narrow reading available.
- `medium` **L984** The three numbers are accurate against today's tree (StarApplication.cpp = 41 lines, StarRenderer.cpp = 89, no .cpp for the platform service headers), but "metered" and "ratcheting down" claim an instrument that does not exist. No step in .github/workflows/gates.yml measures contract .cpp line counts; the only CEILING ratchets in scripts/ are layering-lint.py and render-inventory.py (both scoped to `source/rendering/*.cpp`), round-trip.py (round trips) and dedup-measure.py. prose_claims' UNREGISTERED_GATE verdict cannot catch this because the sentence names no gate, so an unbacked ratchet claim passes green.
  - **fix** Either downgrade to a measurement — "and its size is measured today at `platform` 0, `host` 41, `gpu` 89 lines" — or register an actual ratchet step in gates.yml and name it here so prose_claims can check it.
  - **verifier corrected to** CONFIRMED, with one correction to the finding's supporting text. Defect (line 984, the KIND rule table, column header "the rule it carries" at line 981): "and its size is metered: `platform` 0, `host` 41, `gpu` 89 lines, ratcheting down". The three numbers are exact against today's tree (source/platform/ = four .hpp, zero .cpp; StarApplication.cpp = 41; StarRenderer.cpp = 89), but nothing meters them and nothing ratchets them. gates.yml contains no line-count step, source/test/CMakeLists.txt registers none, and spec-measures.py — the gate that owns "exactly the figures a design decision rests on" — has four MEASURES, none of which is a line count. "metered" is a defined term in this repository (docs/render/architecture-3-target-state.md:147 "of those, metered by a gate"; scripts/render-inventory.py:209 prints "— not metered"), so the word claims an instrument that is absent, and "ratcheting down" claims a ceiling that only goes down with no gate to enforce it. The document already applies the correct idiom to the identical situation at line 434 ("N3.b is measured rather than gated ... an instrument this document reports rather than a gate that can ratchet") and at line 350 treats "a ratchet [with] no metric and no starting ceiling" as a defect that had to be closed. prose_claims cannot see it: prose-claims.py:275 matches only "gated by/as `X`", and this sentence names no gate. Correction to the finding's inventory: scripts/ carries more ceiling ratchets than the finding lists. Besides layering-lint.py (per-file ceilings over source/rendering/*.cpp for render_layering, plus host_api_neutral over source/application/StarMainApplication_sdl.cpp=18), round-trip.py and dedup-measure.py (CEILING = 596), there are also boundary-inventory.py (--max-sink 213, gated as boundary_ratchet) and grant-sweep.py (per-edge REMOVING ceilings). render-inventory.py has no ceiling of its own — it reports layering-lint's, and its own docstring at line 178 says the generated block is "coupling residual only. No line counts". None of these meters a contract .cpp line count, so the conclusion is unchanged. Fix (prefer the document's existing idiom over inventing a gate): "and its size is measured today at `platform` 0, `host` 41, `gpu` 89 lines" — or, if the ratchet is wanted, register a real step in gates.yml and name it with "gated as `X`" so prose_claims' UNREGISTERED_GATE can hold it. A secondary note on the same line, out of scope for this finding: the row is still headed **CONTRACT**, while spec-model.py's KINDS is ('FOUNDATION','INTERFACE','VOCABULARY','BACKEND','LIBRARY','ENTRYPOINT') and `platform`/`host`/`gpu` all carry kind INTERFACE.


## stale-vocab (12 upheld)

- `high` **L970** CATEGORY (a)+(b). This is the canonical "| KIND | the rule it carries |" table — the one place the document defines what each kind means — and it still has five rows headed by CONTRACT. INTERFACE and VOCABULARY, the two kinds the whole register at line ~2839+ actually uses, are defined nowhere. A reader who meets `scene | VOCABULARY | DOMAIN` in the register has no rule to look up, and the CONTRACT row's rule ("declares an interface", ".cpp holds value-type constructors only", metered line counts for platform/host/gpu) is an INTERFACE rule that is simply false of a VOCABULARY, whose whole content is value types.
  - **fix** Replace the CONTRACT row with two rows: **INTERFACE** — declares an implementable interface, may name only foundation types and other INTERFACEs/VOCABULARYs; any `.cpp` holds value-type constructors and trivial defaults only, and its size is metered (`platform` 0, `host` 41, `gpu` 89 lines, ratcheting down); every BACKEND's `==>` points at one. **VOCABULARY** — a shared type language, NOT implementable: it declares types that cross a seam as values, has no implementations, and nothing may `==>` it.
  - **verifier corrected to** CONFIRMED with one correction to the reasoning. Accurate version: at line 984 the document's only KIND-rule table (header at 981, sole match for "the rule it carries" alongside the ZONE table at 996) still carries a **CONTRACT** row and has no **INTERFACE** or **VOCABULARY** row. CONTRACT is not in spec-model.py's KINDS tuple (line 47) and would be rejected by the taxonomy check at line 108, while INTERFACE and VOCABULARY — the kinds the register at 2863-2911 actually uses — are given a rule nowhere in the 6000-line document; the only definition of the split lives in spec-model.py's comment at lines 98-106. The knock-on is that the BACKEND row at line 985 ("implements a contract") and the `==>` row at line 1096 ("every BACKEND has exactly one, and it points at a CONTRACT") both name a kind that no longer exists. CORRECTION to the filed rationale: the finding says the CONTRACT rule "is simply false of a VOCABULARY, whose whole content is value types." Only part of that is right. The false-of-VOCABULARY clauses are "declares an interface" and the metered line counts, which name `platform`, `host` and `gpu` — all three INTERFACE in the register (2868, 2869, 2898). The ".cpp holds value-type constructors and trivial defaults only" clause is *not* false of a VOCABULARY; it is the clause that fits a VOCABULARY best, so "whose whole content is value types" argues the opposite of what it is offered for. The proposed fix is otherwise sound and consistent with the model: an INTERFACE row (implementable, names only foundations and other interfaces/vocabularies per line 3070, metered .cpp, the target of every BACKEND's single `==>`) plus a VOCABULARY row (a shared type language, not implementable, nothing may `==>` it).
- `high` **L4715** CATEGORY (d). The graft rule reads "either they share a component, or *a*'s component grants *b*'s, or both grant a common CONTRACT to dispatch through." As written, a shared VOCABULARY would satisfy it. The implementation does not agree: scripts/spec-consistency.py:588-589 filters the shared set with `if comp.get(c, {}).get("kind") == "INTERFACE"`, so two components that share only `scene` or `net` FAIL the graft rule. The document's statement of its own gate is now weaker than the gate.
  - **fix** "...or both grant a common **INTERFACE** to dispatch through" — and say why: a VOCABULARY carries no virtual call, so it cannot be a dispatch point.
  - **verifier corrected to** CONFIRMED with one scope correction. At line 4729 the graft rule's third clause reads "or both grant a common CONTRACT to dispatch through". CONTRACT is no longer a KIND (scripts/spec-model.py:47 KINDS = FOUNDATION, INTERFACE, VOCABULARY, BACKEND, LIBRARY, ENTRYPOINT), and the enforced gate at scripts/spec-consistency.py:588-589 filters the shared set to kind == "INTERFACE". Measured consequence: 14 unordered component pairs satisfy the sentence and fail the gate (rendering/world_view, windowing/world_view, frontend/world_view, frontend/presentation, rendering/presentation, presentation/windowing, presentation/participant, presentation/transcript, presentation/world_view, transcript/world_view, world_view/participant via `scene`; presentation/mixing, mixing/world_view via `sound`; game/participant via `net`). Fix the clause to "or both grant a common **INTERFACE** to dispatch through", with the reason the implementation already records at scripts/spec-model.py:98-105 — a VOCABULARY has no methods, so it cannot be a dispatch point. Scope correction the original finding understates: the single word is not the whole defect. The KIND-rule table at line 984 still carries a "| **CONTRACT** | declares an interface and may name only foundation types..." row and has no INTERFACE or VOCABULARY rows, and prose at line 4215 explicitly labels the two VOCABULARYs as contracts ("| `scene` CONTRACT | **`sound`** CONTRACT | what exists / what is audible — vocabulary, no engine |"). That table plus that prose are what make the loose reading of 4729 the natural one, so line 984 should be split into INTERFACE and VOCABULARY rows in the same pass, otherwise the corrected graft rule will cite a kind the document never defines. The gate is green today (spec-consistency: OK), so this misleads a future author drawing a new edge rather than hiding a present violation.
- `high` **L1082** CATEGORY (d). Same defect at the arrow-notation table, which is where a reader learns what `==>` means. Under the split, `==>` may point only at an INTERFACE; every one of the design's implements edges (`host_sdl ==> host`, `gpu_opengl ==> gpu`, `rendering ==> presentation`, `transport_tcp ==> transport`, `mixing ==> presentation`) targets an INTERFACE, and pointing one at a VOCABULARY is a gate failure.
  - **fix** "**A includes B and implements the interface that says what A *is*** — its identity | **every BACKEND has exactly one**, and it points at an INTERFACE".
  - **verifier corrected to** Line 1096 (not 1082) of /root/frackin/OpenStarbound/docs/superpowers/specs/2026-08-01-target-state-system-architecture.md, the sole compile-projection arrow legend: "| `A ==> B` | **A includes B and implements the contract that says what A *is*** — its identity | **every BACKEND has exactly one**, and it points at a CONTRACT |". The uppercase kind token CONTRACT is stale — spec-model.py:47 KINDS has no CONTRACT, and spec-consistency.py:526-530 fails any `==>` whose target is not an INTERFACE ("a VOCABULARY has nothing to implement"). This is the one place a reader learns the arrow's rule, so the stale token invites drawing `X ==> scene`, the exact failure spec-model.py:101 records as previously live. Minimal fix: change only the rule-column token — "**every BACKEND has exactly one**, and it points at an INTERFACE". Rewriting the lowercase "the contract that says what A *is*" is optional and not part of the defect: lowercase "contract" is deliberate live idiom at line 1201 ("the presentation contract") and line 4968. The same stale token recurs at line 1160, where it misdescribes the very gate it cites: "IMPLEMENTS_ARITY verdict asserts that every BACKEND has exactly one `==>` and that it points at a CONTRACT" — that should read INTERFACE too, and it is a second instance in the same section.
- `high` **L990** CATEGORY (d) — the sentence is now FALSE, and its purpose collapses with it. Its whole job is to show that KIND does not determine ZONE by exhibiting one kind in two zones. But the register now has `platform | INTERFACE | MACHINE` and `scene | VOCABULARY | DOMAIN` — two different kinds in two different zones, which demonstrates nothing. The `host_sdl`/`rendering` half of the sentence still works (both BACKEND).
  - **fix** Use a pair that is still one kind in two zones: "`platform` is an INTERFACE in `machine/` while `celestial` is an INTERFACE in `domain/`" — `celestial` is the register's only DOMAIN INTERFACE and is exactly the counterexample this sentence needs.
  - **verifier corrected to** Line 1004 (not 990) — "`platform` is a CONTRACT in `machine/` while `scene` is a CONTRACT in `domain/`" — is a stale exhibit that the CONTRACT split broke and that a rename cannot repair. The register now has `platform | INTERFACE | MACHINE` (line 2868) and `scene | VOCABULARY | DOMAIN` (line 2877), so the pair no longer shows one kind in two zones, and rewriting it as "an INTERFACE... an INTERFACE" would state something the register contradicts. Scope, accurately: this breaks ONE of the sentence's TWO exhibits. The second pair — `host_sdl` BACKEND in `machine/` vs `rendering` BACKEND in `device/` (register lines confirm BACKEND/MACHINE and BACKEND/DEVICE) — is intact and by itself carries the point, and the "ZONE is 80% determined by KIND — only 8 of 45 components deviate" measurement at line 1005 is untouched. The sentence's purpose does not collapse; deleting the broken clause leaves the rule meaning exactly what it meant. Fix, correctly scoped: swap in `celestial`, which I confirmed is the register's sole INTERFACE+DOMAIN row (line 2887) — "`platform` is an INTERFACE in `machine/` while `celestial` is an INTERFACE in `domain/`". But do it together with the upstream defect it depends on: the KIND rule table at lines 981-987 still lists "**CONTRACT** | declares an interface and may name only foundation types..." and has no INTERFACE or VOCABULARY row, so line 1004 is currently consistent with its own table and only the register contradicts it. Editing 1004 alone would name a kind the table 20 lines above never defines. (Minor caveat on the exemplar: line 6030 flags `celestial` as slated for a vocabulary/interface split — "the vocabulary half stops being an INTERFACE" — so it is a correct but future-fragile choice; `presentation`/`gpu`/`audio` in `device/` are alternative INTERFACE-in-another-zone exhibits.)
- `high` **L1445** Stale count, and self-contradicted 5 lines later. The zone table's n column reads 10 / 14 / 9 / 8, summing to 41. The register has MACHINE 14, DOMAIN 14, DEVICE 9, COMPOSITION 8 = 45 (I counted from the machine-read components table; the GENERATED tree at line ~2912 also prints "machine/ # 14 components"). Line 1462 in the same subsection already says "at zero exceptions across 45 components". The four MACHINE components added since (`platform_null`, `transport`, `transport_local`, `transport_tcp`) are exactly the missing 4.
  - **fix** machine/ = 14, and note the total is 45 — or, better, generate this column from the register the way the tree beside it already is.
  - **verifier corrected to** Line 1459 (not 1445) of docs/superpowers/specs/2026-08-01-target-state-system-architecture.md: `| **`machine/`** | the OS, the vendor, the asset store, the disk | 10 |` — the n cell is stale. The machine-read components table gives MACHINE=14 (base, content, core, host, host_null, host_sdl, platform, platform_null, platform_pc, script, storage, transport, transport_local, transport_tcp); the four recently added MACHINE components (platform_null, transport, transport_local, transport_tcp) are exactly the shortfall. The other three rows (DOMAIN 14, DEVICE 9, COMPOSITION 8) are correct, so the column currently sums to 41 against the true 45. This contradicts three nearby statements: line 1451 ("only 8 of 45 components"), line 1476 ("at zero exceptions across 45 components"), and the GENERATED tree at line 2926 ("machine/ # 14 components") with line 2982 ("45 components in 4 zone directories"). The mermaid Z_MACHINE subgraph at lines 1227-1246 already lists all 14. Fix: change 10 to 14. Optionally generate the n column from the register, noting that unlike the tree beside it this table is not currently machine-read (it has no <!-- TABLE: --> marker).
- `medium` **L4201** CATEGORY (b). Both `scene` and `sound` are VOCABULARY in the register. The row's own duty text — "vocabulary, no engine" — already says so; only the kind token is stale. Line 4203 in the same table is the mirror case, CATEGORY (a): `gpu` and `audio` are both INTERFACE.
  - **fix** Line 4201: "`scene` VOCABULARY | **`sound`** VOCABULARY". Line 4203: "`gpu` INTERFACE | **`audio`** INTERFACE".
  - **verifier corrected to** Line numbers only. The row is at line 4215, not 4201; the mirror row is at line 4217, not 4203. Register evidence is at lines 2877/2878 (scene, sound = VOCABULARY) and 2898/2899 (gpu, audio = INTERFACE). Fix as proposed: line 4215 -> "| `scene` VOCABULARY | **`sound`** VOCABULARY | what exists / what is audible — vocabulary, no engine |"; line 4217 -> "| `gpu` INTERFACE | **`audio`** INTERFACE | the device interface |". Related but out of scope for this finding: the KIND-definition table at line 984 still defines a **CONTRACT** kind that spec-model.py no longer recognises, and lines 4216/4218 of the same table are already correct (`rendering`/`mixing` and `gpu_opengl`/`audio_sdl` all read BACKEND), which is what makes the two stale rows stand out as an incomplete edit rather than a deliberate shorthand.
- `medium` **L4289** CATEGORY (a), plus a stale enumeration. `celestial` is INTERFACE, so this and its five siblings in the same derivation (lines 4283 "### `celestial` — a CONTRACT", 4299, 4320, 4329, and the grants-table statement at 3056 "a CONTRACT names only foundations and other contracts") should all read INTERFACE. The parenthesised list is also stale in a second way: it names six components and omits `content`, `net`, `presentation` and `transport`, all of which now satisfy the same rule (grants: content→core,base; net→core; transport→core; presentation→core,base,scene,sound).
  - **fix** INTERFACE throughout the `celestial` derivation and its grant row; and either complete the list (`scene`, `sound`, `net`, `gpu`, `audio`, `host`, `platform`, `content`, `transport`, `presentation`) or replace the enumeration with the rule "every other INTERFACE and VOCABULARY in the register names only foundations, INTERFACEs and VOCABULARYs".
  - **verifier corrected to** Stale kind token in the `celestial` derivation, plus an incomplete illustrative list. Actual lines (finding's numbers are all -14): the register, facets table and mermaid all carry `celestial | INTERFACE | DOMAIN` (2887, 3161, 1209), but the derivation names the retired kind CONTRACT at 4297 (heading: "### `celestial` — a CONTRACT, because two components consume the star map and neither owns it"), 4303, 4313 ("**It is a CONTRACT rather than a LIBRARY because of what it must survive**"), 4334, 4343, and in the grant row at 3070. Only 4297 and 4313 are pure kind-token uses that should read INTERFACE. At 4303, 4334, 4343 and 3070 the word is an umbrella covering both INTERFACE and VOCABULARY — the quoted sentence's own enumeration (`scene`, `sound`, `gpu`, `audio`, `host`, `platform`) mixes four INTERFACEs with two VOCABULARYs — so substituting INTERFACE there would make the rule false (`presentation` grants core, base, scene, sound; LIBRARYs such as `world_view` and `game` legally name the VOCABULARYs scene, sound and net). Those four sites want "an INTERFACE or VOCABULARY", or a rephrase to the general rule. The parenthetical list is short by four (`content`, `net`, `presentation`, `transport` all satisfy it, as do all 11 non-`celestial` contracts, so the stated invariant is true register-wide), but it is illustrative: deleting it leaves "every other contract in the register names only foundations and other contracts" unchanged, so this is cosmetic rather than a defect in the rule. The real root cause, unfiled, is line 984: the KIND definition table still lists CONTRACT as a kind and the document never defines INTERFACE or VOCABULARY in prose at all.
- `medium` **L5548** Stale measurement contradicted by the instrument it cites. `python3 scripts/spec-derivations.py` prints "spec-derivations: 45 of 45 components fully derived, 270 of 270 facets answered". The verification table reports a number the gate no longer produces.
  - **fix** "**built** — 45/45 components, 270/270 facets".
  - **verifier corrected to** Line 5562 (the finding cites 5548, which is the "### The full instrument set" heading — line numbers drifted by 14; the quoted text is byte-for-byte accurate at 5562). DEFECT: The `spec_derivations` row of the instrument-set table reports a stale measurement that the instrument named in the same row no longer produces. Current: `| **every component is reasoned for, not merely declared** | ` + "`spec_derivations`" + ` | anchoring | **built** — 41/41 components, 246/246 facets |` `python3 scripts/spec-derivations.py` prints `spec-derivations: 45 of 45 components fully derived, 270 of 270 facets answered` (script line 290), and the generated ledger at spec line 3135 already reads "**45 of 45 components fully derived · 270 of 270 facets answered.**" Line 5562 is outside every generated block, so nothing regenerates it. FIX: `**built** — 45/45 components, 270/270 facets` ADJACENT (out of scope for this finding, flagged only so it is not lost): the same staleness class appears in the facets table at lines 3558, 3569, 3580, 3602 and 3613, which say "32 of 41", "32 of 41", "26 of 41", "11 of 41", "19 of 41" while the generated composition blocks at 2035, 2260, 2335, 2525, 2656 and 2856 say "35 of 45", "35 of 45", "12 of 45", "16 of 45", "22 of 45", "30 of 45". Those are separate rows and belong in a separate finding.
- `medium` **L5723** Stale on both numbers, and the sentence explicitly binds itself to the tool's output. `python3 scripts/grant-sweep.py` reports "UNVERIFIABLE=31" against 45 components. The claim-at-risk line directly above (5722, "The 41 components in the register are boundaries a build enforces (P1)") carries the same stale total.
  - **fix** "**31 of the 45 components are not yet their own directory**", and "The 45 components in the register..." at line 5722.
  - **verifier corrected to** Real defect, correct in substance; the line numbers drifted by 14 and the fix list is one site short. Site 1 — line 5736 (claim-at-risk row of the R4 facets table): is: | **claim at risk** | The 41 components in the register are boundaries a build enforces (P1). | should: | **claim at risk** | The 45 components in the register are boundaries a build enforces (P1). | Site 2 — line 5737 (how-it-shows row of the same table): is: **27 of the 41 components are not yet their own directory**, which is exactly the set `grant_sweep` reports UNVERIFIABLE. should: **31 of the 45 components are not yet their own directory**, which is exactly the set `grant_sweep` reports UNVERIFIABLE. Site 3 — lines 5425-5426, MISSED BY THE ORIGINAL FINDING (block-quoted hard constraint, same two stale numbers, same tool binding): is: > enforceable if and only if it is its own directory.** 27 of the 41 are not yet, which is exactly the > set `grant-sweep` calls UNVERIFIABLE — so the two numbers are not two problems, they are one problem should: 31 of the 45. Evidence: `python3 scripts/grant-sweep.py` -> "UNVERIFIABLE=31"; `scripts/spec-model.py` `components(text)` -> 45; and the generated block at line 5381 already states "**31 of the 45 components have no files yet**", so sites 1-3 contradict a machine-generated block in the same document.
- `medium` **L3544** A whole family of stale composition link counts. Computed from the machine-read grants table via spec-model.py, the closures are now: client_opengl 35, client_sdl_gpu 35, client_headless 30, client_agent 22, server 16, world_sim 13, world_gen 12 — all of 45. The document says 32/41, 32/41 (3555), 26/41 (3566), 11/41 with "world_sim's 12 and server's 13" (3588), 19/41 with "world_gen links 11, world_sim 12 and server 13" (3599), and 13/41 vs "client_opengl's 32" (4355). Every one is wrong, and 3588's superlative ("the smallest composition in the register") survives only by luck: world_gen 12 is still below world_sim 13 and server 16.
  - **fix** Restate as 35, 35, 30, 22, 16, 13, 12 of 45, or generate the history counts from the grants table the way the composition diagrams already are. Note 3599's "The 13 between 19 and client_opengl's 32" happens to still be 13 (35-22) — update the endpoints, not the gap.
  - **verifier corrected to** CONFIRMED, with corrected line numbers (all +14 from the filing) and a wider blast radius. Twelve prose sites state composition link counts against a stale register of 41 components. The grants table now yields, via scripts/spec-model.py: client_opengl 35, client_sdl_gpu 35, client_headless 30, client_agent 22, server 16, world_sim 13, world_gen 12 — all of 45. The document's own generated composition blocks (lines 2035, 2260, 2335, 2420, 2525, 2656, 2856) already print these figures and pass `composition-graphs.py --check`, so the document contradicts itself. Facet-table sites (as filed, renumbered): - 3558 client_opengl "32 of 41" → 35 of 45 - 3569 client_sdl_gpu "Also 32 of 41" → 35 of 45 - 3580 client_headless "26 of 41 ... client_opengl's 32" → 30 of 45 ... 35 - 3602 world_gen "11 of 41 ... below world_sim's 12 and server's 13" → 12 of 45 ... below world_sim's 13 and server's 16. The superlative "the smallest composition in the register" remains true and should be kept. - 3613 client_agent "19 of 41 ... world_gen links 11, world_sim 12 and server 13 ... The 13 between 19 and client_opengl's 32" → 22 of 45 ... 12, 13, 16 ... the gap is still 13, between 22 and 35. Update the endpoints, not the gap. - 4369 server "13 of 41 ... client_opengl's 32" → 16 of 45 ... 35 Six further sites the original filing missed: - 3948, 3949, 3950 — the "four client compositions" table: "**32 of 41**", "**26 of 41**", "**19 of 41**" → 35, 30, 22 of 45 - 3995, 3996, 3997 — the authority table: same three figures - 5645 — "`client_headless` links 26 of 41" → 30 of 45 - 1807 — the counting-convention paragraph itself: "*Links N of 41* ... `server` links 13 because it is itself plus 12 dependencies" → of 45; server links 16, itself plus 15 Two adjacent counts to check while in the area (dependency-count convention, not closure — 1807 requires prose that counts dependencies to say so in the sentence, and these do not): 3869 and 5823 "the server links 15 of 45 components" (closure 16), and 4000 "the composition is 21 of 45 components against the graphical client's 32" — which mixes conventions in one sentence and carries a stale 32 for client_opengl. Root cause worth fixing alongside: prose-claims.py's COUNT_DRIFT check cannot see any of these. Line 455 filters the corpus with `if not l.startswith(("|", " ", "```", "<!--", "%%"))`, and every stale count sits in a table row. Either derive these figures the way the composition diagrams already are, or extend the gate's corpus to table cells so a stale count in a facet table fails as loudly as one in a paragraph.
- `medium` **L1679** CATEGORY (a) — plain renames, all referring to `gpu` (INTERFACE) or to the INTERFACE kind generally. The same site recurs verbatim in the element register at 4752 and the drives table at 4803 ("Owned by the CONTRACT and *dispatched*"), and at 4770 ("`DISPATCH` crosses a CONTRACT — the caller names an interface"), 4116 ("Adopted as a CONTRACT in `machine/`" — that is `content`, INTERFACE), 5872 ("a BACKEND that has swallowed two CONTRACTs" — `platform` and `host`, both INTERFACE), 5078 ("shared CONTRACTs: none") and 6051 ("share no CONTRACT").
  - **fix** INTERFACE at every one of 1679, 4116, 4752, 4770, 4803, 5078, 5872, 6051. Note 5078/6051 are graft-rule diagnostics, so INTERFACE is not just tidier there — it is what the gate actually intersects on.
  - **verifier corrected to** Seven prose sites use the retired KIND token CONTRACT where the current taxonomy requires INTERFACE, plus one generated echo. Edit sites: 1693 ("operation, so the CONTRACT owns the element and each backend implements it" - `gpu`, INTERFACE, confirmed by 1692/1698 "swapTick moves from host_sdl to gpu" / "With swapTick owned by gpu"); 4130 ("Adopted as a CONTRACT in `machine/`" - `content`, INTERFACE, per the heading at 4119); 4766 (elements register, swapTick duty: "Owned by the CONTRACT and *dispatched*", owner `gpu`); 4784 ("`DISPATCH` crosses a CONTRACT - the caller names an interface"); 5092 ("shared CONTRACTs: none"); 5886 ("a BACKEND that has swallowed two CONTRACTs" - those two are ApplicationController -> `host` and Renderer -> `gpu`, NOT `platform`, which 5882 shows already has its own directory); and 6065 ("share no CONTRACT"). Line 4817 is NOT a separate edit: it lies inside the drive-table generated block (4802-4829) and its duty string is copied verbatim from 4766 by scripts/drive-table.py:140, so it is fixed by editing 4766 and re-injecting; editing it in place would be reverted and would trip the freshness gate. At 5092 and 6065 the substitution is load-bearing rather than cosmetic: scripts/spec-consistency.py:588-589 intersects grant sets on kind == "INTERFACE", and the only shared grant there is `core` (FOUNDATION), so INTERFACE is what the gate actually computes. Any such rename should also carry the two upstream sites the finding omits - the KIND rules table row at 984, which still defines CONTRACT and has no INTERFACE or VOCABULARY row, and the normative graft rule at 4729 ("both grant a common CONTRACT to dispatch through").
- `low` **L2902** Stale row count for the components register, sitting immediately above the GENERATED tree whose own footer reads "**45 components in 4 zone directories.**" The register has 45 data rows (spec-model.py: "45 components").
  - **fix** "a 45-row table".
  - **verifier corrected to** Line 2916 (not 2902) carries a stale row count for the components register. Quote: "tree beside a 41-row table is a second declaration of the same fact." The register it names ("the register above", <!-- TABLE: components --> at 2863 through <!-- END TABLE: components --> at 2911) has 45 data rows, confirmed by scripts/spec-model.py ("spec-model: 45 components, 43 grant rows, 24 elements"). The stale 41 is the pre-2026-08-02 count, before platform_null, transport, transport_local and transport_tcp were added (lines 2873-2876). The sentence is the preamble to the GENERATED tree-map block (BEGIN at 2923), whose own footer at line 2982 reads "**45 components in 4 zone directories.**" — so the document contradicts itself within one subsection. Line 5376 makes this a gate violation, not a cosmetic one: coherence is defined as "the document does not contradict itself — ... prose tallies against both", scoped to "all 45 components and all 24 elements." Fix: "a 45-row table".


## purity-1 (4 upheld)

- `high` **L956** The Director's own exemplar is still present, unmodified, inside the KIND-definition cell. It is a current-state measurement plus ratchet commentary attached to a rule about what a kind may contain. (The same row is also stale in a second way: CONTRACT is no longer a KIND — spec-model.py's KINDS tuple has INTERFACE and VOCABULARY in its place — so this table has not been updated by the 2026-08-02 split at all.)
  - **fix** Cut the clause. The cell reads: "declares an interface and may name only foundation types. Any `.cpp` holds **value-type constructors and trivial defaults only** — never a backend's behaviour". The three line counts and the word "ratcheting" belong in Section 17 (Delta from today) or as a named ratchet row in Section 15, beside `host_api_neutral`. Separately, split this row into INTERFACE and VOCABULARY rows to match the register.
  - **verifier corrected to** Line 970 (not 956), the KIND-definition table (lines 967-974): the table was not updated by the 2026-08-02 CONTRACT split. It still carries a single "| **CONTRACT** | …" row and lists five kinds, while scripts/spec-model.py:47 defines six — KINDS = ("FOUNDATION", "INTERFACE", "VOCABULARY", "BACKEND", "LIBRARY", "ENTRYPOINT") — and rejects the token "CONTRACT" outright at spec-model.py:108. The register this table is supposed to define has already moved (line 2854 `platform` INTERFACE, 2855 `host` INTERFACE, 2884 `gpu` INTERFACE, 2863 `scene` VOCABULARY), as have the diagram nodes (1179-1227). A reader consulting the taxonomy table gets a kind that no longer exists and never learns VOCABULARY exists. The follow-on sentence at line 990 is stale in the same way: "`platform` is a CONTRACT in `machine/` while `scene` is a CONTRACT in `domain/`" — `scene` is a VOCABULARY. Fix: split the row into an INTERFACE row (implementable; the .cpp rule and its metering belong here) and a VOCABULARY row (a shared type language, not implementable — nothing implements `Drawable`, per spec-model.py's comment at lines 98-107), and repair line 990. Explicitly NOT part of this defect: the clause "and its size is metered: `platform` 0, `host` 41, `gpu` 89 lines, ratcheting down" should not be cut. It is the only statement in the document that the "value-type constructors and trivial defaults only" prohibition is counted and ratchets down — the phrase occurs nowhere else, and no row in Section 15's instrument set (5536-5553) meters contract .cpp size. Cutting it would leave an unmeasured adjectival rule. If anything is relocated it is the three numerals, not the metering commitment; and neither proposed home exists today (Section 17's ratchet at 5758 is "design not yet approved" and about round-trips; `host_api_neutral` is prose at 5583/5699, not a Section 15 row).
- `high` **L702** The column is "groups it can supply" and counts Lua binding groups out of 11. `, transport_tcp` is not a Lua binding group — it appears in all three rows (702, 703, 704) and reads as a stray edit from the transport component addition. The prose at line 706 says "the three numbers fall straight out of the grant table", confirming the cell should hold a number and nothing else.
  - **fix** Delete `, transport_tcp` from lines 702, 703 and 704, leaving `**11 / 11**`, `**10 / 11**`, `**7 / 11**`. If the transport grant needs stating, it belongs in the grants table (it is already at 3054/3055/3056), not in a binding-group count.
  - **verifier corrected to** Lines 716, 717 and 718 (not 702-704) each append `, transport_tcp` to a cell in a column headed "groups it can supply", which is right-aligned and holds a Lua-binding-group count. `transport_tcp` is a BACKEND component, not one of the 11 Lua callback groups — the canonical list is enumerated at lines 691-701 and again in the generated measures block at line 5487 (`input`, `voice`, `camera`, `renderer`, `clipboard`, `http`, `interface`, `chat`, `celestial`, `team`, `world`), and transport is in neither. The prose at line 721 ("the three numbers fall straight out of the grant table without anyone choosing them") confirms the cells should hold numbers alone, and the subtraction against the `missing` column closes without transport (11-1=10, 11-4=7). Fix: delete `, transport_tcp` from lines 716, 717 and 718, leaving `**11 / 11**`, `**10 / 11**`, `**7 / 11**`. The transport_tcp grant is already stated where it belongs, in the machine-read grants table at lines 3068, 3069 and 3070 (the finding cited 3054-3056). Note this table is hand-written — it is not inside a `<!-- TABLE: -->` or `<!-- BEGIN GENERATED -->` block — so no existing gate will catch the drift.
- `medium` **L2888** Stale count. The machine-read `components` table has 45 data rows (verified by parsing between the TABLE markers), and the document states 45 components at lines 11, 929, 978 and 2954. This is exactly the class of embedded current-state number the Director warns "has already been wrong once".
  - **fix** Change "41-row" to "45-row", or better, drop the number: "a hand-written tree beside the register is a second declaration of the same fact" — which removes the drift surface entirely.
  - **verifier corrected to** CONFIRMED, with the finding's scope widened and its quotation corrected. Core defect (line 2916 of /root/frackin/OpenStarbound/docs/superpowers/specs/2026-08-01-target-state-system-architecture.md): "One directory per component, grouped by zone. Derived from the register above, because a hand-written tree beside a 41-row table is a second declaration of the same fact." The register has 45 rows, not 41 -- confirmed by scripts/spec-model.py ("45 components") and by counting the 45 data rows between <!-- TABLE: components --> (2863) and <!-- END TABLE: components --> (2911). Contradicted 66 lines later by the generated tally at 2982 ("**45 components in 4 zone directories.**") and the prose at 2986 ("Forty-five components"). Proposed fix, unchanged and correct: prefer dropping the number entirely -- "a hand-written tree beside the register is a second declaration of the same fact" -- since the sentence's argument is about duplication, and the count contributes nothing to it while creating the exact drift surface the document's own rule (line ~5915: "no document may state a current-state number an instrument cannot measure") forbids. Two corrections to the filing itself: 1. The Director quotation "has already been wrong once" is not in the document. Do not attribute it. The applicable rule is the one at line ~5915 quoted above. 2. The support line numbers cited (929, 978, 2954) are stale; current are 957, 1006, 2982, plus line 11. Scope widening -- this is not a single instance. Nine further stale "41" counts survive, of which the last three are unambiguous present-tense claims and should be fixed with it: - 5562: "**built** -- 41/41 components, 246/246 facets" (contradicts the elsewhere-stated "45 of 45 components fully derived - 270 of 270 facets answered") - 5736: "The 41 components in the register are boundaries a build enforces (P1)." - 5737: "**27 of the 41 components are not yet their own directory**" - 3558, 3569, 3580, 3602, 3613, 4369: "N of 41 components" inside `history` facet rows, each contradicting the Section-9 grant sweep (e.g. 3558 says client_opengl "links 32 of 41 components" against line 2035's "**client_opengl links 35 of 45 components.**"). These sit in a facet named `history`, so whether they are stale or deliberately retrospective is a judgement call for the author -- but the numerators moved too (32 vs 35), which suggests stale rather than historical.
- `medium` **L3033** The `rendering` grant cell states its rule as a *change from today's tree* rather than as a target-state fact. "Revoked" only means something relative to the current `source/rendering/CMakeLists.txt`, which is a Section 17 fact (and is already stated at lines 926–928).
  - **fix** Cell becomes: "**names no `game`**; `host` is what lets its driver paint it and its input reach the client". The revocation — deleting `${STAR_GAME_INCLUDES}` — belongs in Section 17.
  - **verifier corrected to** Line 3061 (not 3033), grant table row `rendering`: the cell states its rule as a delta rather than a target-state fact — "**`game` is revoked**; `host` is what lets its driver paint it and its input reach the client". "Revoked" only means something relative to today's `source/rendering/CMakeLists.txt`; the `granted` column already omits `game`, so the clause adds no target-state content. This is the one row in the grants table whose exclusion is stated only in change terms — every peer states it purely (3051 "Names no `host`", 3053 "names no `universe` and no `participant`", 3062 "**no `host`**", 3066/3068 "**names no domain type**", 3071 "**names no `world`**", 3072 "**names no `scene`**"), and the same component's own facet row at 3413 already reads "**May not name `game`**". The document forbids this explicitly at 5787 ("Part II describes the target state and nothing else — no migration, no actions against the current tree, no history"; Section 9 sits in Part II) and self-applies the same rule at 1165-1167 when it un-faded `gpu_sdl` because "that is a fact about today's tree and D7 forbids the target state from carrying one". Fix: reword the cell to "**names no `game`**; `host` is what lets its driver paint it and its input reach the client". No relocation work is needed — contrary to the original finding, the today-fact is NOT missing from elsewhere and is NOT solely a Section 17 fact: it is stated at 954-956 (Section 7, as the measurement that decides the split), 3415 (the `rendering` `history` facet), 5462 (Section 15 pass condition), 5729 (Section 16) and 5853 (Section 17 ordering step 7). Severity: editorial/purity, not correctness; column 3 is not machine-read (scripts/spec-model.py:117-128 parses only columns 1 and 2), so the reword is tooling-safe. Adjacent second instance not claimed by the finding: line 3058 `gpu_opengl` closes with "all of which sit in `host_sdl` today", though there the target rule is stated purely first and the today-clause trails as evidence.


## purity-2 (7 upheld)

- `high` **L5548** Section 15's instrument table is a rule/state table (claim · instrument · kind · state). The state cell carries a metered current-state size appended to the state word — the exact shape of the Director's "and its size is metered" example. It is also stale: the generated derivation ledger at line 3121 reads **45 of 45 components fully derived · 270 of 270 facets answered**.
  - **fix** Cell becomes: `**built**`. Deleting the counts changes nothing about what the row asserts. The figures already have a generated home — the `spec-derivations.py#ledger` block at line 3121 — which is the only copy that cannot drift.
  - **verifier corrected to** Line 5562 (filed as 5548; the file has drifted 14 lines) of /root/frackin/OpenStarbound/docs/superpowers/specs/2026-08-01-target-state-system-architecture.md, in Section 15's "The full instrument set" table: | **every component is reasoned for, not merely declared** | `spec_derivations` | anchoring | **built** — 41/41 components, 246/246 facets | The state cell appends a metered current-state size to the state word. It is the only row of the sixteen that puts a number in that column; the four siblings that carry a trailing clause (5559, 5560, 5561, 5566) carry explanation of the state, not measurement of it, and the row at 5554 whose claim is itself about coverage being complete states only `**built**`. The figures are also stale. `scripts/spec-derivations.py` reports `45 of 45 components fully derived, 270 of 270 facets answered`, and that same sentence is generated into line 3135 (filed as 3121) inside the `scripts/spec-derivations.py#ledger` block spanning 3133-3185. `spec-model.py load()` confirms 45 components. 41 x 6 = 246, so the cell predates platform_null, transport, transport_local and transport_tcp. Line 5562 lies outside every generated block in the document, so no generator can correct it. Fix: the cell becomes `**built**`. The deletion changes nothing the row asserts, and the counts already have a home that cannot drift. Not part of this finding, but noted while verifying: line 5601 in the same section reads "Whether forty components is the right forty is a judgement the aggregate review makes, not a gate" — a second hand-maintained count now stale against 45.
- `high` **L3179** `core`'s excludes facet states a rule (it grants nothing) and then appends an inbound-grant MEASUREMENT. It is also wrong now: the grants table has 43 rows and every one names `core`, and there are 44 other components (spec-model.py: 45 components, 43 grant rows). "it names none of them" merely restates "Names **nothing**".
  - **fix** Cell becomes: "Names **nothing** — zero grants, by construction, and that is what makes it a foundation rather than a very low library." The inbound count is the grants table's fact; if it is wanted at all it belongs in prose beneath the register or in the `history` facet (currently "none.").
  - **verifier corrected to** Line 3193 (not 3179), `core`'s **excludes** facet: "All forty other components name it" is factually wrong. Per scripts/spec-model.py there are 45 components — 44 other than `core` — and 43 grant rows, all 43 of which name `core`. The 44th is `base`, which has no grant row (line 3204: "Names **nothing**, as `core` does."), so both the number ("forty") and the universal ("All ... other components") are false. Fix in place, keeping the sentence: "... All forty-three components with grants name it; it names none of them." — or, matching the house style already used at line 3204 ("Twenty-four components name it.") and line 3292 ("Thirteen components name `game` and it names none of them"), "Forty-three components name it; it names none of them." Do NOT delete the clause or move it to `history`: the deletion test shows the rule ("Names **nothing** — zero grants, by construction, and that is what makes it a foundation rather than a very low library") is unchanged by the cut, so there is no rule impurity to remove, and the inbound count is a deliberate convention repeated in two other `excludes` cells. Related stale "forty" counts, out of scope for this cell but worth a separate finding: line 5601 and line 5764.
- `medium` **L5723** R4's `how it shows` cell reports a current measurement rather than naming the observation, and the measurement is stale twice over: the generated coverage block at line 5367 reads "**31 of the 45 components have no files yet** … `grant-sweep` reports them UNVERIFIABLE".
  - **fix** Cell ends at "…an all-or-nothing, directory-granular switch." If the reading must appear, cite the generated block rather than restating it: the count is owned by `scripts/spec-measures.py` and printed at line 5367.
  - **verifier corrected to** Line 5737 (not 5723), R4's `how it shows` cell: "**27 of the 41 components are not yet their own directory**, which is exactly the set `grant_sweep` reports UNVERIFIABLE." Defect: both numbers are stale by exactly one register generation. The authoritative value is generated at line 5381 -- "**31 of the 45 components have no files yet** ... `grant-sweep` reports them UNVERIFIABLE" -- and `scripts/spec-measures.py` reports OK, so 31/45 is current. The four components added since (`platform_null`, `transport`, `transport_local`, `transport_tcp`) have no files, giving 27->31 and 41->45 while the anchored count stays at 14 (line 5433, "the anchored fourteen"). The generator's own docstring (scripts/spec-measures.py:181-183) names this drift class as the reason it exists. The clause must NOT be deleted: the deletion test shows it is load-bearing. Without it the cell establishes only the CMake mechanism and never demonstrates that the claim at risk (line 5736, "The 41 components in the register are boundaries a build enforces (P1)") fails today. The correct fix is to stop restating the count and defer to the generated block, the move the document already makes at line 5455 ("it stands at the figure the generated block above reports") -- e.g. "...and the components that are not yet their own directory are exactly the set `grant_sweep` reports UNVERIFIABLE, at the figure the generated block in Section 15 reports." Two collateral instances of the identical stale pair, both hand-written and outside every generated and HISTORICAL block, should be fixed with it: line 5736 "The 41 components in the register", and the pull-quote at lines 5425-5426, "27 of the 41 are not yet, which is exactly the set `grant-sweep` calls UNVERIFIABLE".
- `medium` **L5411** This blockquote is an explicitly stated CONSTRAINT ("it deserves to be stated as one"). The rule ends at "…if and only if it is its own directory."; the rest is a current-state measurement inside the constraint, and it is stale — the generated block eleven lines above (5367) says 31 of 45.
  - **fix** Blockquote becomes the rule alone: "Under OBJECT-library semantics, containment is decided entirely by which libraries an ENTRYPOINT names. That is an all-or-nothing, directory-granular switch. **A component in this register is enforceable if and only if it is its own directory.**" Move the count sentence to the prose beneath, sourced from the generated coverage block.
  - **verifier corrected to** Stale component counts, three instances, all hand-written prose (not inside any GENERATED or HISTORICAL block). Line 5425 (blockquote, Section 15 "The altitude every gate above was blind to"): "> enforceable if and only if it is its own directory.** 27 of the 41 are not yet, which is exactly the > set `grant-sweep` calls UNVERIFIABLE — so the two numbers are not two problems, they are one problem > counted twice." Lines 5736-5737 (facets/claim-at-risk table): "| **claim at risk** | The 41 components in the register are boundaries a build enforces (P1). |" "| **how it shows** | ... **27 of the 41 components are not yet their own directory**, which is exactly the set `grant_sweep` reports UNVERIFIABLE. |" Ground truth: scripts/spec-model.py `load()` yields 45 components / 43 grants / 24 elements, and the generated coverage block at line 5381 states "**31 of the 45 components have no files yet** ... `grant-sweep` reports them UNVERIFIABLE". So 41 -> 45 and 27 -> 31 in all three places. The drift is exactly the four components added on 2026-08-02 (platform_null, transport, transport_local, transport_tcp), none of which have files. Secondary (structural, weaker): the blockquote at 5421-5427 is introduced as a constraint — "**The consequence is a hard constraint on this entire design, and it deserves to be stated as one:**" — yet mixes a timeless rule with a dated measurement. Cutting the measurement clause leaves the rule's meaning unchanged, so the proposed split is legitimate: keep the blockquote as "Under OBJECT-library semantics, containment is decided entirely by which libraries an ENTRYPOINT names. That is an all-or-nothing, directory-granular switch. **A component in this register is enforceable if and only if it is its own directory.**" and move the count plus the "one problem counted twice" identity into the prose beneath, sourced from the line-5381 generated block so it cannot drift again. Note this structural point does NOT apply to 5736-5737, which are already prose-in-a-table; those need only the number correction. Fixing 5425 alone would leave the document self-contradictory.
- `medium` **L5722** The claim-at-risk cell hard-codes a register size that is now wrong (45 components, per spec-model.py and the generated ledger at 3121). The count is not part of the claim — the claim is about components being build-enforced boundaries — so it is a measurement that only creates drift.
  - **fix** Cell becomes: "The components in the register are boundaries a build enforces (P1)."
  - **verifier corrected to** Line 5736 (not 5722), R4's claim-at-risk cell, hard-codes a stale register size: "The 41 components in the register are boundaries a build enforces (P1)" — the register holds 45 (spec-model.py; components table at 2863; generated ledger at 3135 and coverage row at 5376). The count is not part of the claim (P1 at line 483 states no cardinality, and deleting "41" changes nothing), so the claim cell should read "The components in the register are boundaries a build enforces (P1)." Scope correction the original finding missed: the same R4 block carries the stale number a second time at line 5737, "**27 of the 41 components are not yet their own directory**", and there the count IS load-bearing — it is R4's measured gap and cannot be deleted. The generated measure at line 5381 says "**31 of the 45 components have no files yet**", so 5737 must be re-measured to "31 of the 45" rather than de-numbered. The identical stale pair appears once more in the hard-constraint blockquote at line 5425: "27 of the 41 are not yet, which is exactly the set `grant-sweep` calls UNVERIFIABLE". A fix that touches only 5736 leaves the drift in place two lines below it.
- `medium` **L3190** `base`'s excludes cell states the rule and then reports an inbound-grant count. The count is accurate today (24 grant rows name `base`) but it is a measurement in a rule cell — delete it and the exclusion is unchanged — and it is the class of figure that goes stale silently, exactly as `core`'s did at line 3179.
  - **fix** Cell becomes: "Names **nothing**, as `core` does." If the popularity of `base` is load-bearing, it belongs with the falsifier ("if any service here has exactly one consumer") or in prose reading off the grants table.
  - **verifier corrected to** Line 3204 (not 3190), `base`'s excludes cell: "| **excludes** | Names **nothing**, as `core` does. Twenty-four components name it. |". The excludes facet is defined at line 3115 as "what it may not name, and why each exclusion holds" — an outbound rule — so the second sentence is an inbound measurement in a rule cell. It is accurate today (verified: 24 of 43 grant rows name `base`) and it is unchecked: prose-claims.py builds its COUNT_DRIFT corpus at line 455 by discarding every line beginning with "|", so no register table cell is scanned; spec-consistency.py's spelled-number map is used only for the kind-tally sentence at line 627; and line 3204 sits outside every generated block. The comparator is line 3193 (not 3179), and it has ALREADY gone stale: `core`'s cell says "All forty other components name it" when the register holds 45 components (44 others) and all 43 grant rows name `core`. Two refinements to the filed version: the failure mode is demonstrated, not merely predicted, so `core`'s line 3193 should be corrected in the same edit; and the 24 is additionally the wrong granularity to be mistaken for plurality evidence — `base`'s own owes cell at line 3207 says the falsifier ("if any service here has exactly one consumer") "is owed a count", i.e. consumers per service, not components naming `base`. Fix: cell becomes "Names **nothing**, as `core` does."; if `base`'s in-degree is load-bearing it belongs with the falsifier at 3205 or in prose that reads off the grants table where COUNT_DRIFT can see it.
- `medium` **L3588** Adjacent to the purity lens rather than inside it (these are exempt `history` facets), but every "of 41" figure in Part-III-adjacent Section 10 is now stale in BOTH numerator and denominator against the generated per-composition blocks: generated says client_opengl 35/45 (doc: 32/41, lines 3544, 3555), client_headless 30/45 (26/41, lines 3566, 3935, 3982, 5631), client_agent 22/45 (19/41, lines 3599, 3936, 3983; and "21 of 45" at 3986), server 16/45 (13/41, line 4355; and "15 of 45" at 5809), world_sim 13/45 ("12 of 45" at 3754), world_gen 12/45 (11/41, line 3588).
  - **fix** Re-derive every composition size from the generated `composition_graphs` blocks (lines 2021, 2246, 2321, 2406, 2511, 2642, 2842): 35, 35, 12, 13, 16, 22, 30 — all of 45. Better still, stop quoting them in prose: this is the same failure mode the document already records for the coverage table ("Every number in that table was hand-written, and every one had drifted").
  - **verifier corrected to** CONFIRMED, with corrected line numbers (the doc has drifted ~+14 lines from the filed anchors) and one sharpening. The quote is accurate, now at **line 3602**: "| **history** | It links 11 of 41 components — **the smallest composition in the register**, below `world_sim`'s 12 and `server`'s 13 — and it **replaces two dead utilities** …" Ground truth, re-derived twice (from the seven `<!-- BEGIN GENERATED: scripts/composition-graphs.py#… -->` blocks at lines 1813/2038/2263/2338/2423/2528/2659, and independently by taking the transitive grant closure via `scripts/spec-model.py` — both agree exactly): client_opengl **35/45**, client_sdl_gpu **35/45**, world_gen **12/45**, world_sim **13/45**, server **16/45**, client_agent **22/45**, client_headless **30/45**. Every prose/table figure below disagrees: Stale in BOTH numerator and denominator ("of 41"): - 3558 "It links 32 of 41 components — the largest composition" → 35 of 45 - 3569 "Also 32 of 41 components — same count, as it must be." → 35 of 45 - 3580 "26 of 41 components — the gap to `client_opengl`'s 32 …" → 30 of 45 (and the gap is 5, not 6) - 3602 "11 of 41 … below `world_sim`'s 12 and `server`'s 13" → 12 of 45, below 13 and 16 - 3613 "19 of 41 … `world_gen` links 11, `world_sim` 12 and `server` 13 … The 13 between 19 and `client_opengl`'s 32" → 22 of 45; 12, 13, 16; (the "13" gap survives by coincidence: 35−22=13) - 4369 "It links 13 of 41 components against `client_opengl`'s 32" → 16 of 45 against 35 - 3948/3949/3950 and 3995/3996/3997 table cells: 32 of 41, 26 of 41, 19 of 41 → 35, 30, 22 of 45 - 5645 "`client_headless` links 26 of 41" → 30 of 45 Stale in the numerator only (denominator already fixed to 45) — the finding lists these separately and correctly, so its blanket "BOTH" phrasing applies only to the "of 41" set: - 3768 "The generated diagram above shows it linking **12 of 45 components**: `core`, `base`, `platform_null`, `game`, `world`, `worldgen`, `celestial`, `net`, `script`, `storage`, `content`, and itself." → 13 of 45; the enumeration is also missing `platform`, which is in the real closure - 3869 and 5823 "the (dedicated) server links **15 of 45 components**" → 16 of 45 - 4000 "the composition is 21 of 45 components against the graphical client's 32" → 22 of 45 against 35 Two aggravating facts the finding did not state: 1. The document's own counting convention at **line 1807** ("**One counting convention, stated once.** *Links N of 41* is the closure INCLUDING the entrypoint — `server` links 13 because it is itself plus 12 dependencies") is itself stale in denominator and in its worked example, and it forecloses the "these are dependency counts" defence: it requires prose that counts dependencies to say so in the sentence, and none of 3768/3869/4000/5823 does. 2. The gate cannot see most of these. `scripts/prose-claims.py` builds its corpus as `prose = "\n".join(l for l in live.splitlines() if not l.startswith(("|", " ", "```", "<!--", "%%")))` (line 455) — every table-cell figure above is stripped — and its COUNT_DRIFT check validates only the total, skipping any "N of 45 components" subset form outright. `prose-claims: OK` is therefore not evidence these numbers are right; it is evidence nothing checks them. The same 41→45 drift also survives at 2916 ("a 41-row table"), 5425/5737 ("27 of the 41"), 5562 ("41/41 components, 246/246 facets" against the generated 3135 "45 of 45 components … 270 of 270 facets") and 5764 ("whether forty-one is the right forty-one"). Severity note: these sit in `history` facet rows and prose, not inside a rule clause, so the purity/deletion test does not apply — this is a factual-accuracy defect, not a rule-impurity one, exactly as the finding itself concedes. The ordinal claims ("smallest composition", "the floor among participant-bearing compositions") remain true; only the arithmetic is wrong. The proposed fix (re-derive from the generated blocks, or stop quoting sizes in prose) is sound; the 3602 sentence is the worst case because its stale "12" and "13" now name world_gen's and world_sim's *actual* sizes, so a reader cross-checking against the generated blocks gets a coherent-looking wrong answer.


## table-census (3 upheld)

- `high` **L967** THE #1 PROMOTION CANDIDATE. The KIND rules table (L967-972) is the definitional table for the register's primary axis — it states, for each kind, the rule that governs it — and it is an UNREAD prose table. It has already drifted: 5 rows naming CONTRACT, against spec-model.py:47 `KINDS = ("FOUNDATION", "INTERFACE", "VOCABULARY", "BACKEND", "LIBRARY", "ENTRYPOINT")` — 6 kinds, no CONTRACT. `python3 scripts/spec-model.py` reports 45 components whose kinds tally INTERFACE 8, VOCABULARY 3, BACKEND 12, LIBRARY 13, FOUNDATION 2, ENTRYPOINT 7. The register was updated; its own rule table was not. This is precisely the condition the document names at L42 ('nothing normative lives in prose alone').
  - **fix** Wrap it as `<!-- TABLE: kinds -->` / `<!-- END TABLE: kinds -->`, add a `kinds(text)` reader to scripts/spec-model.py returning {kind: rule}, and assert `set(kinds(text)) == set(KINDS)` so a kind added to the tuple without a rule row (or a row naming a dead kind) is a hard failure. Meanwhile split the CONTRACT row into INTERFACE ('declares an implementable interface; only an INTERFACE may be the target of `==>`') and VOCABULARY ('declares shared types everybody uses and nobody implements — nothing implements `Drawable`').
  - **verifier corrected to** Location: L981-987 (the finding cited L967; lines have drifted by 14). Accurate defect: the KIND rules table at L981-987 is the document's only definitional table for the register's primary axis, and it is stale against `scripts/spec-model.py:47`. It carries five rows — FOUNDATION (L983), CONTRACT (L984), BACKEND (L985), LIBRARY (L986), ENTRYPOINT (L987) — where the model declares six kinds with no CONTRACT among them. Concretely: one dead row (L984 `| **CONTRACT** | declares an interface and may name only foundation types. ... |`) and two missing rows (INTERFACE, VOCABULARY), plus a lowercase residue in the BACKEND row at L985 ("implements a contract"). No rule row for INTERFACE or VOCABULARY exists anywhere else in the spec. Correction to the finding's wording: "5 rows naming CONTRACT" is wrong. The table HAS five rows; exactly ONE of them names the kind CONTRACT. The correct statement is "a 5-row table whose kind column is stale: 1 dead kind, 2 missing kinds." Correction to the fix's framing: the necessary fix is the content repair — split the L984 row into INTERFACE ("declares an implementable interface; only an INTERFACE may be the target of `==>`") and VOCABULARY ("declares shared types everybody uses and nobody implements"), and lowercase-fix L985. The promotion to a machine-read `<!-- TABLE: kinds -->` block with a `kinds(text)` reader asserting `set(kinds(text)) == set(KINDS)` is a sound and well-motivated hardening (it is exactly the class of drift L42 forbids), but it is a recommendation, not part of the defect; the parallel ZONE table at L996-1001 is likewise unread and stayed correct, which shows unread-ness is not by itself the failure mode. Scope note: this table is one instance of a wider CONTRACT residue — 24 occurrences remain, including two normative `==>` rules at L1096 and L1160 that still say the target of `==>` is "a CONTRACT". Those are separate findings, not covered by this one.
- `high` **L5548** TWO WRITERS FOR ONE FACT, and the prose one is stale. This cell in the unread instrument-set table (L5536) restates the derivation coverage that the GENERATED block at L3121 already writes: '**45 of 45 components fully derived · 270 of 270 facets answered.**' The prose says 41/41 and 246/246. I confirmed by running spec-derivations' own parse: 45 of 45 components fully derived, 45 × 6 facets = 270. The four components added since (platform_null, transport, transport_local, transport_tcp) are invisible to this cell.
  - **fix** Delete the counts from the cell and leave `**built** — see the generated ledger in Section 10`, so the generated block at L3121 stays the sole writer. If the number is wanted here, make it a second injection point of scripts/spec-derivations.py rather than a hand-typed pair.
  - **verifier corrected to** Accurate version, with two strengthenings and one line-number correction. LOCATION: the cell is at L5562 (not 5548); the generated ledger sentence is at L3135, inside the block opened at L3133 by `<!-- BEGIN GENERATED: scripts/spec-derivations.py#ledger -->`. DEFECT (confirmed): L5562 hand-types "**built** — 41/41 components, 246/246 facets", restating a fact the generated ledger at L3135 already owns ("**45 of 45 components fully derived · 270 of 270 facets answered.**"). The hand-typed pair is stale by exactly the four components added on 2026-08-02 (platform_null, transport, transport_local, transport_tcp): 41+4=45, 246+24=270. This violates N2.a as stated at L386 ("**One fact has one writer.**") and its document-level restatement at L3109 ("because one fact gets one writer"). STRENGTHENING 1 — the cell is structurally invisible to BOTH readers, not merely "unread". It carries no `<!-- TABLE: name -->` marker (the file has only five such markers: shared-words L88, seam-methods L1602, components L2863, grants L3043, elements L4749), so spec-model.py never parses it. And scripts/prose-claims.py — which has a COUNT_DRIFT verdict written for exactly this class — currently reports "prose-claims: OK" because L455 builds its corpus as `prose = "\n".join(l for l in live.splitlines() if not l.startswith(("|", " ", "```", "<!--", "%%")))`, stripping every table row. In a paragraph, its `\b(%s|\d+)\s+components\b` scan would have matched 41 against a register of 45 and fired. So the gate that exists to catch this is disarmed by the fact that the claim sits in a table cell. STRENGTHENING 2 — the finding is under-scoped. The same stale-denominator class recurs in other hand-typed cells: L3558 "It links 32 of 41 components", L3569 "Also 32 of 41 components", L3580 "26 of 41 components" — all contradicted by the generated "**client_opengl links 35 of 45 components.**" at L2035. The counting-convention sentence at L1807, "**One counting convention, stated once.** *Links N of 41* is the closure INCLUDING the entrypoint", is itself stale and escapes COUNT_DRIFT only because no register noun follows the numeral. FIX: the proposed fix is sound — cut the counts and leave "**built** — see the generated ledger in Section 10", or make the cell a second injection point of scripts/spec-derivations.py (Section 15 already hosts a generated block at L5497-5506, so injection there is precedented). Additionally, either regenerate or de-number the "of 41" cells at L3558/3569/3580 and L1807, and consider whether prose-claims.py should stop stripping table rows for the COUNT_DRIFT scan specifically, since that exclusion is what let this survive.
- `medium` **L982** FOUR TAXONOMY TABLES DUPLICATE spec-model.py TUPLES WITH NO MARKER AND NO COMPARISON. ZONE rules (L982, 4 rows) vs ZONES at spec-model.py:48; element KIND (L4599, 4 rows) vs EKINDS:52; CADENCE (L4621, 7 rows) vs CADENCES:53; CARDINALITY (L4668, 5 rows) vs CARDINALITIES:54. All four currently agree — I checked row-by-row — which is exactly why they are a hazard rather than a bug: the KIND table at L967 was in the same state until the split, and nothing noticed it stop agreeing. Note also that spec-consistency.py:61-77 re-declares all five tuples a second time, so each taxonomy already has three writers (doc table, spec-model, spec-consistency).
  - **fix** Mark all four (`<!-- TABLE: zones -->`, `element-kinds`, `cadences`, `cardinalities`), add readers, and assert each parsed label set equals the corresponding spec-model tuple. Then delete the duplicate tuples at spec-consistency.py:61-77 in favour of `MODEL.ZONES` etc., per spec-model's own rule 3 ('ONE IMPLEMENTATION').
  - **verifier corrected to** FIVE taxonomy tables restate spec-model.py tuples with no TABLE marker and no gate comparison, and one of them is already wrong. Unmarked, unparsed taxonomy tables (line numbers corrected): - KIND rules, header L981, 5 rows, vs KINDS at spec-model.py:47 - ZONE rules, header L996, 4 rows, vs ZONES at spec-model.py:48 - element kind, header L4613, 4 rows, vs EKINDS at spec-model.py:52 - CADENCE, header L4635, 7 rows, vs CADENCES at spec-model.py:53 - CARDINALITY, header L4682, 5 rows, vs CARDINALITIES at spec-model.py:54 The KIND table is not a hazard, it is a live defect: L984 reads "| **CONTRACT** | declares an interface and may name only foundation types. Any `.cpp` holds **value-type constructors and trivial defaults only**..." while spec-model.py:47 reads KINDS = ("FOUNDATION", "INTERFACE", "VOCABULARY", "BACKEND", "LIBRARY", "ENTRYPOINT"). No gate sees it; prose-claims.py:66 still declares KINDWORD = r'(?:FOUNDATION|CONTRACT|BACKEND|LIBRARY|ENTRYPOINT)'. The other four agree today. This class has fired before on the same tables: prose-claims.py:441-446 documents "a markdown TABLE whose `| ZONE |` header sat two rows above its stale `| **SEAM** |` row", i.e. the L996 table when it had five rows. Writer count is five, not three, for KINDS/ZONES: the doc table, spec-model.py:47-48, spec-consistency.py:61-67, composition-graphs.py:49-50, and prose-claims.py:65-66. spec-consistency.py already imports MODEL (lines 58-59) and then restates the tuples anyway, contradicting spec-model.py's rule 3 "ONE IMPLEMENTATION". Fix: mark all five (`<!-- TABLE: kinds -->`, `zones`, `element-kinds`, `cadences`, `cardinalities`), add readers plus FLOOR entries, and assert each parsed first-column label set equals the corresponding spec-model tuple; fix L984 to the INTERFACE/VOCABULARY split; then delete the restated tuples in spec-consistency.py:61-77 and composition-graphs.py:49-50 in favour of MODEL.KINDS / MODEL.ZONES etc. Note the readers must not reuse _CELL, since these tables' first column is bold-unbackticked (`**MACHINE**`) in three of the five and bold-backticked (`**`LOOP`**`) in one.

